schema_version = 1

[repository]
source_root = "src/paraegox"
term_scan_roots = ["src", "crates", "configs"]
exception_scan_roots = ["src", "crates", "tests", "scripts", ".github"]
documentation_roots = ["README.md", "README_zh.md"]
untracked_only_roots = ["docs"]
allowed_top_level_directories = [".github", "configs", "crates", "docs", "scripts", "src", "tests"]
ignored_top_level_directories = [".git", ".mypy_cache", ".pytest_cache", ".ruff_cache", ".venv", "__pycache__", "build", "dist", "target"]

[registry]
# Do not add placeholders. Add rows only in the same change that creates the real surface.
feature_flags = []
deprecations = []

[[registry.waivers]]
id = "GOV-WAIVER-0001"
rule_id = "rust:expect"
scope = "crates/paraegox-runtime/src/lib.rs"
owner = "Runtime apply, installation, and state owner"
reason = "Keep the admitted B2/P2a state mechanisms, S4/P2b Loop composition, S5/P2c ThreadDomain/ExecutorBudget composition, the S6/P2d local POSIX ProcessDomain baseline, fixed two-CoreService lifecycle evidence, and the remaining owner-private S7 Runtime journal/startup/control mechanisms private behind the exact RuntimeHost entrypoint. S7-E consumes artifact observation, installation verification, sequence-one initialization, store preflight, install-file publication, authenticated bootstrap, and canonical PXAR apply through the real RuntimeHost executable path; S7-F adds the owner-private authenticated PXQR/PXQS query endpoint, explicit payload-v3-to-v4 and payload-v4-to-v5 offline migrations, and fixed-profile restart reassembly before listener publication. The remaining expectations cover internal DTOs and general Runtime assembly capabilities; they do not claim that the executable bootstrap/apply/query, fixed restart vertical, or deploymentd one-shot reconciliation consumer is absent. Remove or narrow the remaining expectations as each real executable consumer replaces them."
expires_at = "2026-09-30"

[[registry.waivers]]
id = "GOV-WAIVER-0002"
rule_id = "rust:expect"
scope = "crates/paraegox-deployment/src/lib.rs"
owner = "Deployment planning, tenure, projection, and apply-envelope owner"
reason = "Keep owner-internal Deck, plan, journal, tenure, projection, canonical-request, and wire types private behind the two exact process facades. S7-E has exact one-shot deploymentd consumers for installer-manifest ingress, DeckCompiler/Planner, durable Loop and Empty plan commits, the authenticated Controller-to-Authority tenure exchange, Runtime bootstrap, and PXAR apply/PXRT persistence. S7-F adds owner-private exact PXQR/PXQS journal/client mechanisms, an explicit offline journal migration, and the exact `reconcile-reference-once-v1` executable consumer without exposing those Rust DTOs. The remaining expectations cover internal types, general workload admission, continuous reconciliation, and wider deployment profiles; Runtime alone owns its fixed-profile restart reassembly, so Deployment creates no second restart authority. Remove or narrow each expectation only when a real executable consumer requires a public surface without exposing mutable state."
expires_at = "2026-09-30"

[[registry.waivers]]
id = "GOV-WAIVER-0003"
rule_id = "pytest:skip"
scope = "tests/system/test_s6_python_reference_worker.py"
owner = "ProcessDomain reference worker and system evidence owner"
reason = "The S6 reference-worker system harness deliberately exercises POSIX process groups, Unix signals, and descriptor semantics. Keep the explicit non-POSIX skip until a separately admitted platform backend and equivalent cleanup harness exist; this waiver does not skip any supported POSIX profile."
expires_at = "2026-09-30"

[[registry.waivers]]
id = "GOV-WAIVER-0004"
rule_id = "rust:ignore"
scope = "crates/paraegox-runtime/src/process_domain.rs"
owner = "Runtime ProcessDomain cross-language evidence owner"
reason = "Only the exact python_reference_worker_round_trips_through_rust_process_domain test is ignored by the Rust-only job because it requires the uv-managed interpreter; the uv CI job executes it explicitly. Remove this split-run exception when the P2e production Artifact runner or an independent cross-language suite becomes the sole owner of equivalent Rust-host/Python-worker evidence."
expires_at = "2026-09-30"

[[registry.waivers]]
id = "GOV-WAIVER-0005"
rule_id = "rust:ignore"
scope = "crates/paraegox-runtime/src/process_domain.rs"
owner = "Runtime ProcessDomain native-worker system evidence owner"
reason = "Only the exact rust_reference_worker_faults_preserve_uncertain_ownership_and_cleanup test is ignored by the default library test because it consumes the separately built reference-worker executable by absolute path. CI builds that executable and runs this test explicitly; remove the split-run exception when P2e provides the production Artifact runner and equivalent end-to-end evidence."
expires_at = "2026-09-30"

[[registry.waivers]]
id = "GOV-WAIVER-0006"
rule_id = "pytest:skip"
scope = "tests/system/test_s7_tenure_authority_process.py"
owner = "Deployment tenure-authority POSIX system evidence owner"
reason = "The S7-D Authority harness deliberately exercises Unix peer credentials, owner/group/mode ACLs, Unix-domain sockets, signals, advisory descriptor locks, and stale-socket recovery. Keep the explicit non-POSIX skip until a separately admitted platform authority backend has equivalent credential, locking, commit-before-reply, and crash-recovery evidence; this waiver does not skip any supported POSIX reference profile."
expires_at = "2026-09-30"

[[registry.waivers]]
id = "GOV-WAIVER-0007"
rule_id = "pytest:skip"
scope = "tests/system/test_s7_tenure_authority_process.py"
owner = "Deployment tenure-authority service-account isolation evidence owner"
reason = "The real successful acquire/replay/crash-recovery and wrong-peer store scenarios require the exact Linux ext4 production state-store profile; the former also switches the Authority binary to a distinct non-root OS account while the test client remains the Controller peer. GitHub's Linux reference runner provides non-interactive sudo and executes both evidence paths. macOS APFS remains fail-closed because mode bits cannot prove the absence of inherited extended ACLs and the workspace has no admitted FD-anchored ACL backend; local macOS retains pure functional, argument, and unsafe-configuration evidence only. Remove or narrow this platform split after PC1 admits APFS extended-ACL, durability, and dedicated non-interactive service-account evidence."
expires_at = "2026-09-30"

[[registry.waivers]]
id = "GOV-WAIVER-0008"
rule_id = "pytest:skip"
scope = "tests/system/test_s7_runtime_install_process.py"
owner = "RuntimeHost release/install production-profile system evidence owner"
reason = "The successful end-to-end S7-E Runtime install scenario requires the exact Linux ext4 Runtime store profile, non-interactive sudo to provision a root:root mode-0555 installed RuntimeHost executable, and execution under a distinct non-root service identity. macOS APFS and non-Linux hosts remain fail-closed and may run only non-production argument and rejection evidence; they must not turn this skip into a support claim. Remove or narrow the split after PC1 admits FD-anchored APFS extended-ACL, durability, launchd/service-identity evidence and PC2 independently admits the corresponding Windows backend evidence."
expires_at = "2026-09-30"

[[registry.waivers]]
id = "GOV-WAIVER-0009"
rule_id = "pytest:skip"
scope = "tests/system/test_s7_deployment_process.py"
owner = "DeploymentController production-profile process evidence owner"
reason = "The Linux-only S7-E DeploymentController harness requires exact ext4 Runtime, Controller, and Authority stores plus non-interactive sudo. It provisions root:root mode-0555 RuntimeHost, deploymentd, and tenure-authority executables and three distinct non-root Runtime, Controller, and Authority service accounts, with setgid socket directories, mode-0660 sockets, and Unix peer-credential checks. Ubuntu CI run 30748840399 at commit 1ed704c verified only the S7-E baseline with 372 pytest passes and no skips, including actual execution of its initialize, Loop commit, authenticated bootstrap and tenure, Loop apply, Empty commit/apply, terminal replay, and fail-closed no-mutation process fixture. The current S7-F/SF6 source additionally covers lost-response query routing, zero extra PXAR, real Runtime SIGKILL/same-store restart, epoch advance, network-inaccessible terminal-decision replay, and Empty exact-zero convergence; those additions remain pending a fresh Ubuntu CI run and are not claimed Linux-validated here. macOS APFS and non-Linux hosts remain fail-closed and may run only non-production argument, parser, and pure state-transition evidence; they must not turn this skip into a support claim. Remove or narrow the split after PC1 admits FD-anchored APFS extended-ACL, durability, launchd/service-identity evidence and PC2 independently admits the corresponding Windows backend evidence."
expires_at = "2026-09-30"

[[registry.waivers]]
id = "GOV-WAIVER-0010"
rule_id = "rust:allow"
scope = "crates/paraegox-runtime-contracts/src/reference_*.rs"
owner = "Canonical reference-control contract owner"
reason = "The canonical terminal Receipt constructor, signing transcript, and strict wire encoder intentionally preserve the one fixed protocol field order in one place; grouping those fields would introduce a second protocol DTO or obscure review of byte order. Keep this narrow lint exception until a contract-version migration can replace the field list atomically across producer, verifier, and frozen-wire evidence."
expires_at = "2026-09-30"

[[registry.waivers]]
id = "GOV-WAIVER-0011"
rule_id = "rust:allow"
scope = "crates/paraegox-deployment/src/controller_*.rs"
owner = "DeploymentController bootstrap and apply provisioning owner"
reason = "The sealed fixed-profile provisioning constructors require independently validated identity, manifest, endpoint, key, and lifecycle facts at their trust boundary. Keep this narrow argument-count exception while the executable vertical is integrated; remove it only if a replacement value object preserves validation ownership without exposing caller-constructible protocol state."
expires_at = "2026-09-30"

[[registry.waivers]]
id = "GOV-WAIVER-0012"
rule_id = "rust:allow"
scope = "crates/paraegox-runtime/src/runtime_control_state.rs"
owner = "Runtime control-state and apply owner"
reason = "The private apply child module remains colocated with the control-state owner during S7-E endpoint integration, and one fixed test helper names the full expected request boundary. Remove the dead-code exception when the production endpoint is the sole module owner and replace the test-helper exception when a sealed fixture can preserve the same independent field coverage."
expires_at = "2026-09-30"

[[registry.waivers]]
id = "GOV-WAIVER-0013"
rule_id = "pytest:skip"
scope = "tests/system/test_[gt][12]_public_*_cli.py"
owner = "G2/T1 public DeveloperLocal Node and Deployment process-smoke owner"
reason = "The exact G2 deployment and T1 Agent-bootstrap process harnesses have only three admitted execution preconditions: Linux signal, process-group, uid/gid and setpriv semantics; initial root authority solely to prepare owner/mode-isolated fixtures before every ParaEGOX process is dropped to one non-root uid/gid; and PARAEGOX_PUBLIC_CLI_BINARY naming an already-built binary from the exact immutable ref under validation so neither harness builds or implicitly selects a workspace binary. Each missing precondition has one inline skip in each of those two exact files; functional failure, readiness failure and scenario-specific unavailability are never skippable. Remove or narrow the corresponding skip when an equivalent admitted platform and privilege-isolation harness exists or when the exact-ref validation runner supplies the required binary unconditionally."
expires_at = "2026-09-30"

[[registry.packages]]
path = "crates/paraegox-kernel"
cargo_package = "paraegox-kernel"
owner = "Kernel mechanisms"
responsibility = "Canonical digest framing, identity values, and owner-local monotonic time values shared by admitted mechanism owners."
status = "public"
public_entrypoints = ["paraegox_kernel::digest", "paraegox_kernel::identity", "paraegox_kernel::time"]
consumers = ["paraegox-runtime-contracts", "paraegox-runtime", "paraegox-deployment"]
first_tests = ["crates/paraegox-kernel/src/digest.rs", "crates/paraegox-kernel/src/identity.rs", "crates/paraegox-kernel/src/time.rs"]
removal_condition = "Removal requires migrating every registered mechanism consumer and its canonical digest, identity, or time evidence."

[[registry.packages]]
path = "crates/paraegox-runtime-contracts"
cargo_package = "paraegox-runtime-contracts"
owner = "Runtime contract owner"
responsibility = "Source-plan provenance, canonical PXTA binding assignments, canonical PXTE v1 Loop, additive PXTE v2 Loop+Thread, additive PXTE v3 Loop+Thread+Process, narrow reference PXTE v4/PXAR v5 assignments, composite target digests, complete Runtime apply requests, strict PXWP v1 worker-session framing, apply-control commitments, writer-tenure signing and fingerprints, temporal constraints, and the canonical signed apply-envelope wire contracts. S7-E publicly exposes the strict installation seam around the existing canonical Runtime build descriptor and singleton compatibility manifest owners plus a narrow `reference_control` facade over the already canonical reference request, bootstrap, lifecycle, profile, and key-fingerprint owners. The first S7-F contract tranche additionally exposes the existing canonical authenticated PXQR/PXQS query owner and a strict durable Slice verifier that requires complete caller-supplied `PlanProvenance`, rebuilds the target Slice commitment, and never infers a missing `SourcePlanRef` from PXTA/PXTE bytes. The additive managed-service and managed-fabric tranche defines a single Runtime-owned lifecycle spec, PXMP v1 projection derived from verified installation ingress, PXTE v5/PXAR v6 desired/request bytes, and an independent signed PXFT v1 terminal without reinterpreting frozen PXTE v4/PXAR v5/PXRT v1. The distributed fixed-stack tranche adds PXDP/PXDT/PXTE v7/PXAR v8 desired bytes for exactly one FabricService-owned Zenoh Session with loopback TCP plus explicit remote mTLS endpoints, opaque credential/peer-identity references, and a separate unsigned PXTP observation payload that requires an authenticated carrier. The additive managed Model+Agent contract defines a fixed Fabric→Agent and Model→Agent dependency profile, complete provider/adapter binding, PXTE v8/PXAR v9 desired bytes, and an independent signed PXMT v1 terminal while leaving frozen predecessors untouched. These contracts do not by themselves create a Runtime endpoint, Controller producer, restart reassembly, Fabric session, credential resolver, TLS handshake, or general service graph. Deployment and Runtime consume the contracts without introducing a second codec. Executable observations and independently compiled facts can generate the one descriptor, the installer can generate the one manifest without accepting manifest input, and Runtime initialization can strictly reverify both exact artifacts. Desired Process profile references still do not enforce a sandbox or launch an OS process, and this package creates neither installer filesystem/process authority nor control-plane transport authority. G2 additively places Describe, frozen PXFB and frozen PXQR behind one Controller-signed PXCC/PXCB carrier and returns one Runtime-signed PXDR for current LegacyReady or ManagedReady facts. The embedded ReferenceChannelBinding remains the existing Runtime-local UDS/process binding and is not redefined as a TLS channel. T2-A additively defines the asymmetric remote-Agent PXAE/PXAD/PXTE-v9/PXAR-v10/PXAU state graph and its independent PXRA/PXRR authenticated access carrier, including a shared frozen PXAP payload-digest helper; it is contract/schema and state-transition evidence only, not a running remote-Agent capability or a new lifecycle, transport, access, or UI owner."
status = "public"
public_entrypoints = ["paraegox_runtime_contracts::assignment", "paraegox_runtime_contracts::execution", "paraegox_runtime_contracts::thread_execution", "paraegox_runtime_contracts::process_execution", "paraegox_runtime_contracts::process_protocol", "paraegox_runtime_contracts::provenance", "paraegox_runtime_contracts::apply", "paraegox_runtime_contracts::temporal", "paraegox_runtime_contracts::wire", "paraegox_runtime_contracts::installation", "paraegox_runtime_contracts::reference_control", "paraegox_runtime_contracts::managed_service", "paraegox_runtime_contracts::managed_fabric_plan", "paraegox_runtime_contracts::managed_serving_bootstrap", "paraegox_runtime_contracts::managed_agent_stack_plan", "paraegox_runtime_contracts::managed_model_agent_stack_plan", "paraegox_runtime_contracts::distributed_agent_stack_plan", "paraegox_runtime_contracts::remote_agent_data_plane_plan", "paraegox_runtime_contracts::remote_agent_access"]
consumers = ["paraegox-deployment", "paraegox-runtime", "paraegox-fabric"]
first_tests = ["crates/paraegox-runtime-contracts/src/assignment.rs", "crates/paraegox-runtime-contracts/src/execution.rs", "crates/paraegox-runtime-contracts/src/thread_execution.rs", "crates/paraegox-runtime-contracts/src/process_execution.rs", "crates/paraegox-runtime-contracts/src/process_protocol.rs", "crates/paraegox-runtime-contracts/src/provenance.rs", "crates/paraegox-runtime-contracts/src/apply.rs", "crates/paraegox-runtime-contracts/src/temporal.rs", "crates/paraegox-runtime-contracts/src/wire.rs", "crates/paraegox-runtime-contracts/src/installation.rs", "crates/paraegox-runtime-contracts/src/reference_control.rs", "crates/paraegox-runtime-contracts/src/managed_service.rs", "crates/paraegox-runtime-contracts/src/managed_fabric_plan.rs", "crates/paraegox-runtime-contracts/src/managed_serving_bootstrap.rs", "crates/paraegox-deployment/src/distributed_agent_stack_node_reconcile.rs", "crates/paraegox-fabric/src/runtime_apply.rs", "crates/paraegox-runtime-contracts/src/managed_agent_stack_plan.rs", "crates/paraegox-runtime-contracts/src/managed_model_agent_stack_plan.rs", "crates/paraegox-runtime-contracts/src/distributed_agent_stack_plan.rs", "crates/paraegox-runtime-contracts/src/remote_agent_data_plane_plan.rs", "crates/paraegox-runtime-contracts/src/remote_agent_access.rs", "crates/paraegox-runtime-contracts/tests/fixtures/distributed_agent_stack_v1.hex", "crates/paraegox-runtime-contracts/tests/remote_agent_access_contract.rs", "crates/paraegox-deployment/src/deployment_process.rs", "crates/paraegox-runtime/src/runtime_control_state.rs", "crates/paraegox-runtime/src/runtime_artifact.rs", "crates/paraegox-runtime/src/runtime_initializer.rs", "crates/paraegox-runtime/src/runtime_host_entrypoint.rs", "tests/contract/test_s5_runtime_thread_execution_request.py", "tests/contract/test_s6_runtime_process_execution_request.py", "tests/contract/test_s6_process_worker_protocol.py", "tests/contract/test_s7_managed_fabric_successor.py", "tests/contract/test_s7_managed_serving_bootstrap.py", "tests/contract/test_s7_managed_agent_stack_successor.py", "tests/contract/test_t2_remote_agent_access.py", "tests/fixtures/wire/t2_remote_agent_data_plane_v1.json", "tests/fixtures/wire/t2_remote_agent_access_v1.json", "tests/system/test_s7_runtime_install_process.py"]
removal_condition = "Removal requires replacing both the independent Deployment producer and Runtime consumer in one accepted contract migration."

[[registry.packages]]
path = "crates/paraegox-runtime"
cargo_package = "paraegox-runtime"
owner = "Runtime apply, installation, and state owner"
responsibility = "Signed-request admission and apply-control transitions through strict canonical apply-request decoding, bounded Mailbox and PortBinding, the current-thread RuntimeHost reactor and structured task owner, the strict PXHW v1 inherited-stream watchdog endpoint, S4 LoopDomain dispatch, S5 RuntimeHost-owned ExecutorBudget/ThreadDomain, and the private S6 local POSIX ProcessDomain baseline remain implemented at their previously admitted evidence levels. S7-D/W1 provides the crate-private bounded Runtime journal/state machine and owner-private Unix journal store with strict path/identity validation, one-writer locking, bounded canonical reads, atomic file/directory-sync publication, restart precedence, and fail-closed uncertainty; ProductionReference remains exact Linux GNU ext4 while macOS APFS fails closed pending PC1 evidence. S7-E adds the one-shot initializer, compiled-actual verification, pinned final-executable observation, durable installation, and exact `release-descriptor-v1` and `install-v1` commands. S7-F payload v5 persists complete contract-owned Slice provenance across prepared, active, retiring, recovery, and terminal lineages, the exact prepared request-time response channel, and a distinct reserved-at-crash exact-zero resource shape that never fabricates ownership identities. Normal startup accepts only v5. The frozen `migrate-journal-v3-to-v4-v1` still targets exact payload v4, while `migrate-journal-v4-to-v5-v1` accepts only lossless states with no prepared or recovery action; both run stopped under the same lock with separately durable read-only source evidence and canonical PXMR receipts. The exact `serve-bootstrap-v1` mode runs one authenticated owner-private Unix control endpoint: the same bounded four-byte-framed channel strictly handles canonical PXBR bootstrap, PXQR query and PXAR v5 apply requests, then the explicit PXFB/PXAR6/PXAR7/PXAR8/PXAR9 successors only under their admitted cutover/profile rules. Before a listener capability exists, fixed-profile startup validates every durable Slice, terminal Receipt, lineage, and historical channel, invalidates the old generation, converges interrupted normal/takeover resources to exact-zero or operational quarantine, and performs at most one bounded fresh recovery action; a durable post-intent restart never replays the callback. Query is a read-only projection of durable plus live owner state, returns a Runtime-signed and request-correlated PXQS, and never performs recovery or mutates authority. Apply revalidates socket identity and peer credentials, authenticates Controller and Authority signatures plus manifest/policy/channel/store/clock/CAS pins, drives the fixed reference Loop or exact-zero Empty native owner, and returns a canonical Runtime-signed PXRT terminal Receipt. Historical terminal replay is byte-identical and authenticated against the original request-time channel; fresh apply uses a sealed one-use post-tenure token and repeated owner-clock fences. Installed-production observation requires a root:root mode-0555 final executable; non-Linux artifact observation and non-ext4 production state fail closed. The deploymentd facade now consumes PXQR/PXQS through an exact one-shot reconciliation command, but a continuous Controller reconciler, wider plan profiles, and a complete general RuntimeAssemblyEngine remain unimplemented. The additive Unix DeveloperLocal lifecycle observes and hashes its actual executable, strictly initializes or reopens one owner-private Runtime store, serves authenticated PXBR before an authenticated PXFB performs the one-way managed cutover on the same UDS identity, recovers the Runtime-owned Fabric, Model, and Agent generations, privately owns the sole two-lane PXAP-over-Fabric transport and descriptor implementation, issues only an opaque conversation handle after exact PXMT ActiveReady correlation, and joins Agent, Model, Fabric, UDS and owner-thread shutdown. This DeveloperLocal path is an explicit non-production consumer and does not weaken ProductionReference filesystem or service-account policy. The additive managed Model+Agent owner now durably executes PXAR v9 on the same authenticated endpoint, resolves and verifies the exact Model plan and adapter binding, starts Model before Agent, exposes only a Ready generation-fenced Agent dependency, signs PXMT, and drains/stops Agent then Model before Fabric; it remains a fixed profile rather than a general assembly graph. Restart reassembly covers only the compiled-in in-process reference Loop/Empty and fixed Fabric-to-Agent and Fabric/Model-to-Agent profiles; it does not add general readiness, recover Thread/Process assembly, or solve the existing independently grouped ProcessDomain orphan/containment limitation. PXHW still proves only reactor progress and Process launch resolution remains a trusted caller seam. The G2 DeveloperLocal control option keeps the exact PXRP/PXCB transport pins but selects the typed PXCC/PXDR bounds. It authenticates the outer Controller carrier before dispatch, reports Runtime-owned LegacyReady or ManagedReady facts, serves the frozen PXQR only while the legacy owner exists, and performs the existing durable PXFB cutover on the same listener; managed mode rejects PXQR rather than reinterpreting predecessor state. Ready publication now retains and revalidates the exact Runtime-local channel, store, manifest and build facts used by PXDR, without treating that UDS channel as TLS or distributed readiness. T2-D0 additively provides only a crate-private PXOJ v1 bounded canonical state codec and a fakeable exactly-one Open-plus-literal-Echo owner. Prepared commits the exact attempt, Echo request, target/store/epoch/PXAU/PXST/profile/client scope, trusted PXCB proof scope, and two distinct preallocated Describe challenges. A private verified bundle binds exact scope, both signatures, strict PXAP projection and signed generations to the sole action-owned data-plane binding; each send action exists only after a successful claim commit, Uncertain never recreates one, an unknown commit outcome poisons the in-memory object until durable reload, and terminal resume locally revalidates both proofs without external calls. T2-C0 additively provides one crate-private, bounded PXDE v1 latest-slot ledger inside the existing ManagedFabricStore under the same `runtime.lock`. Each strict slot retains the byte-exact authenticated PXAG and PXAH, whole-PXAH receipt digest, shared-domain PXAP payload digest, active PXST digest, target, Runtime store instance, RuntimeHost epoch, Fabric and Agent generations, request digest, slot sequence, previous-slot digest and complete record checksum. Its commit-before-reply path writes and syncs a private single-link temporary file, revalidates the directory, held writer lock, Agent-stack authority and prior final identity, renames and syncs the directory, then strictly reads back byte-identical final data before moving in-memory authority. The endpoint performs post-commit live reverification of both retained signatures plus independently exported current descriptor, PXST, target, store, epoch and generation facts before returning the exact PXAH. Temporary files are never recovery candidates, and pre-publication or post-rename uncertainty fail-stops the owner until strict reopen."
status = "experimental"
public_entrypoints = ["paraegox_runtime::host_watchdog", "paraegox_runtime::RuntimeAgentConversationHandle", "paraegox_runtime::RuntimeAgentConversationError", "paraegox_runtime::run_runtime_host_process", "paraegox_runtime::run_runtime_host_entrypoint", "paraegox_runtime::{RuntimeFabricCredentialRequirementV1, RuntimeFabricCredentialResolveError, RuntimeFabricCredentialResolveErrorV2, RuntimeFabricCredentialResolverV1, RuntimeFabricCredentialResolverV2, RuntimeResolvedFabricCredentialFilesV1, RuntimeResolvedFabricPeerCredentialV2}", "paraegox_runtime::{RuntimeModelBackendResolveError, RuntimeResolvedModelBackendV1, RuntimeModelBackendResolverV1}", "paraegox_runtime::{RuntimeAgentProviderResolveError, RuntimeResolvedAgentProviderV1, RuntimeAgentProviderResolverV1, RuntimeDeveloperLocalIdentityRefsV1, RuntimeDeveloperLocalSigningSeedsV1, RuntimeDeveloperLocalIdentityV1, RuntimeDeveloperLocalDistributedAgentStackConfigV1, RuntimeDeveloperLocalConfigV1, RuntimeDeveloperLocalReadyV1, RuntimeDeveloperLocalLifecycleV1, RuntimeDeveloperLocalError, start_runtime_developer_local_v1}", "paraegox_runtime::{RuntimeAgentDeveloperLocalBootstrapV1, RuntimeAgentDeveloperLocalConversationV1, RuntimeAgentDeveloperLocalIpcClientV1, RuntimeAgentDeveloperLocalIpcConfigV1, RuntimeAgentDeveloperLocalIpcError, RuntimeAgentDeveloperLocalIpcLifecycleV1, RuntimeAgentDeveloperLocalIpcLimitsV1, RuntimeAgentDeveloperLocalIpcPathsV1, start_runtime_agent_developer_local_ipc_v1}"]
current_capability_limit = "The additive experimental PXAR8 path invokes RuntimeFabricCredentialResolverV2 only, with no V1 fallback, and starts one lifecycle-owned Fabric generation. It accepts a live-link snapshot only when the control handle and Runtime-held generation match, the Fabric owner supplies a typed nonzero session epoch, and every peer binding digest and owner-issued sequence matches the complete configured peer count and canonical order. PXDA v2 binds one exact local Evidence store epoch/owner, persists a bounded EvidenceCommitIntent containing one canonical PXTP RuntimeFact per peer, appends each record to LocalEvidenceStoreV1, and verifies its typed commit receipt plus exact readback. Only after that verified commit may Runtime start the existing ManagedAgentAssembly on the same generation-fenced Fabric control, require an initial zero binding census, verify the two installed request/event descriptor digests, the contract-owned binding-set digest and physical census two, then atomically clear the handoff while retaining the Evidence owner head and durably persist signed ActiveReady before publishing an opaque Agent handle. A broker-publication failure is reported as CommittedHandleUnavailable and can be retried from authenticated terminal replay; loss of the durable/lifecycle owner remains the distinct CommittedOwnerRestartRequired outcome. Restart never uses an old PXTP batch as authority to recreate Agent effects: phase-11/Committed and prior ActiveReady recovery advance Fabric and Agent generations through a fresh RecoveryIntent, require a new Fabric session and commit a new PXTP/PXEV batch while preserving historical terminal bytes. Shutdown revokes the handle and retires Agent plus both bindings before Fabric; uncertain Agent cleanup retains Fabric authority and fails closed into quarantine/restart recovery. Remote focused evidence validates the host-local path from an already validated snapshot through durable Evidence, real local bindings, ActiveReady, broker claim, labelled Echo conversation and Agent-first cleanup. That boundary does not validate remote link observation, restricted cutover, a credentialed real model, production SAN/fingerprint identity, a real two-host mTLS system, partition/reconnect behavior, continuous reconciliation/link health, a public distributed CLI/TUI composition, or an Evidence service/replication plane. Additive T1 routes exact PXAG/PXAH over the existing restricted Runtime-control listener. Runtime authenticates Controller and current PXCB facts before the sole mutable seam delegates Apply to the existing PXAR-v6/v7 owners; committed or exact resident replay produces a fresh current PXAH, while historical PXST is byte-preserved only after revalidation and never causes another successor transition. Describe exposes only the current active Runtime-owned opaque descriptor rooted in the exact PXST and current generations. Runtime remains the sole Agent transport/descriptor/effect owner, and this bootstrap boundary provides no descriptor access, authorization, live session, remote Agent conversation, Echo, reconnect or TUI proof. T2-D0 has only the commit trait and deterministic in-memory fake; it has no filesystem implementation, APFS durability, atomic store, real Controller Describe exchange or discovery-currentness proof. DescribeSource and OnceTransport remain fake seams, not a Mac data-plane connector or Zenoh composition. PXCB is retained only as exact trusted proof scope and is not connector configuration. There is no public API, CLI, deployed remote Agent or Echo, reconnect or partition owner, TUI, two-host evidence, or production-platform claim. Ubuntu r164 at exact source `03c67bdda436e89a1d06f38706984a6bbf3a0d9c` passed formatting, Runtime all-target check, Clippy with warnings denied, all 21 focused D0 tests, and the full Runtime suite with 626 passed, zero failed and two ignored. T2-C0 remains owner-private latest-slot evidence only. Its `record_sequence` and `previous_record_digest` express replacement continuity inside the current slot; because prior slots are not retained and no external monotonic value is consulted, they prove neither historical authenticity nor anti-rollback, durable high-water or key-rotation continuity. After the first PXDE exists, an older binary is expected to fail closed on reopen; downgrade compatibility is not guaranteed. Retrying the exact PXAG creates the next PXDE sequence and repeats the temporary write, file sync, rename, directory sync and exact readback even when the deterministic signed PXAH is byte-identical; there is no idempotent no-write guarantee, bounded write-amplification guarantee or SSD lifetime/performance SLO. The seven focused PXDE/store unit tests cover strict codec, replacement, temporary-file and injected pre/post-rename branch semantics only; they are not process-abort or power-cut certification. PXDE is not a PXRA dispatcher, not a descriptor or access capability and not proof of deployed remote-Agent reachability; it grants no access authorization, live session, remote conversation or Echo, connector activation, reconnect or partition recovery, TUI, two-host result or production-platform claim. Ubuntu r176 at exact source `9eb8e6a7e61d0ba6149d4313dd66bce1b2f2ff3f` passed formatting, Runtime all-target check, Clippy with warnings denied and the exact endpoint test 1/1 as `nobody` with a link-count-1 test binary. The r174 mechanism baseline `baa64655f1f15c6ec03c6ad170fc3b0bbfd1a3fc` passed all seven focused PXDE/store tests and the full Runtime suite with 633 passed, zero failed and two ignored."
consumers = ["paraegox-runtime-host", "paraegox-local"]
first_tests = ["crates/paraegox-runtime/src/admission.rs", "crates/paraegox-runtime/src/apply_state.rs", "crates/paraegox-runtime/src/request.rs", "crates/paraegox-runtime/src/mailbox.rs", "crates/paraegox-runtime/src/port_binding.rs", "crates/paraegox-runtime/src/runtime_clock.rs", "crates/paraegox-runtime/src/task_registry.rs", "crates/paraegox-runtime/src/dispatcher.rs", "crates/paraegox-runtime/src/loop_domain.rs", "crates/paraegox-runtime/src/card_instance.rs", "crates/paraegox-runtime/src/card_executor.rs", "crates/paraegox-runtime/src/component_runtime.rs", "crates/paraegox-runtime/src/thread_component_runtime.rs", "crates/paraegox-runtime/src/executor_budget.rs", "crates/paraegox-runtime/src/thread_domain.rs", "crates/paraegox-runtime/src/thread_registry.rs", "crates/paraegox-runtime/src/process_domain.rs", "crates/paraegox-runtime/src/process_platform.rs", "crates/paraegox-runtime/src/process_transport.rs", "crates/paraegox-runtime/src/process_workspace.rs", "crates/paraegox-runtime/src/liveness.rs", "crates/paraegox-runtime/src/recovery.rs", "crates/paraegox-runtime/src/runtime_ownership.rs", "crates/paraegox-runtime/src/runtime_journal.rs", "crates/paraegox-runtime/src/runtime_store.rs", "crates/paraegox-runtime/src/remote_agent_descriptor_evidence.rs", "crates/paraegox-runtime/src/runtime_control_state.rs", "crates/paraegox-runtime/src/runtime_reference_apply.rs", "crates/paraegox-runtime/src/runtime_reference_owner.rs", "crates/paraegox-runtime/src/runtime_control_endpoint.rs", "crates/paraegox-runtime/src/runtime_developer_local.rs", "crates/paraegox-runtime/src/runtime_provisioning.rs", "crates/paraegox-runtime/src/runtime_initializer.rs", "crates/paraegox-runtime/src/runtime_artifact.rs", "crates/paraegox-runtime/src/runtime_build_metadata.rs", "crates/paraegox-runtime/src/runtime_install_files.rs", "crates/paraegox-runtime/src/runtime_host_entrypoint.rs", "crates/paraegox-runtime/src/runtime_agent_developer_local_ipc.rs", "crates/paraegox-runtime/src/bin/paraegox-rust-reference-worker.rs", "crates/paraegox-runtime/tests/rust_reference_worker.rs", "crates/paraegox-runtime/src/core_service.rs", "crates/paraegox-runtime/src/managed_service_assembly.rs", "crates/paraegox-runtime/src/managed_fabric_runtime.rs", "crates/paraegox-runtime/src/managed_fabric_state.rs", "crates/paraegox-runtime/src/managed_agent_transport/tests.rs", "crates/paraegox-runtime/src/managed_agent_transport/port_descriptor.rs", "crates/paraegox-runtime/tests/fixtures/agent_conversation_port_descriptor_v1.hex", "crates/paraegox-runtime/src/remote_agent_outbox.rs", "crates/paraegox-runtime/src/remote_agent_one_echo.rs", "crates/paraegox-runtime/src/managed_agent_runtime.rs", "crates/paraegox-runtime/src/managed_agent_stack_runtime.rs", "crates/paraegox-runtime/src/managed_model_agent_stack_state.rs", "crates/paraegox-runtime/src/managed_model_agent_stack_runtime.rs", "crates/paraegox-runtime/src/managed_model_runtime.rs", "crates/paraegox-runtime/src/runtime_agent_provider.rs", "crates/paraegox-runtime/src/host_watchdog.rs", "crates/paraegox-runtime/src/runtime_host.rs", "crates/paraegox-runtime-host/tests/external_watchdog.rs", "tests/system/test_s7_runtime_install_process.py"]
removal_condition = "Removal requires replacing the executable consumer and every proven canonical-request-to-bounded-Domain ownership path without adding a second desired-state, task, thread, process, recovery, or payload-queue owner."

[[registry.packages]]
path = "crates/paraegox-runtime-host"
cargo_package = "paraegox-runtime-host"
owner = "Final RuntimeHost executable build and external service-manager adapter owner"
responsibility = "Final executable composition root for the single RuntimeHost reactor and the exact release/install/control/offline-migration commands, plus the separate POSIX reference OS service-manager adapter. Its build script is the sole final-artifact build-identity producer: it obtains a nonzero build instance ID from the OS CSPRNG, captures Cargo's canonical target triple, tracks the complete in-workspace final-artifact dependency closure, and emits generated source only into OUT_DIR. Main embeds those facts in the final executable and passes them directly to the Runtime entrypoint; no environment, operator argument, descriptor, or journal may replace them at runtime. No arguments preserves the existing RuntimeHost reactor; exact versioned commands produce the descriptor, verify/install the root-owned executable and singleton manifest into the sequence-one Runtime store, serve the authenticated local bootstrap/query/apply endpoint with fixed-profile restart reassembly before listener publication, preserve the frozen stopped payload-v3-to-v4 migration, or migrate the explicitly lossless stopped payload-v4 subset to payload v5 while retaining migration evidence. The separate service-manager adapter remains the exclusive owner of its exact RuntimeHost child/process group, PXHW observations, bounded restart/backoff ledger, TERM/KILL/reap actions, and quarantine mutation, with its existing same-group-descendant and independently grouped ProcessDomain limitations. This package creates no NodeDaemon, Node facts, Deployment authority, Controller query/reconciliation owner, DeploymentController/deploymentd, Fabric, second restart loop, or general installer framework."
status = "experimental"
public_entrypoints = ["paraegox_runtime_host::service_manager", "paraegox-runtime-host release-descriptor-v1/install-v1/serve-bootstrap-v1/migrate-journal-v3-to-v4-v1/migrate-journal-v4-to-v5-v1 CLI"]
consumers = ["Runtime release pipeline", "system installer/operator"]
first_tests = ["crates/paraegox-runtime-host/build.rs", "crates/paraegox-runtime-host/src/main.rs", "crates/paraegox-runtime/src/runtime_host_entrypoint.rs", "crates/paraegox-runtime/src/runtime_artifact.rs", "crates/paraegox-runtime/src/runtime_build_metadata.rs", "crates/paraegox-runtime/src/runtime_initializer.rs", "crates/paraegox-runtime/src/runtime_install_files.rs", "crates/paraegox-runtime-host/src/service_manager.rs", "crates/paraegox-runtime-host/tests/external_watchdog.rs", "tests/system/test_s7_runtime_install_process.py"]
removal_condition = "Remove only when another admitted final executable owner preserves the CSPRNG build identity, exact release/install artifact chain, one-shot Runtime initialization, and existing OS-managed RuntimeHost lifecycle evidence without introducing a second build, installer, state, or restart authority."

[[registry.packages]]
path = "crates/paraegox-deployment"
cargo_package = "paraegox-deployment"
owner = "Deployment planning, one-shot Controller, and reference tenure-authority process owner"
responsibility = "Current executable projection for the admitted reference, managed Fabric/Model/Agent, and distributed fixed-stack successor requests. The exact `initialize-distributed-agent-stack-v1` and `observe-distributed-agent-stack-nodes-once-v1` commands initialize and observe that bounded coordinator state; the retired unconsumed legacy v1-v4 general draft/projection modules have been removed. The deterministic DeckSpec-to-DeckLock compiler, typed Deck validation, reference-profile Planner, stable allocation delta, and manifest-bound projection; and the POSIX DeploymentTenureAuthority single-writer process. The Authority process validates the exact Controller signature/allowlist and local peer credential, durably commits an Ed25519 WriterTenureProof before returning it, and replays exact operations without resetting state. S7-E has a real one-shot DeploymentController facade and `paraegox-deploymentd` binary; S7-F adds an eighth exact versioned command, `reconcile-reference-once-v1`, on top of the stopped `migrate-controller-journal-v7-to-v8-v1` command and owner-private exact PXQR/PXQS request, response, closure, and recovery mechanisms. `initialize-reference-v1` consumes the installer manifest exactly once and pins it with the Controller request-auth identity. `commit-reference-loop-v1` reopens only that journal-pinned manifest, runs DeckCompiler and Planner, previews both transitions, and durably commits the reference Loop plan. `acquire-tenure-v1` is the real Controller client for the authenticated local Authority IPC; it persists exact Prepared/Uncertain/Committed transactions, pins the Authority domain, replays only the globally latest matching writer proof, and fails closed when another writer fences it. `bootstrap-runtime-v1` authenticates the local Runtime bootstrap exchange and durably pins the exact Runtime store, manifest, channel policy, request-time response signer, clock, and host epoch. `apply-reference-v1` lazily creates and commits the exact signed PXAR before one direct Runtime send, persists only a strictly correlated Runtime-signed PXRT, returns byte-identical terminal replay without resending, and never promotes NotSent or Uncertain transport outcomes to success. `reconcile-reference-once-v1` reads exactly one fresh query id plus nonce from the OS CSPRNG only when a new attempt is required, durably commits canonical PXQR before at most one Runtime query, never sends PXAR, commits an authenticated PXQS before its separately durable typed decision, and recovers request-only, response-only, and terminal-decision states without replaying resident authority. It maps exact serving, operation, desired, live, plan, and direct-receipt evidence to Prepared, Active, Retired, or Uncertain and fails closed on conflicting terminal or plan evidence. `commit-reference-empty-v1` admits only revision-one Loop state with terminal rollout evidence, archives that rollout, tombstones the stable allocation, and commits the revision-two Empty plan for the same apply path. Tenure, terminal apply, terminal reconcile, and committed Empty-plan replays return stable transaction-bound results without a second Authority/Runtime send or Controller-journal mutation. Loop-plan replay is byte-identical only while its receipt-bearing snapshot has not been superseded; a later bootstrap successor changes that physical snapshot, and a bootstrap refresh may legitimately pin a newer Runtime epoch. Changed identities, authority/channel policy, operation inputs, CAS state, superseded tenure, or stale/conflicting reconciliation evidence fail closed. The stopped same-lock v7-to-v8 migration accepts only the explicitly lossless zero-query legacy subset, retains separately durable source evidence plus a canonical receipt, and never opens an old payload through the normal path. Authority and Controller ProductionReference stores accept exact Linux ext4; macOS APFS remains fail closed pending PC1 ACL and crash-durability evidence. The original narrow Loop-to-Empty executable path is committed at 1ed704c and verified by Ubuntu CI run 30748840399. The additive DeveloperLocal A2 facades start a real same-user tenure Authority, derive and validate profile-pinned fixture or provisioned-provider identities, persist the real Controller journal, perform authenticated PXBR then PXFB/PXAR6/PXAR9 exchanges, and retain exact PXMT bytes for Runtime capability issuance. The fixture facade alone creates the deterministic selection; the provider-neutral Provisioned facade admits only an exact provisioned provider reference, configuration digest and adapter binding, chooses no provider, and owns no Secret. Explicit PXAR9 EmptyDeactivate is available for fixture and provisioned A2 profiles, permanent, and separate from normal launcher shutdown; the frozen PXAR7/PXST predecessor facade remains distinct. These public inputs are narrow launcher DTOs, not general DeploymentController, graph, provider, transport, or production-platform APIs. Deploymentd remains one-shot rather than a daemon or continuous reconciler; general workload admission and wider deployment profiles remain absent. Runtime alone owns fixed-profile Loop/Empty and managed Fabric/Model/Agent restart reassembly; Deployment creates no second restart authority, and bounded reconciliation does not constitute general Thread/Process live-state recovery. The current G2/D1 owner-private clients add one Controller-signed PXCC Runtime path and one Controller-signed PXNR Node path. Runtime Describe, Node Describe, Node-issued challenge, challenge-nonce-bound PXQR, PXNO publication, Node Latest and restricted apply remain explicit no-retry steps with durable Controller transitions around their effect boundaries. The public two-phase distributed fixture facade exposes only Runtime observation authorities between prepare and complete, then only ordered PXDS v2 receipts; paraegox-local is its sole executable consumer, and the path is not a public distributed CLI, two-machine proof, remote Agent/TUI flow, reconnect owner or continuous reconciler."
status = "experimental"
public_entrypoints = ["paraegox_deployment::run_tenure_authority_process", "paraegox_deployment::run_deploymentd_process", "paraegox-deploymentd initialize-reference-v1/commit-reference-loop-v1/commit-reference-empty-v1/acquire-tenure-v1/bootstrap-runtime-v1/apply-reference-v1/reconcile-reference-once-v1/migrate-controller-journal-v7-to-v8-v1/initialize-distributed-agent-stack-v1/observe-distributed-agent-stack-nodes-once-v1 CLI", "paraegox_deployment::{DeveloperLocalPeerIdentityV1, DeveloperLocalTenureAuthorityIdentityBytesV1, DeveloperLocalTenureAuthorityConfigV1, DeveloperLocalTenureAuthorityFactsV1, DeveloperLocalTenureAuthorityV1, DeveloperLocalTenureAuthorityError, DeveloperFixtureIdentitySeedV1, DeveloperFixtureDerivedIdentityV1, DeveloperFixturePathsV1, DeveloperFixtureRuntimePinsV1, DeveloperFixtureControllerCredentialsV1, DeveloperFixtureFabricEndpointV1, DeveloperFixtureAgentStackInputV1, DeveloperFixtureAgentStackOutcomeV1, DeveloperProvisionedAgentStackInputV1, DeveloperProvisionedAgentStackOutcomeV1, DeveloperFixtureAgentStackDeactivationOutcomeV1, DeveloperFixtureAgentStackError, run_developer_fixture_agent_stack_v1, run_developer_provisioned_agent_stack_v1, deactivate_developer_fixture_agent_stack_v1, DeveloperFixtureModelAgentStackInputV1, DeveloperFixtureModelAgentStackOutcomeV1, DeveloperProvisionedModelAgentStackInputV1, DeveloperProvisionedModelAgentStackOutcomeV1, DeveloperFixtureModelAgentStackDeactivationOutcomeV1, DeveloperFixtureModelAgentStackError, run_developer_fixture_model_agent_stack_v1, run_developer_provisioned_model_agent_stack_v1, deactivate_developer_fixture_model_agent_stack_v1, deactivate_developer_provisioned_model_agent_stack_v1}", "paraegox_deployment::{DeveloperFixtureDistributedCoordinatorV1, DeveloperFixtureDistributedTransportV1, DeveloperFixtureDistributedTargetV1, DeveloperFixtureDistributedAgentStackInputV1, DeveloperFixtureDistributedNodeV1, PreparedDeveloperFixtureDistributedAgentStackV1, DeveloperFixtureDistributedAgentStackOutcomeV1, DeveloperFixtureDistributedAgentStackError, prepare_developer_fixture_distributed_agent_stack_v1, complete_developer_fixture_distributed_agent_stack_v1}", "paraegox_deployment::{DeveloperDeploymentEnrollmentFactsFieldsV1, DeveloperDeploymentEnrollmentFactsV1, DeveloperDeploymentStartFieldsV1, DeveloperDeploymentStartInputV1, DeveloperDeploymentStartModeV1, DeveloperDeploymentOwnerV1, DeveloperDeploymentReadyV1, DeveloperDeploymentStartOutcomeV1, DeveloperDeploymentErrorV1, start_developer_deployment_v1}", "paraegox_deployment::{DeveloperDeploymentAgentBootstrapStartFieldsV1, DeveloperDeploymentAgentBootstrapStartInputV1, DeveloperDeploymentAgentBootstrapReadyV1, DeveloperDeploymentAgentBootstrapStartOutcomeV1, start_developer_deployment_agent_bootstrap_v1}"]
consumers = ["paraegox-tenure-authority", "paraegox-deploymentd", "paraegox-local"]
first_tests = ["crates/paraegox-deployment/src/deck.rs", "crates/paraegox-deployment/src/planner.rs", "crates/paraegox-deployment/src/manifest_ingress.rs", "crates/paraegox-deployment/src/controller_journal.rs", "crates/paraegox-deployment/src/controller_store.rs", "crates/paraegox-deployment/src/controller_initializer.rs", "crates/paraegox-deployment/src/controller_tenure.rs", "crates/paraegox-deployment/src/controller_query.rs", "crates/paraegox-deployment/src/controller_reconcile.rs", "crates/paraegox-deployment/src/runtime_control_client.rs", "crates/paraegox-deployment/src/managed_serving_client.rs", "crates/paraegox-deployment/src/managed_fabric_apply.rs", "crates/paraegox-deployment/src/managed_agent_stack_apply.rs", "crates/paraegox-deployment/src/managed_fabric_store.rs", "crates/paraegox-deployment/src/distributed_agent_stack_node_reconcile.rs", "crates/paraegox-deployment/src/distributed_agent_stack_producer.rs", "crates/paraegox-deployment/src/distributed_agent_stack_store.rs", "crates/paraegox-deployment/src/deployment_process.rs", "crates/paraegox-deployment/src/developer_local_tenure_authority.rs", "crates/paraegox-deployment/src/developer_fixture_agent_stack.rs", "crates/paraegox-deployment/src/managed_model_agent_stack_producer.rs", "crates/paraegox-deployment/src/managed_model_agent_stack_apply.rs", "crates/paraegox-deployment/src/tenure_protocol.rs", "crates/paraegox-deployment/src/tenure_client.rs", "crates/paraegox-deployment/src/tenure_authority/mod.rs", "crates/paraegox-deployment/src/tenure_authority_process.rs", "tests/governance/test_s7_pure_compile_boundary.py", "tests/governance/test_s7_tenure_authority_boundary.py", "tests/system/test_s7_tenure_authority_process.py", "tests/system/test_s7_deployment_process.py", "crates/paraegox-local/src/composition.rs", "tests/system/test_t1_public_agent_bootstrap_cli.py"]
current_capability_limit = "The public developer-deployment facades consume only typed facts projected by paraegox-local from an independently whole-file-SHA-pinned, canonical, signature-verified and cross-pinned PXEA; Deployment never receives PXEA file bytes or chooses credential paths or providers. Fresh and explicit Resume retain one real Authority, both bounded restricted connectors and whichever one-writer PXJR or PXFS store owns progress. PXJR v9 keeps the checksum-bound PXCR remote connector extension. PXFJ v7 retains the predecessor post-PXFB ManagedReady Describe state and additively owns exactly three Runtime Agent-control slots: Fabric apply, Agent-stack apply and conversation-port descriptor. Legacy v2-v6 reopen only with all three slots Idle. Each Apply prepare commits byte-exact inner PXAR and outer PXAG with one shared operation/request identity but independent outer nonce/signature; the consuming send claim durably enters Uncertain before one transport attempt, and Uncertain provides no replay or action authority. Each terminal commits correlated inner and outer bytes atomically. Terminal Resume revalidates and sends nothing. Agent-bootstrap Ready additionally requires exact PXFT and PXST ActiveReady receipts plus a descriptor PXAH rooted in that PXST and current generations. The descriptor is bootstrap-only evidence and grants no access, authorization, live session, Agent data plane, conversation or Echo capability. Publish ambiguity, uncertain control, or missing terminals yields ReconcileRequired or stable failure and never fabricates success. This remains a bounded single-target developer composition, not T2, two-machine evidence, remote Agent/TUI, reconnect, continuous reconciliation or production support."
removal_condition = "Remove only after an admitted successor preserves the one-writer Authority epoch/proof journal, Controller manifest pin and plan/tenure transactions, exact operation replay, key and service-principal isolation, both strict process facades, both public developer-deployment Ready/Reconcile/owner lifecycles, PXJR-v9/PXFJ-v7 restart truth including the three exact Agent-control slots and no-replay Uncertain boundary, and the existing pure projection/compiler consumers without introducing a second authority, state owner, provider selector or hand-written Runtime Slice path."

[[registry.packages]]
path = "crates/paraegox-agent-contracts"
cargo_package = "paraegox-agent-contracts"
owner = "Agent conversation protocol owner"
responsibility = "DeckRun-bound, language-neutral, bounded PXAC request, terminal, and additive control framing; identity correlation, canonical digests, scoped request-idempotency, explicit open/get/watch/cancel, finite event batches, and request-relative cursor validation. Kinds 1/2 remain the original submit/terminal bytes while kinds 3 through 10 are independently decoded control operations. The protocol owns no transport, Session journal, Runtime lifecycle, model provider, Tool, or Memory semantics."
status = "experimental"
public_entrypoints = ["paraegox_agent_contracts"]
consumers = ["paraegox-agent-service", "paraegox-runtime managed Agent transport", "paraegox-sdk Agent worker and Textual console client"]
first_tests = ["crates/paraegox-agent-contracts/tests/agent_conversation_protocol.rs", "crates/paraegox-agent-contracts/tests/agent_conversation_control.rs", "tests/agent_worker/test_agent_conversation_protocol.py", "tests/agent_worker/test_agent_conversation_control.py", "tests/agent_worker/test_agent_conversation_worker.py", "tests/console/test_console_client.py"]
removal_condition = "Removal requires atomically migrating the independent Rust AgentService and Python worker/console codecs without weakening DeckRun scope, canonical correlation, bounds, or conflict behavior."

[[registry.packages]]
path = "crates/paraegox-agent-service"
cargo_package = "paraegox-agent-service"
owner = "AgentSession and Turn semantic owner"
responsibility = "Bounded DeckRun-scoped Session, Turn, request, terminal, seal, snapshot, event-cursor, and cancellation-intent semantics plus a non-borrowing asynchronous ModelProvider handoff boundary. On POSIX, one owner-private append-only journal durably commits Session open, request acceptance, model handoff, terminal, DeckRun seal, and cancellation intent. AgentService returns one non-Clone invocation token only after the handoff record is durable; consuming that token is the only way to commit a provider outcome. A durable cancel intent signals the in-flight provider cooperatively, while restart never replays the provider: cancel-without-handoff recovers as CancelledBeforeModel and any handed-off request without a terminal recovers as ModelOutcomeUncertain. The additive AgentConversationModelServiceProviderV1 maps one already-admitted Agent request into the provider-neutral ModelService and maps its bounded outcome back; it does not transfer Session journal, cancellation-intent, recovery or terminal authority to Model. Explicit open/get/watch/cancel control handling never creates a missing Session. The deterministic echo provider remains a labelled offline fixture. Runtime/Fabric lifecycle, forced provider preemption, provider-specific ModelBackend I/O and DeveloperLocal composition are owned by their respective packages; AgentService itself makes no credentialed external-service claim."
status = "experimental"
public_entrypoints = ["paraegox_agent_service"]
consumers = ["paraegox-runtime managed Agent transport and assembly", "paraegox-local Model provider composition"]
first_tests = ["crates/paraegox-agent-service/tests/agent_service_semantics.rs", "crates/paraegox-agent-service/tests/agent_service_control.rs", "crates/paraegox-agent-service/tests/agent_service_durable_journal.rs"]
removal_condition = "Removal requires preserving DeckRun/Session/Turn ownership, exact request replay/conflict, unique terminal, sealing, bounded retention, and queryable event semantics in an admitted successor."

[[registry.packages]]
path = "crates/paraegox-model"
cargo_package = "paraegox-model"
owner = "Provider-neutral bounded Model invocation and admission mechanism owner"
responsibility = "Experimental embedded Model CoreService mechanism for provider-neutral bounded text invocation. It owns validated invocation identity/source binding, one configured backend identity, fixed no-queue in-flight admission, cancellation observed before backend handoff, exactly one backend call for each admitted invocation, bounded terminal output, coherent saturating admitted/completed/abandoned accounting, and an exact statically linked Adapter factory registry. Registration binds a complete nonzero adapter descriptor (implementation ID, version and capability ID) to one configured backend identity; resolution rejects duplicate or unknown descriptors, provider/configuration drift, factory refusal and built-backend identity drift without discovery, routing or fallback. AgentService maps its durable Agent request into this mechanism and maps the Model outcome back while retaining exclusive AgentSession/Turn journal, cancellation-intent, recovery and terminal ownership; the consolidated model-adapters package supplies the compiled OpenAI Responses and DeepSeek Chat Completions leaf factories. A2 commits the exact provider selection and adapter descriptor in signed PXTE v8/PXAR v9 desired state, and Runtime rechecks them through the provider-neutral backend resolver before owning ModelService lifecycle and the generation-fenced Model→Agent dependency. This package owns no standalone process, desired-state contract, cross-process contract, dynamic loader, plugin admission, router, retry, fallback, provider HTTP, Secret, Agent semantics, prompt catalog, Tool catalog or billing authority."
status = "experimental"
public_entrypoints = ["paraegox_model"]
consumers = ["paraegox-agent-service Agent-to-Model adapter", "paraegox-model-adapters ModelBackend adapters", "paraegox-runtime managed Model lifecycle", "paraegox-local adapter-registry composition"]
first_tests = ["crates/paraegox-model/src/lib.rs", "crates/paraegox-agent-service/tests/agent_service_semantics.rs", "crates/paraegox-model-adapters/src/tests.rs", "crates/paraegox-runtime-contracts/src/managed_model_agent_stack_plan.rs", "crates/paraegox-runtime/src/managed_model_runtime.rs", "crates/paraegox-runtime/src/managed_model_agent_stack_runtime.rs", "crates/paraegox-local/src/composition.rs"]
admission_authority = "ADR-0009 Agent conversation and client boundary plus the accepted current Model CoreService foundation batch; strategic CoreService admission is allowed before an independent process only while the exact embedded Agent-to-Model integration and semantic evidence remain bounded and reviewable."
integration_checkpoint = "Satisfied for the previously validated A2 fixture and OpenAI composition: each commits the exact provider plus adapter ID/version/capability in PXAR9, resolves the same statically linked Adapter through ModelAdapterRegistryV1, lets Runtime install ModelServiceV1 before AgentService, and releases the TUI capability only after exact PXMT ActiveReady. The additive DeepSeek composition uses the same provider-neutral path but still requires fresh Rust gates and a credentialed external smoke before any external-availability claim."
unmet_checkpoint_action = "If that use path or semantic evidence is absent at batch close, fold the still-needed mechanism into AgentService or remove the crate and its registry/API/dependency rows rather than retaining an orphan CoreService label."
current_capability_limit = "Runtime-managed but embedded in-process statically linked Rust mechanism only; committed A2 desired state authenticates the exact adapter ID/version/capability, while the resolver still maps only explicitly compiled adapters. This is not an independently shared Model process, a cross-process Model contract, dynamic plugin admission/activation, a router, or a fallback/canary service."
removal_condition = "Removal or folding requires migrating the real AgentService/provisioned-Model/DeveloperLocal invocation path and preserving exact Adapter registration/selection, bounded no-queue admission, pre-handoff cancellation, single backend handoff, terminal output validation and abandoned-future accounting."

[[registry.packages]]
path = "crates/paraegox-model-adapters"
cargo_package = "paraegox-model-adapters"
owner = "Provisioned HTTP ModelBackend leaf-adapter owner"
responsibility = "Consolidated provider-specific ModelBackendV1 leaf implementations for OpenAI Responses API v1 and DeepSeek Chat Completions v1. Each adapter owns one digest-bound provider configuration, a fixed compiled adapter descriptor, a fixed official HTTPS endpoint, zeroizing resolved API-key ownership, bounded request/response/output/timeout handling, no redirect/retry/environment fallback, and conservative cancellation/transport uncertainty mapping. The OpenAI adapter sends store=false and traverses typed output_text items. The DeepSeek adapter admits only its exact compiled model set, sends non-streaming chat requests with thinking explicitly disabled, and accepts one bounded terminal text choice. This package does not read environment variables or import Runtime/Deployment provider-selection contracts; paraegox-local validates the exact config-selected profile/provider/model/SecretRef and committed adapter binding, resolves the referenced Secret at the owning boundary, and returns only the registry-built provider-neutral backend to Runtime. Authorization headers are marked sensitive and Secret-bearing values are excluded from Debug/error/configuration surfaces. Consolidating these leaf implementations prevents one crate per provider; it does not create dynamic plugin admission, provider discovery, automatic routing, fallback, ModelService, Agent semantics, Runtime lifecycle or a CoreService process. Loopback HTTP tests are focused adapter evidence only; no credentialed external DeepSeek smoke has yet passed."
status = "experimental"
public_entrypoints = ["paraegox_model_adapters"]
consumers = ["paraegox-local adapter factory and Runtime Model-backend resolver composition"]
first_tests = ["crates/paraegox-model-adapters/src/tests.rs", "crates/paraegox-local/src/composition.rs"]
removal_condition = "Removal requires preserving exact committed provider/config/Secret-reference validation, both bounded no-retry provider protocols, zeroizing resolver-source ownership, and the Runtime recovery rebuild seam without moving provider-specific HTTP or credentials into ModelService, AgentService or Runtime."

[[registry.packages]]
path = "crates/paraegox-fabric"
cargo_package = "paraegox-fabric"
owner = "FabricService Zenoh session and binding owner"
responsibility = "One private Zenoh 1.9 session, explicit TCP peer configuration, typed request/response binding envelopes, BindingEpoch fencing, bounded prevalidation ingress, fail-closed replacement, retirement, worker join, and session shutdown. The additive secured-hybrid profile configures that same sole session with exactly one loopback TCP listener, one non-loopback IPv4 TLS listener, one to eight explicit TLS connectors, mandatory role-specific identity files, one root CA, mTLS, hostname/IP validation and certificate-expiry link closure; discovery remains disabled and protocols are limited to TCP/TLS. Resolved credential values hold only normalized absolute process-local file paths and redact every Debug view; desired opaque refs and certificate bytes remain outside this owner. A successful install alone issues PortBinding lifecycle/CAS tokens; PXBD v1 decodes only to the distinct request-side ClientPortBindingV1, which the type system excludes from replacement, ingress-snapshot, and retirement APIs. Its platform-independent wire-carried ingress bounds cap item capacity at 4096 and byte capacities at the existing envelope limits. Exact golden bytes lock the full PXBD header, ordered fields, route length, reserved bytes, and digest. PXBD is only an enabler for a future authenticated owner-to-owner bootstrap channel: no such channel is implemented here, and its unkeyed digest is corruption detection rather than authentication, authorization, discovery, or lifecycle authority. The additive exact-Zenoh-1.9.0 unstable live-link observation API reads only this FabricService instance's private Session, admits at most the hard bound of 64 links plus one overflow sentinel, accepts only TLS links whose first exposed certificate Common Name exactly completes the configured expected set, rejects missing or unknown CNs and non-bijective CN-to-ZID mappings, and returns configuration-ordered binding digests, ZIDs and locators with the local ZID. Before opening the sole Session, `FabricService::start` requests one 16-byte sample from the OS CSPRNG and fails closed without retry on entropy failure or an all-zero value. The resulting nonzero session epoch remains fixed for that FabricService across Zenoh reconnects; another service start samples again, and no durable store owns it. Each complete snapshot assigns one Fabric-owner-issued, unique nonzero sequence to every configured peer in configuration order and exposes the final peer sequence as the service-local high-water; only a successful complete classification advances that high-water, while failure or exhaustion leaves it unchanged. Repeated links for the same CN and ZID select the lexicographically smallest source/destination locator pair; TCP-only links are ignored. This is experimental in-memory observation, not production certificate identity or authorization, a durable session epoch, PXTP, Evidence append/readback, ActiveReady, Agent start/bindings/handle, continuous health, certificate rotation, SAN/fingerprint proof, or two-machine proof. The owner-private v1 envelopes remain experimental and are not a stable polyglot ABI. Additive restricted Runtime-control and Unix Node-control clients/endpoints deliberately reuse one private role-scoped raw-query implementation while keeping PXCC/PXAG versus PXNR request bounds and PXDR/PXFR/PXQS/PXAH versus PXNE/PXNS/PXNA response bounds type-separated. Deployment's T1 path retains the exact preflight proof until its durable move-only claim authorizes one consuming send. Every inbound and reply remains unauthenticated raw bytes until the Runtime or Deployment owner performs strict decode, signature verification and correlation; Fabric never signs, decodes, admits, persists or retries PXAG/PXAH and owns no Agent-control state or recovery decision. These restricted lifecycles own only their connector- or listener-scoped query sessions and raw-byte handoff; they do not replace the sole FabricService session or create a general Bus, discovery owner or reconnect policy. T2-B additively gives the sole-session Fabric owner an asymmetric remote-Agent configuration seam. The listener role retains one exact loopback TCP listener and adds one non-loopback IPv4 TLS listener on the same sole FabricService Session with zero connectors and a listener-only nominal credential value; the connector role opens one TLS-only sole FabricService Session with zero listeners, exactly one TLS connector and a connector-only nominal credential value. Each role admits exactly two distinct bounded canonical ASCII non-wild routes, uses default-deny Common-Name-plus-TLS application ACLs with inverse query versus reply-and-declare-queryable flows, disables scouting, admin space and plugin loading, bounds pending sessions, sessions and links to one, fixes the receive-message ceiling, and retains certificate-name and expiry verification. The pinned Zenoh 1.9.0 same-session local-dispatch path preserves local loopback self-query, but that fact is version-specific and grants no access to an independent plaintext session. This configuration-and-transport seam neither consumes nor reinterprets PXCB or PXAR v8 and provides no CLI, PXRA/PXRR activation, Agent conversation or Echo, reconnect owner, two-host proof or wider route authority."
status = "experimental"
public_entrypoints = ["paraegox_fabric", "paraegox_fabric::ResolvedRemoteMtlsListenerCredentialFilesV1", "paraegox_fabric::ResolvedRemoteMtlsConnectorCredentialFilesV1", "paraegox_fabric::FabricServiceConfig::try_remote_agent_listener_v1", "paraegox_fabric::FabricServiceConfig::try_remote_agent_connector_v1"]
consumers = ["paraegox-fabric host-local TCP integration harness", "paraegox-fabric remote-Agent real-mTLS integration harness", "paraegox-runtime managed Agent and restricted Runtime-control endpoints", "paraegox-deployment restricted Runtime-apply and Runtime Agent-control clients", "paraegox-local TLS credential validation and Node-control composition"]
first_tests = ["crates/paraegox-fabric/src/contract.rs", "crates/paraegox-fabric/src/ingress.rs", "crates/paraegox-fabric/src/port_descriptor.rs", "crates/paraegox-fabric/src/runtime_apply.rs", "crates/paraegox-fabric/src/service.rs", "crates/paraegox-fabric/tests/fixtures/port_binding_descriptor_v1.hex", "crates/paraegox-fabric/tests/host_local.rs", "crates/paraegox-fabric/tests/remote_agent_mtls.rs", "crates/paraegox-deployment/src/distributed_agent_stack_node_reconcile.rs", "crates/paraegox-local/src/config.rs", "crates/paraegox-local/src/composition.rs", "crates/paraegox-local/src/identity.rs"]
removal_condition = "Removal requires an admitted Fabric owner that preserves the single production Zenoh session, explicit route configuration, bounded ingress, epoch fencing, typed request/reply, and exact cleanup evidence without introducing a second Bus or Backend."

[[registry.packages]]
path = "crates/paraegox-inspection"
cargo_package = "paraegox-inspection"
owner = "Node-local Inspection projection owner"
responsibility = "Experimental read-only PXIS-v1 projection for exactly the currently admitted Authority, DeploymentController, RuntimeHost, FabricService, and AgentService owner facts. It keeps owner-specific coordinates distinct, compares freshness only in one caller-supplied Inspection observation clock, masks stale/partitioned values to Unknown, derives one conservative aggregate, and owns only a monotonic in-memory projection revision plus last immutable cache. The explicit PXIS-v2 successor embeds that complete canonical PXIS-v1 snapshot byte-for-byte and appends one bounded public-safe NodeDaemon record carrying only Node/incarnation references, registration epoch, status sequence, local observation/validity, conservative state, feature support, reason and the authenticated NodeStatus digest; it does not widen or reinterpret the frozen v1 owner set. Separate PXIQ/PXIP v2 and PXIB v2 surfaces carry the composite snapshot without version inference, while every v1 byte and API remains unchanged. The owner-neutral v1 adapter still builds exactly five canonical owner slots, and the separate Node v2 adapter accepts only an already-authenticated and fenced fact with exact Node/incarnation correlation. Both protocol generations remain bounded one-shot Latest/Watch reads of their own immutable cache; both DeveloperLocal bootstraps zeroize private token/seed bytes and own no socket or source lifecycle. The projection performs no discovery, polling, persistence, retry, stream, heartbeat, history, operational action, desired-state mutation, source-fact synthesis or mutation, health authority, registry, Evidence storage, federation, or cross-Node clock comparison."
status = "experimental"
public_entrypoints = ["paraegox_inspection", "paraegox_inspection::protocol", "paraegox_inspection::adapter", "paraegox_inspection::developer_local"]
consumers = ["paraegox-inspection contract integration tests", "paraegox-local", "paraegox-sdk Textual one-shot startup client"]
first_tests = ["crates/paraegox-inspection/src/developer_local.rs", "crates/paraegox-inspection/tests/local_inspection_contract.rs", "crates/paraegox-inspection/tests/inspection_protocol.rs", "crates/paraegox-inspection/tests/inspection_adapter.rs", "crates/paraegox-inspection/tests/fixtures/local_inspection_snapshot_v1.hex", "crates/paraegox-inspection/tests/fixtures/inspection_latest_request_v1.hex", "crates/paraegox-inspection/tests/fixtures/inspection_watch_request_v1.hex", "crates/paraegox-inspection/tests/fixtures/inspection_snapshot_response_v1.hex", "crates/paraegox-inspection/tests/fixtures/inspection_not_modified_response_v1.hex", "crates/paraegox-inspection/tests/fixtures/inspection_not_found_response_v1.hex", "crates/paraegox-local/src/inspection.rs", "tests/console/test_console_client.py", "tests/console/test_console_tui.py"]
removal_condition = "Removal requires another read-only projection owner to preserve the byte-exact five-owner PXIS-v1 surface, the explicit v2 composite Node successor, conservative freshness masking, strict versioned PXIS/PXIQ/PXIP/PXIB bytes, exact one-shot correlation, error-preserving source adapters, and failure-atomic revision/cache behavior without acquiring source or operational authority."

[[registry.packages]]
path = "crates/paraegox-node"
cargo_package = "paraegox-node"
owner = "NodeDaemon observed-fact and endpoint-discovery owner"
responsibility = "Experimental P5 Node identity/spec/status boundary, bounded NodeDaemon publication reducer, strict read-only PXNQ/PXNS v1 management protocol, Unix single-writer same-tenure durability, and a Unix-only DeveloperLocal/reference paraegox-noded process substrate. Stable enrollment identity, Deployment-owned desired NodeSpec, NodeDaemon-owned NodeIncarnation/registration epoch/status sequence/feature report, and RuntimeHost-owned endpoint descriptors remain distinct. One tenure fences at most eight RuntimeHost identities; forgotten observations remain durably fenced, Runtime epoch/sequence and endpoint generation cannot roll back, and a management endpoint cannot drift within one tenure or survive a Node incarnation change. PXNQ/PXNS provides Latest and one-shot Watch with complete correlation and replay fencing. PXND v2 commits the complete private reducer plus the last accepted PXNO digest and authenticated absolute expiry needed for exact replay and stale-response suppression before returning, while strictly reopening legacy v1 and advancing it only on the next mutation. When an observation-backed aggregate PXNS has reached that absolute deadline, the sole store owner atomically publishes the next node-only PXNS under its writer lock, hides the complete visible Runtime set, clears PXNO/PXNA replay and per-Runtime validity metadata, and preserves every RuntimeHost monotonic fence. The previously committed exact PXNA remains recoverable only while no successor publication has superseded it. PXNB preserves the read-only same-user endpoint; its strict owner-private loader and typed DeveloperLocal endpoint pin the canonical bootstrap, socket inode and peer uid/gid, perform one bounded token-authenticated exchange with no retry, and are now consumed by paraegox-local to verify the real reference child status before projection. The additive PXOB/PXNO mode pins at most eight exact Runtime identities, Ed25519 response keys, channels, authenticated serving baselines and complete Runtime apply endpoint descriptors; a separate same-user capability socket accepts only a strict short-lived PXQR/PXQS pair whose nonce is bound to the exact Node tenure, observation endpoint, authority, endpoint descriptor, proposed next PXNS sequence and bounded issuance/expiry window. Runtime signature, current time window and correlation verification precede one atomic observe-plus-publish PXND transaction and PXNA acknowledgement; relative PXNS freshness is calculated under the durable owner lock, the management endpoint refuses the immutable observation-backed PXNS after its persisted absolute deadline, and an exact committed PXNO can recover a lost PXNA after expiry without republishing or renewing freshness only until the next publication. PXNO cannot rotate the unsigned endpoint generation, carry PXAR/desired state, infer readiness from a connection, proxy Runtime apply, or create a second Runtime truth. This package still has no registration authority/acquisition, production Zenoh observation adapter, double-host execution, continuous reconciliation, partition inference, restart action, or Deployment mutation/authority. Additive PXNR/PXNE supplies a Controller-signed, kind-allowlisted remote carrier for target discovery, frozen Latest/Watch, Node-issued observation challenge and frozen PXNO publication; unsigned PXNE is usable only with same-exchange mTLS pins. The DeveloperLocal bridge consumes only an authenticated marker and delegates to the existing PXNL/PXOL capabilities, so the child remains the unique PXND writer and no second store, retry or cache is introduced."
status = "experimental"
public_entrypoints = ["paraegox_node", "paraegox_node::protocol", "paraegox_node::store", "paraegox_node::process", "paraegox_node::observation", "paraegox-noded developer-local-reference-v1 CLI", "paraegox-noded developer-local-runtime-observation-v1 CLI"]
consumers = ["paraegox-node contract and external-process tests", "paraegox-deployment one-shot remote Node adapter", "paraegox-fabric restricted Node-control transport bounds", "paraegox-local NodeDaemon and authenticated Node-control composition"]
first_tests = ["crates/paraegox-node/src/lib.rs", "crates/paraegox-node/src/store.rs", "crates/paraegox-node/src/process.rs", "crates/paraegox-node/src/observation.rs", "crates/paraegox-node/src/protocol.rs", "crates/paraegox-node/tests/node_daemon_process.rs", "crates/paraegox-node/tests/node_management_protocol.rs", "crates/paraegox-node/tests/fixtures/node_management_latest_request_v1.hex", "crates/paraegox-node/tests/fixtures/node_management_watch_request_v1.hex", "crates/paraegox-node/tests/fixtures/node_management_status_response_v1.hex", "crates/paraegox-node/tests/fixtures/node_management_not_modified_response_v1.hex", "crates/paraegox-node/tests/fixtures/node_management_not_found_response_v1.hex", "crates/paraegox-node/tests/fixtures/node_management_fenced_response_v1.hex", "crates/paraegox-node/tests/fixtures/node_management_cursor_conflict_response_v1.hex", "crates/paraegox-deployment/src/distributed_agent_stack_node_reconcile.rs", "crates/paraegox-fabric/src/runtime_apply.rs", "crates/paraegox-local/src/composition.rs", "crates/paraegox-local/src/main.rs", "tests/system/test_g2_public_deployment_cli.py"]
removal_condition = "Removal requires another NodeDaemon owner to preserve stable Node identity, registration/incarnation/sequence fencing, bounded canonical Runtime discovery, and the strict separation between NodeManagementEndpoint discovery and RuntimeHost-owned apply admission."

[[registry.packages]]
path = "crates/paraegox-evidence"
cargo_package = "paraegox-evidence"
owner = "Local Evidence durable-handoff owner"
responsibility = "Experimental P6a local durable Evidence contract and Unix single-writer store. PXEV v1 separates bounded caller-classified public-safe inline bytes from digest-only redaction, binds owner id/sequence, optional causal correlation and exact previous-owner Evidence id into a canonical record digest, and never upgrades a domain outcome. PXES/PXEF v1 pins one nonzero store epoch and immutable hard retention bounds, appends canonical records idempotently, synchronizes the append before acknowledgement, detects same-id/different-digest conflicts, enforces each owner's sequence/previous-record chain, serves bounded local read/list pages, recovers only an incomplete final append, and fails closed on complete corruption. A write or sync failure after append begins poisons the handle with an uncertain commit until strict reopen resolves it. Capacity exhaustion never evicts authoritative records. This crate has no service process, owner adapters, network protocol, replication, federation, Ops journal, global ordering, exporter, generic object resolver, or ProductionReference filesystem support claim."
status = "experimental"
public_entrypoints = ["paraegox_evidence"]
consumers = ["paraegox-runtime PXAR8 transport-fact handoff", "future local Inspection/Evidence integration"]
first_tests = ["crates/paraegox-evidence/src/lib.rs", "crates/paraegox-evidence/src/store.rs", "crates/paraegox-runtime/src/distributed_agent_stack_state.rs", "crates/paraegox-runtime/src/distributed_agent_stack_runtime.rs"]
admission_authority = "The accepted P6a Evidence roadmap tranche and strategic CoreService-enabler admission rule permit this owner boundary before a running adapter only while its durable semantics, bounded integration batch and review action remain explicit."
integration_checkpoint = "At P6a batch close, at least one real owner-issued terminal Receipt producer and the local Inspection/Evidence read path must consume this durable handoff with focused cross-owner semantic evidence."
unmet_checkpoint_action = "If P6a closes without that producer-to-store-to-reader path, remove the crate and its registry/dependency rows or fold the still-needed private mechanism into the sole real owner instead of retaining an orphan Evidence CoreService label."
current_capability_limit = "The experimental PXAR8 Runtime owner is the first real producer/consumer: it durably records one bounded PXTP RuntimeFact batch intent, appends and synchronously reads back each exact PXEV record, and requires a complete verified batch before its separate lifecycle owner may start the existing Agent assembly. A zero/prefix/full or already-Committed batch is resolved exactly after restart but never reused as authority for Agent effects; physical recovery requires a fresh generation/session/PXTP/PXEV lineage. This remains an in-process local store consumer, not a running Evidence process, local commit IPC, Inspection read path, network protocol, replication, federation, exporter, global ordering or ProductionReference filesystem support claim."
removal_condition = "Replacement must preserve PXEV/PXES/PXEF bytes, the PXAR8 producer-to-store-to-terminal handoff, local durable commit-before-ack, idempotency/conflict, owner causality, bounded retention, query, recovery, and uncertainty semantics without becoming a second owner or generic store."

[[registry.packages]]
path = "src/paraegox_sdk"
owner = "Python managed-workload SDK and DeveloperLocal Textual presentation owner"
responsibility = "The existing Python package keeps the independent ProcessDomain and Agent conversation reference codecs/workers together with the DeveloperLocal Textual presentation and its strict typed Runtime Agent and Inspection IPC clients. The internal `paraegox-console` child owns bounded UTF-8 input, transcript rendering, one-pending-request state, explicit cancel, local slash commands, and three read-only Inspection startup-status lines. The Agent client strictly loads one owner-private PXAB bootstrap, authenticates the same-user PXAI/PXAO Unix exchange, creates fresh Turn/Request identities, performs no retry, and reuses the admitted PXAC codecs. Before constructing the Textual App, the separate Inspection client strictly loads one owner-private PXIB v2 bootstrap, performs exactly one no-retry PXIQ Latest exchange, correlates the complete PXIP response, and strictly decodes the PXIS v2 snapshot; any bootstrap, transport, correlation, or snapshot failure prevents UI startup. The Inspection path has no Watch, retry, cache, background refresh, operational action, continuous-monitoring, Ops, or federation authority. The Python package opens no Zenoh Session, selects no provider/model, reads no API key, owns no Agent or Inspection journal or retry/reconcile policy, and exposes `paraegox-console` only as an internal child rather than a second public chat command."
status = "experimental"
public_entrypoints = ["paraegox-worker reference worker CLI", "paraegox_sdk.agent_worker", "paraegox-console internal DeveloperLocal child (not a public CLI)"]
consumers = ["paraegox-runtime ProcessDomain reference evidence", "paraegox-local DeveloperLocal composition"]
first_tests = ["tests/unit/test_s6_python_worker_protocol.py", "tests/agent_worker/test_agent_conversation_protocol.py", "tests/agent_worker/test_agent_conversation_control.py", "tests/agent_worker/test_agent_conversation_worker.py", "tests/console/test_console_client.py", "tests/console/test_console_tui.py"]
removal_condition = "Removal requires preserving the independent worker protocol evidence and replacing the internal Textual child with another typed-client frontend that retains input bounds, exact terminal correlation, explicit cancel, no-retry transport behavior and joined terminal restoration without taking Fabric, Agent or Model ownership."

[[registry.packages]]
path = "crates/paraegox-local"
cargo_package = "paraegox-local"
owner = "DeveloperLocal composition root"
responsibility = "Three strict Unix public commands share this executable composition owner but start different bounded slices. `paraegox chat --config <absolute TOML>` keeps the provider-profile-pinned owner-private identity, state/socket layout and real DeveloperLocal Authority, DeploymentController, Runtime-owned Zenoh Fabric/Model/Agent stack, separate NodeDaemon reference child, local read-only Inspection owner and internal Python Textual terminal child. Chat config schema v1 is the sole public authority for state root, loopback Fabric listener, provider and model: unknown fields/schema/provider fail closed, deterministic Echo prohibits model and SecretRef, and provisioned profiles require an exact provider-specific model plus the exact environment SecretRef. The chat CLI has no provider/model subcommands or override flags, and config stores no Secret value. Before creating chat state or owners, Local resolves only the selected exact environment reference into zeroizing memory and commits the exact Provisioned provider/config/Secret selection. All chat profiles inject the same repeatable backend resolver into Runtime: committed A2 desired state carries the complete provider selection and adapter descriptor; the resolver rechecks both, maps the explicit profile to one compiled-in adapter, and builds through ModelAdapterRegistryV1. Runtime owns the capacity-one ModelService, starts it before AgentService, and supplies a generation-fenced Model dependency. There is no fixture bypass, default provider, discovery or fallback, and the static mapping is not dynamic plugin admission. Same-root profile/model/config/adapter drift fails closed. The chat composition creates or strictly reopens its owner-private PXNB/PXND layout, publishes one feature-only NodeStatus with zero RuntimeHost observations, starts the NodeDaemon child through a hidden bootstrap-path-only mode, accepts its startup status only after one bounded authenticated PXNQ/PXNS exchange, starts owner-private Runtime Agent and Inspection IPC, then launches `paraegox-console` with the absolute Runtime and Inspection bootstrap paths while removing both admitted API-key variables. Before the App starts, the Textual child uses a separate strict typed client for exactly one no-retry PXIB/PXIQ/PXIP/PXIS v2 Latest read; failure prevents UI startup, while success renders only three read-only startup-status lines with no Watch, cache, background refresh, action, continuous monitoring, Ops, or federation. Chat shutdown joins Textual/Agent IPC, Runtime, NodeDaemon and Authority in owner order while preserving durable Active desired state for same-root recovery. The additive G1 command, `paraegox node --config <absolute TOML>`, consumes a distinct strict non-secret schema v1 containing exact control references, two external-role Ed25519 verification keys, one restricted Runtime-apply transport profile and absolute TLS credential paths; raw Controller/Authority signing seeds, PXNB tokens and certificate bytes cannot be represented. Before durable state, the node composition rejects root execution and gates one canonical owner-private state root plus its exact mode-0700 `credentials` child and no-follow TLS files. Its isolated PXNI stores only the Runtime response seed, PXNB token, local Node identities and complete semantic config commitment. It prepares only Runtime and NodeDaemon state/socket owners, starts the split-trust Runtime with a bound restricted mTLS listener in legacy generic-rejection mode, publishes and reopens a feature-only PXNB/PXND with zero RuntimeHost observations, starts exactly one NodeDaemon child, and prints `paraegox: node ready` only after one authenticated typed Latest equality check. SIGINT, SIGTERM and child death converge through bounded joined Node-then-Runtime cleanup. This G1 node command deliberately does not start Authority, DeploymentController, managed Fabric, Model, Agent, Inspection or Textual, does not perform Controller apply or remote bootstrap, and does not claim G2, Agent chat, registration acquisition, Runtime observation, distributed readiness, production platform support or credentialed external-service availability. The internal two-target scaffolding remains non-public and does not start a distributed owner chain. Node config schema v2 is an explicit successor for the same public `paraegox node --config` command: it retains every v1 field and requires one strict `[node_control]` section containing a nonzero endpoint generation, canonical non-loopback TLS/IPv4 listener, bounded non-wild route, trust/credential/profile refs, configured Node certificate principal and three normalized absolute role-specific TLS paths. Unknown fields, caller-supplied route/config digest, inline token/seed/private-key bytes, schema/section mismatch and endpoint/route/ref/credential/path aliasing fail closed. Local derives the route/config carrier digest only from cross-host semantics, the existing Controller principal and fixed timeout, excluding state root and local paths; the complete config commitment separately binds all v1 fields, that digest and all six TLS paths. Separate PXNI v2 stores only the Runtime response seed, PXNB/PXOB tokens, six stable local identities including the config-pinned Node principal, and the config commitment; it contains no Controller/Authority seed or TLS private-key bytes. Its layout adds only the observation socket and PXOB file to the unchanged G1 owner roots. The v2 lifecycle starts the explicit Runtime G2 listener, reconstructs and rechecks its Runtime-local Ready channel/store/manifest/build facts, reopens the sole PXND owner and converges any expired observation-backed aggregate to one durable next node-only status before writing PXOB, starting the sole Node child, constructing the bridge and opening the Node-control listener. That convergence hides all visible Runtime entries, clears replay and validity metadata, and retains monotonic fences under the same owner lock. Raw PXNR is strictly decoded and Controller-verified before one bridge dispatch; there is no retry. Ready prints only after all owners start; child or worker exit fails closed, and shutdown joins Node-control endpoint/worker, then Node, then Runtime. The Linux system harness requires an exact prebuilt binary, uses root only to prepare owner/mode-isolated fixtures, drops every ParaEGOX process to one non-root uid/gid, and covers fresh readiness, same-state Node stop/restart, Deployment Resume readiness and clean SIGTERM shutdown. It does not explicitly wait the full 60-second maximum; injected-clock store tests cover that boundary and a preserved genuinely expired state supplies separate manual process evidence. This v2 node slice still starts no Authority, Deployment, managed Fabric, Model, Agent, Inspection or Textual owner, and neither the harness nor this registration claims Authority-failure handling, distinct service-account or two-host execution, remote Agent conversation/TUI, reconnect policy or distributed readiness."
status = "experimental"
public_entrypoints = ["paraegox chat --config <absolute TOML> CLI", "ParaEGOX DeveloperLocal chat config schema v1", "paraegox node --config <absolute TOML> CLI", "ParaEGOX DeveloperLocal node config schema v1", "ParaEGOX DeveloperLocal node config schema v2", "ParaEGOX DeveloperLocal node config schema v3", "paraegox deployment --config <absolute TOML> CLI", "ParaEGOX DeveloperLocal deployment config schema v1", "ParaEGOX DeveloperLocal deployment config schema v2"]
consumers = ["ParaEGOX developer-local operator"]
first_tests = ["configs/paraegox-node.example.toml", "configs/paraegox-node.agent-bootstrap.example.toml", "configs/paraegox-deployment.example.toml", "configs/paraegox-deployment.agent-bootstrap.example.toml", "crates/paraegox-local/src/config.rs", "crates/paraegox-local/src/identity.rs", "crates/paraegox-local/src/layout.rs", "crates/paraegox-local/src/inspection.rs", "crates/paraegox-local/src/composition.rs", "crates/paraegox-local/src/error.rs", "crates/paraegox-local/src/main.rs", "tests/fixtures/wire/t1_node_enrollment_artifact_v2.json", ".github/workflows/macos-cli-artifact.yml", "tests/system/test_g2_public_deployment_cli.py", "tests/system/test_t1_public_agent_bootstrap_cli.py"]
current_capability_limit = "The exact `paraegox node --config <absolute TOML>` and `paraegox deployment --config <absolute TOML>` commands now have additive Agent-bootstrap schemas. Node v3 retains all v2 remote-control facts, adds only the exact deterministic provider selection, persists the separate PXNI v3 private identity with no desired service/listener/limit authority, installs the resolver without activating Agent and publishes signed/cross-pinned PXEA v2. Deployment v2 retains every v1 path/key/connector field, verifies the independent whole-file PXEA pin before decoding its provider fact, and alone supplies distinct Fabric/Agent service identities, loopback listener and fixed limits profile to the registered Deployment facade. Existing Controller/PXFS state selects explicit Resume; no store is deleted or silently reinitialized. The exact flushed `paraegox: deployment agent bootstrap ready` marker requires only the typed Agent-bootstrap Ready variant plus a live Authority and is distinct from the unchanged v1 marker. ReconcileRequired, Node unavailability, Authority death, output failure and cleanup failure remain non-success with joined shutdown. Immutable r130 Linux process evidence covers Node-v3/PXEA-v2 fresh readiness, wrong whole-PXEA SHA with no fresh mutation, Deployment-v2 fresh marker and joined SIGTERM, same-state Node and terminal Deployment resume, Node-down failure with no marker and Authority-socket cleanup, and seed/private-key absence from captured logs. That evidence does not independently cover provider unavailable/mismatch or prove descriptor access, authorization, session, conversation, Echo, remote Agent data plane, T2, reconnect, TUI, two-host operation or distributed readiness; macOS remains grammar/static evidence only."
removal_condition = "Removal or replacement requires an explicit CLI/config successor that preserves the affected command's strict versioned config authority, CSPRNG-backed stable identities, owner-private durable storage, split-trust key boundary, TLS path gate, readiness proof and joined shutdown. Replacing chat must additionally preserve pre-state SecretRef resolution, exact provider/config pins, Runtime/Deployment authority boundaries and recovery-time provider rebuilding without introducing a second lifecycle or Fabric owner. Replacing Node v3 must preserve PXNI-v3 secret boundaries, deterministic provider derivation, PXEA-v2 whole-file/signature/cross-pin semantics and the separation from Deployment-owned desired services. Replacing deployment v1/v2 must preserve whole-file-pinned PXEA ingress, zeroizing seed resolution, disjoint Controller/Authority/PXFS ownership, explicit Fresh/Resume selection, facade-only version-distinct Ready markers, ReconcileRequired as non-success, Authority supervision and joined connector/store/Authority shutdown."

[[registry.public_apis]]
module = "paraegox_deployment"
symbols = ["run_tenure_authority_process", "TenureAuthorityProcessError", "run_deploymentd_process", "DeploymentdProcessError"]
owner = "Deployment process composition roots"
consumers = ["paraegox-tenure-authority", "paraegox-deploymentd"]
compatibility = "The public Rust surface is limited to starting either exact process facade and receiving an opaque fail-closed error. The tenure-authority process and deploymentd acquire/reconcile clients communicate over real strict versioned wires, while their Rust DTOs remain owner-private. The one-shot deploymentd accepts only the exact positional grammars `initialize-reference-v1`, `commit-reference-loop-v1`, `commit-reference-empty-v1`, `acquire-tenure-v1`, `bootstrap-runtime-v1`, `apply-reference-v1`, `reconcile-reference-once-v1`, `migrate-controller-journal-v7-to-v8-v1`, `initialize-distributed-agent-stack-v1`, and `observe-distributed-agent-stack-nodes-once-v1`; none accepts an implicit fallback, environment override, retry loop, or caller-supplied request entropy. The two distributed commands consume one canonical owner-private capability file: initialization reopens and verifies both committed managed Agent-stack predecessors, produces PXDJ, and atomically installs PXDJ with initial PXDN in a distinct coordinator Controller store; observation synchronously pins trusted-local socket metadata, then performs one no-retry PXNL exchange whose connect/read/write phases share one deadline per fixed target, atomically advances only PXDN while preserving exact PXDJ bytes, reopens the full pair, and emits only `ready` or `blocked`. This is local trusted endpoint discovery only: it sends no PXAR v8, adds no continuous loop, and does not claim a dual-host or Zenoh carrier rollout. Initialization pins the installer manifest, Loop and Empty commands commit only their admitted plan transitions, tenure and bootstrap pin their authenticated authority domains, and apply exposes only a commit-before-send PXAR followed by an exact validated PXRT or a fail-closed error. Reconcile prepares at most one fresh CSPRNG-backed PXQR, sends no PXAR, and commits an exact validated PXQS before a separate Prepared/Active/Retired/Uncertain decision. A current terminal decision or response-only recovery uses no network or fresh entropy; request-only restart is durably closed and returns Uncertain with no network or fresh entropy, while only a later invocation may create and send one fresh attempt. The stopped migration command preserves only the explicitly lossless v7 zero-query subset as v8, commits separate read-only source evidence and a canonical receipt, and is not a normal reopen fallback. These facades do not expose journal, Planner, tenure, request, wire, or store DTOs and provide no daemon or continuous reconcile loop, general workload admission, wider deployment profile, or second restart authority. Fixed-profile Loop/Empty restart reassembly remains exclusively Runtime-owned and does not imply general Thread/Process recovery. Incompatible command, receipt, migration, or ownership semantics require a new explicit version."
tests = ["crates/paraegox-deployment/src/tenure_authority_process.rs", "crates/paraegox-deployment/src/deployment_process.rs", "tests/governance/test_s7_tenure_authority_boundary.py", "tests/system/test_s7_tenure_authority_process.py", "tests/system/test_s7_deployment_process.py"]

[[registry.public_apis]]
module = "paraegox_deployment"
symbols = ["DeveloperLocalPeerIdentityV1", "DeveloperLocalTenureAuthorityIdentityBytesV1", "DeveloperLocalTenureAuthorityConfigV1", "DeveloperLocalTenureAuthorityFactsV1", "DeveloperLocalTenureAuthorityV1", "DeveloperLocalTenureAuthorityError", "DeveloperFixtureIdentitySeedV1", "DeveloperFixtureDerivedIdentityV1", "DeveloperFixturePathsV1", "DeveloperFixtureRuntimePinsV1", "DeveloperFixtureControllerCredentialsV1", "DeveloperFixtureFabricEndpointV1", "DeveloperFixtureAgentStackInputV1", "DeveloperFixtureAgentStackOutcomeV1", "DeveloperProvisionedAgentStackInputV1", "DeveloperProvisionedAgentStackOutcomeV1", "DeveloperFixtureAgentStackDeactivationOutcomeV1", "DeveloperFixtureAgentStackError", "run_developer_fixture_agent_stack_v1", "run_developer_provisioned_agent_stack_v1", "deactivate_developer_fixture_agent_stack_v1", "DeveloperFixtureModelAgentStackInputV1", "DeveloperFixtureModelAgentStackOutcomeV1", "DeveloperProvisionedModelAgentStackInputV1", "DeveloperProvisionedModelAgentStackOutcomeV1", "DeveloperFixtureModelAgentStackDeactivationOutcomeV1", "DeveloperFixtureModelAgentStackError", "run_developer_fixture_model_agent_stack_v1", "run_developer_provisioned_model_agent_stack_v1", "deactivate_developer_fixture_model_agent_stack_v1", "deactivate_developer_provisioned_model_agent_stack_v1", "DeveloperFixtureDistributedCoordinatorV1", "DeveloperFixtureDistributedTransportV1", "DeveloperFixtureDistributedTargetV1", "DeveloperFixtureDistributedAgentStackInputV1", "DeveloperFixtureDistributedNodeV1", "PreparedDeveloperFixtureDistributedAgentStackV1", "DeveloperFixtureDistributedAgentStackOutcomeV1", "DeveloperFixtureDistributedAgentStackError", "prepare_developer_fixture_distributed_agent_stack_v1", "complete_developer_fixture_distributed_agent_stack_v1"]
owner = "DeveloperLocal Authority and fixed DeploymentController composition facades"
consumers = ["paraegox-local"]
compatibility = "This experimental Unix-only surface exists solely to compose fixed DeveloperLocal profiles through the real durable Controller, authenticated tenure Authority and Runtime endpoints. Existing fixture/provisioned facades retain their exact PXBR/PXFB/PXAR6/PXAR7/PXAR9, PXFT/PXST/PXMT, provider-selection, adapter-binding, replay and explicit-deactivation semantics. The additive distributed fixture facade is a move-only two-phase owner path: prepare first advances and verifies exactly two RuntimeHost-sorted single-target predecessors and exposes only their public RuntimeObservationAuthority values; after Local starts the two Node children, complete consumes exact Node/PXOB capabilities, performs the bounded D1 initialize/observe/restricted-apply path and returns only the two durable PXDS v2 receipts in target order. Deployment's internal remote Runtime path sends Controller-signed PXCC Describe and ReferenceQuery through one no-retry transport, while its remote Node adapter performs Controller-signed Describe, ObservationChallenge, Latest/Watch and PublishRuntimeObservation with exact mTLS route/principal pins. A query request is constructed solely from the Node challenge and its authentication nonce must equal the challenge query nonce byte-for-byte. These facades choose no provider or credential, expose no Controller journal/store DTO, and do not constitute a public distributed command, Mac-side Controller composition, real two-machine proof, remote Agent/TUI path, continuous reconciliation, reconnect loop or production platform claim. Incompatible identity, lifecycle, challenge binding, transport, provider, adapter or terminal semantics require a successor facade."
tests = ["crates/paraegox-deployment/src/developer_local_tenure_authority.rs", "crates/paraegox-deployment/src/developer_fixture_agent_stack.rs", "crates/paraegox-deployment/src/distributed_agent_stack_node_reconcile.rs", "crates/paraegox-deployment/src/managed_model_agent_stack_producer.rs", "crates/paraegox-deployment/src/managed_model_agent_stack_apply.rs", "crates/paraegox-deployment/src/managed_serving_client.rs", "crates/paraegox-deployment/src/runtime_control_client.rs", "crates/paraegox-local/src/composition.rs"]

[[registry.public_apis]]
module = "paraegox_deployment"
symbols = ["DeveloperDeploymentEnrollmentFactsFieldsV1", "DeveloperDeploymentEnrollmentFactsV1", "DeveloperDeploymentStartFieldsV1", "DeveloperDeploymentStartInputV1", "DeveloperDeploymentStartModeV1", "DeveloperDeploymentOwnerV1", "DeveloperDeploymentReadyV1", "DeveloperDeploymentStartOutcomeV1", "DeveloperDeploymentErrorV1", "start_developer_deployment_v1"]
owner = "Single-target developer DeploymentController enrollment and restart facade"
consumers = ["paraegox-local public deployment composition"]
compatibility = "This experimental Unix-only facade accepts one named, validated start bundle; its enrollment value is a typed projection of an already whole-file-pinned, canonical, signature-verified and cross-pinned PXEA, not caller-supplied wire bytes. Fresh creates one Controller store, commits real Authority tenure before use, executes the exact Controller-signed Node Describe, Runtime Describe, Node challenge, challenge-nonce-bound PXQR/PXQS, PXNO/PXNA and Node Latest sequence, cuts over PXJR to the one PXFS successor, then performs one PXFB and one fresh post-terminal Describe. Resume strictly reopens either the PXFS cutover marker or PXJR payload v9, revalidates manifest/auth/config/target/Authority pins, resolves the unique newer Prepared/Uncertain tenure transaction before any older committed proof, and initializes PXCR only when the otherwise valid Controller has no remote extension. PXJR v9 commits each exact request before a move-only send claim and each verified response before projection. On restart it durably closes AttemptInFlight authority, sends RequestDurableNotSent bytes at most once only after re-verifying their Controller signature and typed semantics, verifies the typed response and mTLS pins before commit, admits a fresh read-only attempt only from an explicit closed phase, and never blindly replays ambiguous NodePublish. Its typed partial projection and terminal cutover facts share one canonical decoder and expose no send token. PXFJ v6 retains the exact post-PXFB Describe phase and request/response bytes. A recovered in-flight Describe is closed without response and only a fresh identity may follow; a durable response is reverified. An uncertain PXFB is never replayed and a later ManagedReady PXDR cannot synthesize PXFR. DeveloperDeploymentStartOutcomeV1::Ready is constructible only after both durable PXFR and durable fresh ManagedReady PXDR; every other durable ambiguity returns ReconcileRequired with the active Controller or PXFS owner. DeveloperDeploymentOwnerV1 retains both restricted connector sessions, the current durable store lock and the real Authority; its bounded try_poll_exit/wait_for_exit supervision delegates to the Authority poll, and shutdown_and_join closes connectors, releases the store and joins Authority exactly once. This surface exposes no journal/store DTO, retry loop, two-machine proof, remote Agent/TUI, reconnect policy, continuous reconciler or production support claim. Incompatible enrollment provenance, persistent-version, attempt, Ready/Reconcile, supervision or shutdown semantics require an explicit successor."
tests = ["crates/paraegox-deployment/src/lib.rs", "crates/paraegox-deployment/src/controller_journal.rs", "crates/paraegox-deployment/src/controller_store.rs", "crates/paraegox-deployment/src/managed_serving_client.rs", "crates/paraegox-deployment/src/managed_fabric_apply.rs", "crates/paraegox-deployment/src/managed_fabric_store.rs", "crates/paraegox-deployment/src/deployment_process.rs", "crates/paraegox-deployment/src/developer_local_tenure_authority.rs", "crates/paraegox-local/src/composition.rs"]

[[registry.public_apis]]
module = "paraegox_deployment"
symbols = ["DeveloperDeploymentAgentBootstrapStartFieldsV1", "DeveloperDeploymentAgentBootstrapStartInputV1", "DeveloperDeploymentAgentBootstrapReadyV1", "DeveloperDeploymentAgentBootstrapStartOutcomeV1", "start_developer_deployment_agent_bootstrap_v1"]
owner = "Single-target developer DeploymentController Agent-bootstrap facade"
consumers = ["paraegox-local public deployment schema v2 composition"]
compatibility = "This additive Unix-only facade first consumes the complete predecessor DeveloperDeploymentStartInputV1 and then exactly one independently selected managed Agent provider, distinct nonzero Fabric and Agent service identities and one strict loopback Fabric listener; the fixed lifecycle, ingress and semantic limits stay Deployment-owned. It performs the predecessor T0 cutover before the three exact PXAG operations: PXAR v6 Fabric apply, PXAR v7 Agent-stack apply and DescribeConversationPort. For either Apply, the outer PXAG request_id equals the inner operation_id while outer nonce/signature authority remains independent. PXFJ v7 adds exactly fabric_agent_control, agent_stack_agent_control and conversation_port_descriptor slots; v2-v6 decode only with all three additive slots Idle. Prepare durably commits the exact inner and outer request bytes before transport; a move-only consuming claim commits Uncertain before one send, and Uncertain supplies neither replay nor action authority. A verified terminal atomically commits the exact inner and outer receipt bytes. Resume strictly revalidates terminal bytes and performs zero sends. Ready requires both Apply slots ReceiptDurable with their exact ActiveReady inners and one ReceiptDurable descriptor rooted in that PXST, and reports the final PXFJ sequence, managed-ready digest, exact PXFT/PXST digests, whole descriptor PXAH digest and current generations. The descriptor receipt is bootstrap evidence only: this facade provides no descriptor access, authorization, live session, Agent data plane, conversation or Echo operation. It adds no T2, reconnect loop, continuous reconciliation, remote TUI, two-host or production-platform claim. Incompatible inputs, slot order/version, durability phase, identity correlation, terminal validation, Ready facts or ownership semantics require an explicit successor."
tests = ["crates/paraegox-deployment/src/lib.rs", "crates/paraegox-deployment/src/deployment_process.rs", "crates/paraegox-deployment/src/managed_fabric_apply.rs", "crates/paraegox-deployment/src/managed_agent_stack_apply.rs", "crates/paraegox-deployment/src/managed_serving_client.rs", "crates/paraegox-deployment/src/managed_fabric_store.rs", "crates/paraegox-local/src/composition.rs", "tests/system/test_t1_public_agent_bootstrap_cli.py"]

[[registry.public_apis]]
module = "paraegox_kernel::digest"
symbols = ["Digest32", "DigestBuildError", "Digest32Builder"]
owner = "Kernel mechanisms"
consumers = ["paraegox-runtime-contracts", "paraegox-runtime", "paraegox-deployment"]
compatibility = "The SHA-256-v1 framing and golden bytes are stable; semantic domains remain contract-owned and versioned."
tests = ["crates/paraegox-kernel/src/digest.rs"]

[[registry.public_apis]]
module = "paraegox_kernel::identity"
symbols = ["RuntimeHostId", "PrincipalRef"]
owner = "Kernel identity values"
consumers = ["paraegox-runtime-contracts", "paraegox-runtime", "paraegox-deployment"]
compatibility = "Types stay opaque and non-interchangeable; their 16-byte values are stable in explicitly registered B2 envelope fields without defining a global serializer beyond those fields."
tests = ["crates/paraegox-kernel/src/identity.rs"]

[[registry.public_apis]]
module = "paraegox_kernel::time"
symbols = ["ClockDomainRef", "ClockGeneration", "BoundedDuration", "MonotonicInstant", "ClockReading", "MonotonicDeadline", "TimeError"]
owner = "Kernel time values"
consumers = ["paraegox-runtime-contracts", "paraegox-runtime", "paraegox-deployment"]
compatibility = "ClockDomainRef's 16-byte value, nonzero ClockGeneration, and nanosecond BoundedDuration are stable where embedded by B2 wire v1; MonotonicInstant, ClockReading, and MonotonicDeadline remain owner-local and are not serialized as protocol time."
tests = ["crates/paraegox-kernel/src/time.rs"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::assignment"
symbols = ["TARGET_ASSIGNMENTS_VERSION", "MAX_TARGET_ASSIGNMENTS", "MAX_TARGET_ASSIGNMENTS_BYTES", "RUNTIME_APPLY_REQUEST_VERSION", "MAX_RUNTIME_APPLY_REQUEST_BYTES", "BindingId", "InstanceRef", "PortRef", "MailboxRef", "SchemaRef", "PortDirection", "InteractionKind", "PortCardinality", "PortSpec", "OverflowPolicy", "DeliveryProfile", "MailboxSpec", "PortEndpoint", "BindingAssignment", "TargetAssignments", "RuntimePlanSlice", "RuntimeApplyRequest", "AssignmentContractError", "AssignmentWireErrorCode", "AssignmentWireError", "RequestWireErrorCode", "RequestWireError"]
owner = "Runtime canonical assignment and complete-request contract owner"
consumers = ["paraegox-deployment", "paraegox-runtime", "paraegox-fabric"]
compatibility = "PXTA v1 fixes the big-endian 256-byte static 1:1 binding record, BindingId ordering, exact schema identity/version/digest, duplicate rejection, bounds, enum values, assignment digest domain, and stable error codes; PXAR v1 fixes the bounded outer framing of one unchanged B2 envelope plus its canonical assignment body, without creating a second signature or request digest. Incompatible changes require a new protocol version."
tests = ["crates/paraegox-runtime-contracts/src/assignment.rs", "crates/paraegox-runtime/src/admission.rs", "tests/contract/test_s3_runtime_apply_request.py"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::provenance"
symbols = ["RUNTIME_SLICE_HEADER_VERSION", "PlanProvenance", "RuntimeSliceHeader", "RuntimeSliceCommitment", "SourceScopeRef", "SourcePlanRef", "SourcePlanRevision", "SourcePlanDigest", "TargetAssignmentDigest", "TargetSliceDigest", "ProvenanceContractError"]
owner = "Runtime contract owner"
consumers = ["paraegox-deployment", "paraegox-runtime"]
compatibility = "The B1 header version, target-slice digest framing, and golden outputs remain stable; B2 envelope v1 canonically carries their semantic fields and derived digests, while Rust layout remains non-ABI."
tests = ["crates/paraegox-runtime-contracts/src/provenance.rs", "crates/paraegox-runtime/src/apply_state.rs"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::apply"
symbols = ["WRITER_TENURE_SIGNING_TRANSCRIPT_VERSION", "MAX_TENURE_NONCE_BYTES", "MAX_TENURE_SIGNATURE_BYTES", "MAX_TENURE_SIGNING_TRANSCRIPT_BYTES", "PlanWriterRef", "ApplyOperationId", "TenureAuthorityRef", "TenureKeyRef", "PlanWriterEpoch", "TenureProofAlgorithm", "TenureProofAuthority", "WriterTenureClaim", "WriterTenureSigningTranscript", "WriterTenureProof", "TenureProofError", "PlanWriterContext", "ExpectedActive", "RuntimeApplyControl", "RuntimeApplyControlCommitment", "ApplyContractError"]
owner = "Runtime contract owner"
consumers = ["paraegox-deployment", "paraegox-runtime"]
compatibility = "The B1 exact target-slice CAS, proof-envelope fingerprint including its signature, apply-control v1 digest framing, and golden outputs remain stable; writer-tenure signing-transcript v1 and its bounds are published and exclude the signature, while B2 envelope compatibility is governed by the wire module."
tests = ["crates/paraegox-runtime-contracts/src/apply.rs", "crates/paraegox-runtime/src/admission.rs", "crates/paraegox-runtime/src/apply_state.rs", "tests/contract/test_s2_apply_vectors.py"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::temporal"
symbols = ["APPLY_TEMPORAL_CONSTRAINT_VERSION", "TemporalConstraintId", "ApplyTemporalConstraint", "TemporalContractError"]
owner = "Runtime contract owner"
consumers = ["paraegox-deployment", "paraegox-runtime"]
compatibility = "Version 1 fixes the constraint identity, target clock domain and generation, and original and remaining nanosecond budgets; remaining budget may only attenuate, while local monotonic instants and deadlines are never transported."
tests = ["crates/paraegox-runtime-contracts/src/temporal.rs", "crates/paraegox-runtime-contracts/src/wire.rs", "crates/paraegox-runtime/src/admission.rs", "tests/contract/test_s2_apply_vectors.py"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::wire"
symbols = ["RUNTIME_APPLY_ENVELOPE_VERSION", "APPLY_REQUEST_SIGNING_TRANSCRIPT_VERSION", "MAX_RUNTIME_APPLY_ENVELOPE_BYTES", "MAX_APPLY_AUTH_NONCE_BYTES", "MAX_APPLY_AUTH_SIGNATURE_BYTES", "ApplyAuthKeyRef", "ApplyAuthAlgorithm", "ApplyRequestAuthClaim", "ApplyRequestAuthentication", "ApplyRequestSigningTranscript", "RuntimeApplyEnvelopeDraft", "RuntimeApplyEnvelope", "ApplyAuthError", "EnvelopeContractError", "WireErrorCode", "WireError"]
owner = "Runtime contract owner"
consumers = ["paraegox-deployment", "paraegox-runtime"]
compatibility = "Version 1 fixes the magic, 37 ordered TLVs, big-endian scalar and length encoding, frame, nonce, and signature bounds, the 36-field request-signing transcript excluding only the request signature, request-digest framing, and stable WireErrorCode values; incompatible changes require a new protocol version."
tests = ["crates/paraegox-runtime-contracts/src/wire.rs", "crates/paraegox-runtime/src/admission.rs", "tests/contract/test_s2_apply_vectors.py"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::execution"
symbols = ["TARGET_EXECUTION_PLAN_VERSION", "RUNTIME_APPLY_REQUEST_V2_VERSION", "MAX_LOOP_DOMAINS", "MAX_MAILBOX_EXECUTIONS", "MAX_DOMAIN_OUTSTANDING", "MAX_SERVICE_COST_TOKENS", "MAX_MINIMUM_SERVICE_WEIGHT", "MAX_ARRIVALS_PER_WINDOW", "MAX_EXECUTION_DURATION_NANOS", "MAX_TARGET_EXECUTION_PLAN_BYTES", "MAX_RUNTIME_APPLY_REQUEST_V2_BYTES", "DomainRef", "CardDefinitionRef", "CardImplementationRef", "TargetExecutionDigest", "CallModel", "WorkloadKind", "BlockingRisk", "RunBoundProvenance", "DispatchClass", "OverrunAction", "LoopDomainCapacity", "LoopLifecycleBudgets", "LoopDomainSpec", "CardSubjectSpec", "LoopExecutionRequirements", "CallbackBudgets", "MailboxDispatchPolicy", "MailboxExecutionSpec", "TargetExecutionPlan", "TargetPlanAssignments", "RuntimePlanSliceV2", "RuntimeApplyRequestV2", "ExecutionContractError", "TargetPlanContractError", "ExecutionRecordSection", "ExecutionWireErrorCode", "ExecutionWireError", "TargetPlanWireErrorCode", "RequestV2WireErrorCode", "RequestV2WireError"]
owner = "Runtime execution contract owner"
consumers = ["paraegox-deployment", "paraegox-runtime"]
compatibility = "PXTE v1 fixes bounded big-endian 64-byte LoopDomain and 236-byte Mailbox execution records, canonical ordering, enum values, digest framing, bounds, and stable rejection codes for the inbound-Mailbox-driven Loop-eligible subset. PXAR v2 strictly frames one unchanged signed envelope, one PXTA v1 body, and one PXTE v1 body with three explicit lengths and no v1/v2 fallback; its explicit v2 composite digest commits the PXTA and PXTE digests through the existing Slice assignment-digest field. PXTA may be a superset, but records absent from PXTE remain execution-inert and grant no CardInstance, LoopDomain, callback, dispatcher, or Task authority. Loop admission requires cooperative async I/O or routing, no blocking risk, measured or certified run bounds, CooperativeCancel or Escalate overrun handling, total arrivals-times-(run-plus-cleanup) utilization within the common capacity window, control utilization within reserved capacity, and a strict scheduler-independent Control start bound derived across the signed PXTA/PXTE plan. MailboxExecutionSpec introduces subjects only for this inbound-Mailbox-driven subset: source-only Cards with no input Mailbox are not expressible, so these types are not a general or complete execution-plan contract. Incompatible framing, digest, eligibility, or scope changes require new explicit versions and domains."
tests = ["crates/paraegox-runtime-contracts/src/execution.rs", "crates/paraegox-runtime/src/admission.rs", "tests/contract/test_s4_runtime_execution_request.py", "tests/fixtures/wire/s4_runtime_apply_request_v2.json"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::thread_execution"
symbols = ["TARGET_EXECUTION_PLAN_V2_VERSION", "RUNTIME_APPLY_REQUEST_V3_VERSION", "MAX_THREAD_DOMAINS", "MAX_THREAD_MAILBOX_EXECUTIONS", "MAX_EXECUTOR_THREADS", "MAX_TARGET_EXECUTION_PLAN_V2_BYTES", "MAX_RUNTIME_APPLY_REQUEST_V3_BYTES", "ThreadDomainRef", "TargetExecutionDigestV2", "ExecutorBudgetSpec", "ThreadDomainSpec", "ThreadInvocationBudgets", "ThreadExecutionRequirements", "ThreadDispatchPolicy", "ThreadMailboxExecutionSpec", "TargetExecutionPlanV2", "TargetPlanAssignmentsV3", "RuntimePlanSliceV3", "RuntimeApplyRequestV3", "ThreadExecutionContractError", "TargetPlanV3ContractError", "ThreadExecutionRecordSection", "ThreadExecutionWireErrorCode", "ThreadExecutionWireError", "TargetPlanV3WireErrorCode", "RequestV3WireErrorCode", "RequestV3WireError"]
owner = "Runtime Thread execution contract owner"
consumers = ["paraegox-deployment", "paraegox-runtime"]
compatibility = "PXTE v2 additively embeds one byte-exact optional PXTE v1 Loop body and fixes bounded big-endian ThreadDomain and Thread Mailbox records, global framework/worker/distinct-instance-native budgeting, utilization, exact PXTA references, Loop/Thread identity separation, canonical ordering, digest framing, and stable rejection codes. PXAR v3 strictly frames one unchanged signed envelope, PXTA v1, and PXTE v2 with no older-version fallback; its v3 composite digest commits both canonical bodies through the existing signed Slice field. Runtime execution currently admits only the trusted single-subject one-worker zero-native profile; wider workers and nonzero native pools remain valid desired-state contracts but fail closed until an admitted Runtime owner exists. Incompatible framing, digest, eligibility, or scope changes require new explicit versions and domains."
tests = ["crates/paraegox-runtime-contracts/src/thread_execution.rs", "crates/paraegox-runtime/src/admission.rs", "crates/paraegox-runtime/src/thread_component_runtime.rs", "tests/contract/test_s5_runtime_thread_execution_request.py", "tests/fixtures/wire/s5_runtime_apply_request_v3.json"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::process_execution"
symbols = ["TARGET_EXECUTION_PLAN_V3_VERSION", "RUNTIME_APPLY_REQUEST_V4_VERSION", "MAX_PROCESS_DOMAINS", "MAX_PROCESS_MAILBOX_EXECUTIONS", "MAX_PROCESS_RESTART_ATTEMPTS", "MAX_PROCESS_WORKER_FRAME_BYTES", "PROCESS_WORKER_PROTOCOL_VERSION", "PROCESS_WORKER_HEADER_BYTES", "MAX_PROCESS_WORKER_PAYLOAD_BYTES", "MAX_PROCESS_WORKER_CREDITS", "MAX_PROCESS_WORKER_RETAINED_BYTES", "MAX_PROCESS_IPC_PAYLOAD_BYTES", "MAX_TARGET_EXECUTION_PLAN_V3_BYTES", "MAX_RUNTIME_APPLY_REQUEST_V4_BYTES", "ProcessDomainRef", "ProcessLaunchProfileRef", "ProcessTargetProfileRef", "ProcessSandboxProfileRef", "ProcessEntrypointRef", "TargetExecutionDigestV3", "WorkerRuntimeKind", "WorkspacePolicy", "ProcessAccessPolicy", "FailureContainmentPolicy", "SideEffectClass", "InvocationReplayPolicy", "RuntimeVersionRange", "ProcessProfileSelections", "ProcessLaunchSpec", "ProcessCapacitySpec", "ProcessLivenessBudgets", "ProcessShutdownBudgets", "ProcessLifecycleBudgets", "ProcessResourceLimits", "ProcessRestartPolicy", "ProcessDomainPolicies", "ProcessDomainSpec", "ProcessInvocationBudgets", "ProcessExecutionRequirements", "ProcessWorkloadSelection", "ProcessMailboxExecutionSpec", "TargetExecutionPlanV3", "TargetPlanAssignmentsV4", "RuntimePlanSliceV4", "RuntimeApplyRequestV4", "ProcessExecutionContractError", "TargetPlanV4ContractError", "ProcessExecutionRecordSection", "ProcessExecutionWireErrorCode", "ProcessExecutionWireError", "TargetPlanV4WireErrorCode", "RequestV4WireErrorCode", "RequestV4WireError"]
owner = "Runtime Process execution contract owner"
consumers = ["paraegox-deployment", "paraegox-runtime"]
compatibility = "PXTE v3 additively embeds one byte-exact optional PXTE v2 Loop/Thread body and fixes bounded ProcessDomain and Process Mailbox desired records, exact profile identities and digests, resource/lifecycle/restart declarations, no-replay invocation requirements, canonical ordering, composite digest framing, and stable rejection codes. PXAR v4 strictly frames one unchanged signed envelope, PXTA v1, and PXTE v3 with no older-version fallback. These are desired-state and wire contracts only; profile references do not prove OS sandbox or resource enforcement. Incompatible framing, digest, eligibility, or scope changes require new explicit versions and domains."
tests = ["crates/paraegox-runtime-contracts/src/process_execution.rs", "crates/paraegox-runtime/src/admission.rs", "tests/contract/test_s6_runtime_process_execution_request.py", "tests/fixtures/wire/s6_runtime_apply_request_v4.json"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::process_protocol"
symbols = ["PROCESS_WORKER_PROTOCOL_MAGIC", "PROCESS_WORKER_PROTOCOL_VERSION", "PROCESS_WORKER_HEADER_BYTES", "MAX_PROCESS_WORKER_FRAME_BYTES", "MAX_PROCESS_WORKER_PAYLOAD_BYTES", "MAX_PROCESS_WORKER_CREDITS", "MAX_PROCESS_WORKER_RETAINED_BYTES", "ProcessSessionGenerations", "ProcessSessionIdentity", "ProcessFrameDirection", "ProcessWorkerState", "ProcessFrameKind", "ConstructOutcome", "InvocationTerminalKind", "StopReason", "StoppedOutcome", "ProcessFrameBody", "ProcessFrame", "ProcessTerminalParts", "ProcessProtocolPhase", "ProcessProtocolSession", "ProcessProtocolErrorCode", "ProcessProtocolError"]
owner = "Runtime Process worker protocol owner"
consumers = ["paraegox-runtime", "paraegox-sdk reference worker"]
compatibility = "PXWP v1 fixes the canonical frame, exact live-generation identity fence, direction and phase rules, per-direction sequencing, Invoke/Invoked acknowledgement, bounded credits and retained-byte accounting, heartbeat snapshots, cancellation, terminal results, and stable error codes. It does not define process spawning, OS containment, Runtime readiness, replay, or recovery policy. Incompatible framing or session semantics require a new protocol version."
tests = ["crates/paraegox-runtime-contracts/src/process_protocol.rs", "tests/contract/test_s6_process_worker_protocol.py", "tests/unit/test_s6_python_worker_protocol.py", "tests/fixtures/wire/s6_process_worker_protocol_v1.json"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::installation"
symbols = ["MAX_INSTALLED_RUNTIME_ARTIFACT_BYTES", "MAX_INSTALLED_RUNTIME_BUILD_DESCRIPTOR_BYTES", "MAX_INSTALLED_RUNTIME_MANIFEST_BYTES", "InstalledRuntimeArtifactObservationV1", "RuntimeCompiledInstallationFactsV1", "GeneratedRuntimeBuildDescriptorV1", "VerifiedRuntimeInstallationV1", "RuntimeInstallationError", "generate_build_descriptor", "generate_manifest", "verify_existing"]
owner = "Runtime release/install artifact contract owner"
consumers = ["paraegox-runtime RuntimeHost release/install entrypoint"]
compatibility = "The version-1 installation seam preserves the canonical Runtime build descriptor and singleton compatibility manifest bytes, domain-separated digests, exact artifact/descriptor/manifest bounds, and typed executable-observation and independently compiled-facts inputs. Descriptor generation accepts no operator-supplied identity, manifest generation accepts no manifest input, and strict verification reconstructs no mutable alternate owner; incompatible framing, construction authority, or verification semantics require a new explicit contract version with no fallback."
tests = ["crates/paraegox-runtime-contracts/src/installation.rs", "crates/paraegox-runtime/src/runtime_artifact.rs", "crates/paraegox-runtime/src/runtime_initializer.rs", "crates/paraegox-runtime/src/runtime_host_entrypoint.rs", "tests/system/test_s7_runtime_install_process.py"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::reference_control"
symbols = ["REFERENCE_TARGET_EXECUTION_VERSION", "REFERENCE_RUNTIME_APPLY_REQUEST_VERSION", "REFERENCE_RUNTIME_APPLY_ENVELOPE_VERSION", "REFERENCE_APPLY_SIGNING_TRANSCRIPT_VERSION", "REFERENCE_BOOTSTRAP_VERSION", "REFERENCE_BOOTSTRAP_SIGNING_TRANSCRIPT_VERSION", "REFERENCE_QUERY_VERSION", "REFERENCE_QUERY_SIGNING_TRANSCRIPT_VERSION", "REFERENCE_APPLY_TERMINAL_RECEIPT_VERSION", "REFERENCE_APPLY_TERMINAL_RECEIPT_SIGNING_TRANSCRIPT_VERSION", "REFERENCE_PROFILE_VERSION", "MAX_REFERENCE_TARGET_EXECUTION_BYTES", "MAX_REFERENCE_RUNTIME_APPLY_REQUEST_BYTES", "MAX_REFERENCE_RUNTIME_PLAN_SLICE_BYTES", "MAX_REFERENCE_RUNTIME_APPLY_ENVELOPE_BYTES", "MAX_REFERENCE_APPLY_AUTH_NONCE_BYTES", "MAX_REFERENCE_APPLY_AUTH_SIGNATURE_BYTES", "MAX_REFERENCE_BOOTSTRAP_REQUEST_BYTES", "MAX_REFERENCE_BOOTSTRAP_RESPONSE_BYTES", "MAX_REFERENCE_QUERY_REQUEST_BYTES", "MAX_REFERENCE_QUERY_RESPONSE_BYTES", "MAX_REFERENCE_QUERY_NONCE_BYTES", "MAX_REFERENCE_QUERY_SIGNATURE_BYTES", "REFERENCE_QUERY_RECORD_COUNT", "MAX_REFERENCE_BOOTSTRAP_NONCE_BYTES", "MAX_REFERENCE_BOOTSTRAP_SIGNATURE_BYTES", "MAX_REFERENCE_APPLY_TERMINAL_RECEIPT_BYTES", "MAX_REFERENCE_APPLY_TERMINAL_RECEIPT_SIGNATURE_BYTES", "MAX_REFERENCE_LIFECYCLE_NANOS", "REFERENCE_ADMISSION_TENURE_NONCE_CAPACITY", "REFERENCE_ADMISSION_REQUEST_NONCE_CAPACITY", "REFERENCE_ADMISSION_TEMPORAL_LINEAGE_CAPACITY", "REFERENCE_PROFILE_LIFECYCLE_CONCURRENCY", "REFERENCE_PROFILE_MAILBOX_SLOTS", "REFERENCE_PROFILE_DISPATCH_SLOTS", "REFERENCE_PROFILE_BACKGROUND_TASK_SLOTS", "REFERENCE_CHANNEL_BINDING_VERSION", "ed25519_control_key_fingerprint", "ReferenceAdmissionPolicyInputV1", "ReferenceAdmissionPolicyFingerprintV1", "reference_admission_policy_fingerprint_v1", "reference_local_control_endpoint_identity_digest_v1", "reference_runtime_peer_credentials_digest_v1", "ReferenceBootstrapChannelPolicyInputV1", "reference_bootstrap_channel_policy_fingerprint_v1", "ReferenceControlContractErrorCode", "ReferenceControlWireErrorCode", "ReferenceControlWireError", "ReferenceControlError", "ValidatedReferenceLifecycleBudgetsV1", "ReferenceAssemblyModeV1", "ReferenceLoopFactsV1", "ReferenceTargetExecutionPlanV4", "ReferenceApplySigningTranscriptV2", "ReferenceApplyRequestDraftV1", "ReferenceApplyRequestV1", "ReferenceApplyIngressIdentitiesV1", "reference_apply_ingress_identities_v1", "verify_reference_durable_slice_v1", "ReferenceApplyTerminalOutcomeV1", "ReferenceApplyTerminalLifecycleEffectV1", "ReferenceApplyTerminalHeadV1", "ReferenceApplyTerminalResultRefV1", "ReferenceApplyTerminalFactsV1", "ReferenceApplyTerminalReceiptAuthClaimV1", "ReferenceApplyTerminalReceiptSigningTranscriptV1", "ReferenceApplyTerminalReceiptDraftV1", "ReferenceApplyTerminalReceiptV1", "ReferenceBootstrapRequestIdV1", "ReferenceChannelBindingV1", "ReferenceBootstrapSigningTranscriptV1", "ReferenceBootstrapRequestDraftV1", "ReferenceBootstrapRequestV1", "ReferenceBootstrapServingIdentityV1", "ReferenceControllerBootstrapExpectationV1", "ReferenceBootstrapCompatibilityV1", "ReferenceBootstrapStateV1", "ReferenceOperationalReasonV1", "ReferenceBootstrapFactsV1", "ReferenceBootstrapResponseAuthClaimV1", "ReferenceBootstrapResponseDraftV1", "ReferenceBootstrapResponseV1", "ReferenceQueryIdV1", "ReferenceQuerySelectorV1", "ReferenceQuerySigningTranscriptV1", "ReferenceQueryRequestDraftV1", "ReferenceQueryRequestV1", "ReferenceQueryOwnerStateV1", "ReferenceQueryDurablePhaseV1", "ReferenceQueryOperationLookupV1", "ReferenceQueryOperationStateV1", "ReferenceQueryDesiredHeadV1", "ReferenceQueryDesiredStateV1", "ReferenceQueryLiveStateV1", "ReferenceQueryLiveFactsV1", "ReferenceQueryFactsV1", "ReferenceQueryResponseAuthClaimV1", "ReferenceQueryResponseDraftV1", "ReferenceQueryResponseV1"]
owner = "Reference-profile Runtime control contract facade owner"
consumers = ["paraegox-deployment", "paraegox-runtime"]
compatibility = "This narrow facade delegates to the canonical PXTE v4, PXAR v5, signed-envelope v2, bootstrap v1, query v1, terminal-Receipt v1, profile v1, lifecycle, admission-policy, channel-policy, ingress-identity, and key-fingerprint owners. It adds no second serializer or transport protocol. Exact decode preserves authenticated request bytes and exposes the canonical PXTA-plus-PXTE Slice range used by Runtime admission. The durable Slice verifier accepts only exact PXTA-zero plus PXTE-v4 bytes together with complete journal-owned provenance and the expected target Slice digest; it cannot recover or fabricate missing provenance. Query requests and responses preserve the existing PXQR/PXQS bytes, digests, signing transcripts, invalid-precedence behavior, bounds, request/channel/store correlation, expectation semantics, and serving freshness. Runtime's authenticated local endpoint and deploymentd's bounded one-shot reconciler are the real producer and consumer of these canonical query contracts; neither changes their owner or creates a second codec. Incompatible request, bootstrap, query, terminal-Receipt, profile, digest, signing, or error-code semantics require a new explicit canonical version rather than fallback."
tests = ["crates/paraegox-runtime-contracts/src/reference_control.rs", "crates/paraegox-runtime-contracts/src/reference_assembly.rs", "crates/paraegox-deployment/src/controller_apply.rs", "crates/paraegox-deployment/src/controller_bootstrap.rs", "crates/paraegox-deployment/src/controller_journal.rs", "crates/paraegox-deployment/src/deployment_process.rs", "crates/paraegox-deployment/src/runtime_control_client.rs", "crates/paraegox-runtime/src/admission.rs", "crates/paraegox-runtime/src/runtime_control_endpoint.rs", "crates/paraegox-runtime/src/runtime_control_state.rs"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::managed_service"
symbols = ["MANAGED_SERVICE_CONTRACT_VERSION", "ManagedServiceId", "ManagedServiceGeneration", "ManagedServiceLifecycleStage", "ManagedServiceLifecycleBudgetsV1", "ManagedServiceSpecV1", "ManagedServiceContractError"]
owner = "Runtime managed CoreService lifecycle contract owner"
consumers = ["paraegox-runtime"]
compatibility = "Version 1 defines only an independently owned service identity, a nonzero Runtime generation fence, and finite nonzero prepare/start/readiness/drain/stop budgets. It is not a ServiceSpec graph, committed-plan wire, ProcessDomain launch contract, endpoint binding, or second desired-state owner; those require explicit successors and real executable consumers."
tests = ["crates/paraegox-runtime-contracts/src/managed_service.rs", "crates/paraegox-runtime/src/managed_service_assembly.rs"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::managed_fabric_plan"
symbols = ["MANAGED_FABRIC_PROJECTION_VERSION", "MANAGED_FABRIC_APPLY_REQUEST_VERSION", "MANAGED_FABRIC_TARGET_EXECUTION_VERSION", "MANAGED_FABRIC_PROFILE_VERSION", "MANAGED_FABRIC_APPLY_ENVELOPE_VERSION", "MANAGED_FABRIC_APPLY_SIGNING_TRANSCRIPT_VERSION", "MAX_MANAGED_FABRIC_LISTEN_ENDPOINT_BYTES", "MAX_MANAGED_FABRIC_LIFECYCLE_NANOS", "MANAGED_FABRIC_PROJECTION_BYTES", "MAX_MANAGED_FABRIC_TARGET_EXECUTION_BYTES", "MAX_MANAGED_FABRIC_PLAN_SLICE_BYTES", "MAX_MANAGED_FABRIC_APPLY_REQUEST_BYTES", "MANAGED_FABRIC_APPLY_TERMINAL_RECEIPT_VERSION", "MANAGED_FABRIC_APPLY_TERMINAL_SIGNING_TRANSCRIPT_VERSION", "MAX_MANAGED_FABRIC_APPLY_TERMINAL_RECEIPT_BYTES", "MAX_MANAGED_FABRIC_APPLY_TERMINAL_SIGNATURE_BYTES", "ManagedFabricProjectionFieldsV1", "ManagedFabricManifestProjectionV1", "ManagedFabricListenEndpointV1", "ManagedFabricTargetModeV1", "ManagedFabricTargetExecutionV1", "ManagedFabricApplySigningTranscriptV2", "ManagedFabricApplyRequestDraftV1", "ManagedFabricApplyRequestV1", "verify_managed_fabric_durable_slice_v1", "managed_fabric_compatibility_digest_v1", "ManagedFabricApplyTerminalOutcomeV1", "ManagedFabricApplyTerminalLifecycleEffectV1", "ManagedFabricApplyTerminalHeadV1", "ManagedFabricApplyTerminalResultRefV1", "ManagedFabricApplyTerminalStateV1", "ManagedFabricApplyTerminalEvidenceV1", "ManagedFabricApplyTerminalFactsV1", "ManagedFabricApplyTerminalReceiptAuthClaimV1", "ManagedFabricApplyTerminalReceiptSigningTranscriptV1", "ManagedFabricApplyTerminalReceiptDraftV1", "ManagedFabricApplyTerminalReceiptV1", "ManagedFabricWireErrorCode", "ManagedFabricWireError", "ManagedFabricPlanError"]
owner = "Managed Fabric successor plan contract owner"
consumers = ["paraegox-deployment", "paraegox-runtime"]
compatibility = "PXMP v1, PXTE v5 and PXAR v6 are additive strict successors that admit exactly one Runtime-managed FabricService with a canonical IPv4-loopback TCP listen endpoint, or authoritative EmptyDeactivate. Desired bytes contain no Runtime generation, Agent identity, route/key expression, connect endpoint, Zenoh type, or nonempty PXTA. The transition projection is derived independently by Controller and Runtime from already verified installation ingress plus the locally compiled managed-fabric compatibility digest; decoding request bytes alone is never compatibility admission. Independent PXFT v1 binds one Runtime-signed terminal to the exact PXAR6 request, store, provenance, operation, nonce, Slice/assignment, request-time channel and response key; its ActiveReady, EmptyExactZero, NoEffectRejected, Uncertain, and Quarantined outcomes carry explicit lifecycle-effect, desired-head, generation, resource/raw evidence, Runtime epoch/snapshot, and selection-clock facts. Empty exact-zero may prove either a no-effect fast path or prior lifecycle cleanup. PXRT v1 and PXFT v1 strictly cross-reject, while PXTE v4, PXAR v5 and their fixtures remain byte-for-byte frozen. Incompatible wire, terminal, shape, digest, endpoint, or desired-state semantics require a new explicit version."
tests = ["crates/paraegox-runtime-contracts/src/managed_fabric_plan.rs", "tests/contract/test_s7_managed_fabric_successor.py", "tests/fixtures/wire/s7_managed_fabric_successor_v1.json"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::managed_serving_bootstrap"
symbols = ["MANAGED_SERVING_BOOTSTRAP_VERSION", "MANAGED_SERVING_BOOTSTRAP_REQUEST_SIGNING_VERSION", "MANAGED_SERVING_BOOTSTRAP_RESPONSE_SIGNING_VERSION", "MAX_MANAGED_SERVING_BOOTSTRAP_REQUEST_BYTES", "MAX_MANAGED_SERVING_BOOTSTRAP_RESPONSE_BYTES", "RUNTIME_CONTROL_CARRIER_VERSION", "RUNTIME_CONTROL_CARRIER_REQUEST_SIGNING_VERSION", "RUNTIME_CONTROL_DESCRIBE_READY_SIGNING_VERSION", "MAX_RUNTIME_CONTROL_CARRIER_REQUEST_BYTES", "MAX_RUNTIME_CONTROL_DESCRIBE_READY_RESPONSE_BYTES", "ManagedServingBootstrapRequestIdV1", "ManagedServingReadinessV1", "ManagedServingBootstrapFactsV1", "ManagedServingBootstrapSigningTranscriptV1", "ManagedServingBootstrapRequestDraftV1", "ManagedServingBootstrapRequestV1", "ManagedServingBootstrapResponseAuthClaimV1", "ManagedServingBootstrapResponseDraftV1", "ManagedServingBootstrapResponseV1", "RuntimeControlCarrierKindV1", "RuntimeControlCarrierSigningTranscriptV1", "RuntimeControlCarrierRequestDraftV1", "RuntimeControlCarrierRequestV1", "ControllerAuthenticatedRuntimeControlCarrierV1", "RuntimeControlDescribeReadyPhaseV1", "RuntimeControlDescribeReadyFactsV1", "RuntimeControlDescribeReadyResponseDraftV1", "RuntimeControlDescribeReadyResponseV1", "RuntimeAuthenticatedControlDescribeReadyV1", "ManagedServingBootstrapError"]
owner = "Recovered Runtime serving-observation contract owner"
consumers = ["paraegox-deployment", "paraegox-runtime", "paraegox-fabric restricted Runtime-control transport"]
compatibility = "PXFB/PXFR v1 remains the authenticated, request-correlated, read-only recovered-serving observation: it reports the Runtime host epoch, snapshot sequence, clock generation, exact store/target/manifest/channel identities and RecoveredReady without applying desired state, refreshing authority, retrying an uncertain request or mutating Runtime. Additive PXCC/PXDR v1 wraps only Describe, one byte-identical independently authenticated PXFB, or one byte-identical independently authenticated PXQR under an exact PXCB and Controller signature; no apply, Agent or arbitrary payload kind is admitted. PXDR is Runtime-signed, echoes the exact request digest/nonce and PXCB, and reports LegacyReady or ManagedReady plus recovered serving facts. Its ReferenceChannelBinding is explicitly the existing Runtime-local UDS/process binding, never a TLS session binding, certificate identity or transport authorization. Fabric consumes only the exact bounded raw-frame sizes and owns no codec or authentication. Incompatible fields, kind selection, bounds, signing, correlation, phase or channel semantics require a new explicit version."
tests = ["crates/paraegox-runtime-contracts/src/managed_serving_bootstrap.rs", "tests/contract/test_s7_managed_serving_bootstrap.py", "tests/fixtures/wire/s7_managed_serving_bootstrap_v1.json", "crates/paraegox-deployment/src/managed_serving_client.rs", "crates/paraegox-deployment/src/distributed_agent_stack_node_reconcile.rs", "crates/paraegox-fabric/src/runtime_apply.rs", "crates/paraegox-runtime/src/runtime_control_endpoint.rs"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::managed_serving_bootstrap"
symbols = ["RUNTIME_AGENT_CONTROL_REQUEST_MAGIC", "RUNTIME_AGENT_CONTROL_RECEIPT_MAGIC", "RUNTIME_AGENT_CONTROL_VERSION", "RUNTIME_AGENT_CONTROL_REQUEST_SIGNING_VERSION", "RUNTIME_AGENT_CONTROL_RECEIPT_SIGNING_VERSION", "MAX_RUNTIME_AGENT_PORT_DESCRIPTOR_BYTES", "MAX_RUNTIME_AGENT_CONTROL_REQUEST_BYTES", "MAX_RUNTIME_AGENT_CONTROL_RECEIPT_BYTES", "RuntimeAgentControlRequestIdV1", "RuntimeAgentControlKindV1", "RuntimeAgentControlRequestFieldsV1", "RuntimeAgentControlSigningTranscriptV1", "RuntimeAgentControlRequestDraftV1", "RuntimeAgentControlRequestV1", "ControllerAuthenticatedRuntimeAgentControlRequestV1", "RuntimeAgentControlResponseAuthClaimV1", "RuntimeVerifiedHistoricalManagedAgentStackReceiptV1", "RuntimeAgentControlReceiptDraftV1", "RuntimeAgentControlReceiptV1", "RuntimeAuthenticatedAgentControlReceiptV1", "runtime_agent_control_descriptor_payload_digest_v1"]
owner = "Runtime Agent-control carrier contract owner"
consumers = ["paraegox-runtime", "paraegox-deployment", "paraegox-fabric restricted Runtime-control transport"]
compatibility = "Additive PXAG/PXAH v1 is one target-scoped Controller-authenticated request and Runtime-authenticated receipt contract with exactly ApplyManagedFabric, ApplyManagedAgentStack and DescribeConversationPort kinds. The two Apply kinds carry the frozen canonical PXAR v6 or PXAR v7 bytes unchanged; each PXAG request_id must equal the carried inner operation_id, while the outer nonce, signing transcript and signature remain independently fresh. PXAH is exactly request-, nonce-, target-, store-, epoch-, kind-, audience- and PXCB-correlated and carries one exact PXFT, one exact PXST or one bounded opaque PXAP descriptor. Outer verification does not replace the inner terminal signature check. A historical PXST may be wrapped only through the Runtime-verified historical marker: the inner terminal bytes and receipt-time Runtime-local channel remain unchanged, no successor effect is replayed, and the containing PXAH uses the current authenticated carrier facts. The Describe receipt is rooted in the exact PXST receipt digest and current Fabric/Agent generations, but its verifier is intentionally bootstrap-only and supplies no descriptor access, authorization, live session, Agent data-plane, conversation or Echo capability. The additive shared helper validates exact PXAP-v1 framing and computes the frozen PXAH payload-digest domain for both an old bootstrap descriptor root and a freshly described descriptor root; it never computes or substitutes the distinct whole-PXAH receipt digest. Incompatible magic, bound, kind, field order, identity correlation, inner-byte preservation, historical-receipt rule, descriptor root/generation or signing semantics require an explicit successor."
tests = ["crates/paraegox-runtime-contracts/src/managed_serving_bootstrap.rs", "tests/contract/test_s7_managed_fabric_successor.py", "tests/fixtures/wire/s7_managed_fabric_successor_v1.json", "tests/contract/test_s7_managed_agent_stack_successor.py", "tests/fixtures/wire/s7_managed_agent_stack_successor_v1.json", "crates/paraegox-runtime-contracts/tests/remote_agent_access_contract.rs", "tests/contract/test_t2_remote_agent_access.py", "tests/fixtures/wire/t2_remote_agent_data_plane_v1.json", "tests/fixtures/wire/t2_remote_agent_access_v1.json", "crates/paraegox-deployment/src/managed_serving_client.rs", "crates/paraegox-runtime/src/runtime_control_endpoint.rs"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::managed_agent_stack_plan"
symbols = ["MANAGED_AGENT_STACK_PROJECTION_VERSION", "MANAGED_AGENT_STACK_APPLY_REQUEST_VERSION", "MANAGED_AGENT_STACK_TARGET_EXECUTION_VERSION", "MANAGED_AGENT_STACK_PROFILE_VERSION", "MANAGED_AGENT_PROVIDER_SELECTION_VERSION", "MAX_MANAGED_AGENT_KEY_EXPRESSION_BYTES", "MAX_MANAGED_AGENT_INGRESS_ITEMS", "MAX_MANAGED_AGENT_INGRESS_BYTES", "MIN_MANAGED_AGENT_FRAME_BYTES", "MAX_MANAGED_AGENT_FRAME_BYTES", "MIN_MANAGED_AGENT_RESPONSE_BODY_BYTES", "MAX_MANAGED_AGENT_RESPONSE_BODY_BYTES", "MAX_MANAGED_AGENT_HANDLER_TIMEOUT_NANOS", "MAX_MANAGED_AGENT_SESSIONS", "MAX_MANAGED_AGENT_TURNS_PER_SESSION", "MAX_MANAGED_AGENT_REQUESTS_PER_SESSION", "MAX_MANAGED_AGENT_EVENT_BATCH", "MANAGED_AGENT_STACK_PROJECTION_BYTES", "MAX_MANAGED_AGENT_STACK_TARGET_EXECUTION_BYTES", "MAX_MANAGED_AGENT_STACK_PLAN_SLICE_BYTES", "MAX_MANAGED_AGENT_STACK_APPLY_REQUEST_BYTES", "MANAGED_AGENT_STACK_TERMINAL_RECEIPT_VERSION", "MANAGED_AGENT_STACK_TERMINAL_SIGNING_VERSION", "MAX_MANAGED_AGENT_STACK_TERMINAL_RECEIPT_BYTES", "MAX_MANAGED_AGENT_STACK_TERMINAL_SIGNATURE_BYTES", "ManagedAgentProviderRefV1", "ManagedAgentSecretRefV1", "ManagedAgentStackProjectionV1", "ManagedAgentStackTargetModeV1", "ManagedAgentProviderProfileV1", "ManagedAgentIngressLimitsV1", "ManagedAgentSemanticLimitsV1", "ManagedAgentPortPlanV1", "ManagedAgentProviderSelectionV1", "ManagedAgentServicePlanV1", "ManagedAgentStackTargetExecutionV1", "ManagedAgentStackApplySigningTranscriptV2", "ManagedAgentStackApplyRequestDraftV1", "ManagedAgentStackApplyRequestV1", "verify_managed_agent_stack_durable_slice_v1", "managed_agent_stack_compatibility_digest_v1", "ManagedAgentStackTerminalOutcomeV1", "ManagedAgentStackTerminalLifecycleEffectV1", "ManagedAgentStackTerminalHeadV1", "ManagedAgentStackTerminalResultRefV1", "ManagedAgentStackTerminalStateV1", "ManagedAgentStackTerminalEvidenceFieldsV1", "ManagedAgentStackTerminalEvidenceV1", "ManagedAgentStackTerminalFactsV1", "ManagedAgentStackTerminalAuthClaimV1", "ManagedAgentStackTerminalSigningTranscriptV1", "ManagedAgentStackTerminalReceiptDraftV1", "ManagedAgentStackTerminalReceiptV1", "ManagedAgentStackPlanError"]
owner = "Fixed managed Fabric→Agent successor plan contract owner"
consumers = ["paraegox-deployment", "paraegox-runtime"]
compatibility = "PXSP v1, PXTE v6, PXAR v7 and PXST v1 form one additive fixed successor, not a general service graph. The active shape retains the exact verified PXTE v5 Fabric desired value and adds exactly one Agent service with two distinct typed bindings, explicit bounded ingress and semantic limits, and an explicit provisioned or labelled deterministic-fixture provider selection. EmptyDeactivate is the only other shape. Runtime generations, journal paths, credentials, arbitrary dependency edges and raw Zenoh objects are excluded from desired bytes. PXAR v6/v7 and PXFT/PXST cross-reject; a successful PXST reports separate Fabric and Agent generations, exact two-binding census, dependency/readiness, resource evidence and request-time authentication correlation. Incompatible graph shape, provider policy, route, limits, terminal evidence or wire semantics require a new explicit version."
tests = ["crates/paraegox-runtime-contracts/src/managed_agent_stack_plan.rs", "tests/contract/test_s7_managed_agent_stack_successor.py", "tests/fixtures/wire/s7_managed_agent_stack_successor_v1.json", "crates/paraegox-runtime/src/managed_agent_runtime.rs", "crates/paraegox-runtime/src/managed_fabric_runtime.rs"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::managed_model_agent_stack_plan"
symbols = ["MANAGED_MODEL_AGENT_STACK_PROJECTION_VERSION", "MANAGED_MODEL_AGENT_STACK_APPLY_REQUEST_VERSION", "MANAGED_MODEL_AGENT_STACK_TARGET_EXECUTION_VERSION", "MANAGED_MODEL_AGENT_STACK_PROFILE_VERSION", "MANAGED_MODEL_ADAPTER_BINDING_VERSION", "MANAGED_MODEL_BOUNDED_TEXT_CAPABILITY_ID", "MANAGED_MODEL_AGENT_DEPENDENCY_VERSION", "MAX_MANAGED_MODEL_IN_FLIGHT", "MANAGED_MODEL_AGENT_STACK_PROJECTION_BYTES", "MAX_MANAGED_MODEL_AGENT_STACK_TARGET_EXECUTION_BYTES", "MAX_MANAGED_MODEL_AGENT_STACK_PLAN_SLICE_BYTES", "MAX_MANAGED_MODEL_AGENT_STACK_APPLY_REQUEST_BYTES", "MANAGED_MODEL_AGENT_STACK_TERMINAL_RECEIPT_VERSION", "MANAGED_MODEL_AGENT_STACK_TERMINAL_SIGNING_VERSION", "MAX_MANAGED_MODEL_AGENT_STACK_TERMINAL_RECEIPT_BYTES", "MAX_MANAGED_MODEL_AGENT_STACK_TERMINAL_SIGNATURE_BYTES", "ManagedModelAdapterVersionV1", "ManagedModelCapabilityIdV1", "ManagedModelAdapterBindingV1", "ManagedModelServicePlanV1", "ManagedModelAgentStackTargetModeV1", "ManagedModelAgentDependencyKindV1", "ManagedModelAgentDependencyReadinessV1", "ManagedModelAgentDependencyLossPolicyV1", "ManagedModelAgentServiceDependencyV1", "ManagedModelAgentStackProjectionV1", "ManagedModelAgentStackTargetExecutionV1", "ManagedModelAgentStackApplySigningTranscriptV2", "ManagedModelAgentStackApplyRequestDraftV1", "ManagedModelAgentStackApplyRequestV1", "verify_managed_model_agent_stack_durable_slice_v1", "ManagedModelAgentStackTerminalOutcomeV1", "ManagedModelAgentStackTerminalLifecycleEffectV1", "ManagedModelAgentStackTerminalHeadV1", "ManagedModelAgentStackTerminalResultRefV1", "ManagedModelAgentStackTerminalStateV1", "ManagedModelAgentStackTerminalEvidenceFieldsV1", "ManagedModelAgentStackTerminalEvidenceV1", "ManagedModelAgentStackTerminalFactsV1", "ManagedModelAgentStackTerminalAuthClaimV1", "ManagedModelAgentStackTerminalSigningTranscriptV1", "ManagedModelAgentStackTerminalReceiptDraftV1", "ManagedModelAgentStackTerminalReceiptV1", "managed_model_agent_stack_compatibility_digest_v1", "ManagedModelAgentStackPlanError"]
owner = "Fixed managed Fabric→Model+Agent successor plan contract owner"
consumers = ["paraegox-deployment", "paraegox-runtime"]
compatibility = "PXMM v1, PXTE v8, PXAR v9 and PXMT v1 form one additive fixed successor, not a general service graph or plugin system. The active shape retains one exact PXTE v6 FabricAndAgent value, adds exactly one bounded Model service, commits the complete provider selection and exact adapter ID/version/capability binding, and derives the only two admitted readiness edges: Fabric→Agent and Model→Agent with generation fencing and stop-consumer loss policy. EmptyDeactivate retains the exact embedded PXTE v6 empty value and contains no Model plan or dependency bytes. Runtime generation, backend object, credential bytes, arbitrary dependency edges, discovery, routing and fallback remain excluded from desired state. A successful signed PXMT ActiveReady binds the exact PXAR9 request, Runtime store and request-time channel, reports distinct Fabric/Model/Agent generations, exact two-binding census and readiness evidence, and is the only receipt that can release the A2 conversation capability. Frozen PXAR6/PXAR7/PXAR8 and PXFT/PXST/PXDS contracts remain distinct and cross-reject. Incompatible provider, adapter, dependency, terminal, lifecycle or wire semantics require an explicit successor."
tests = ["crates/paraegox-runtime-contracts/src/managed_model_agent_stack_plan.rs", "crates/paraegox-deployment/src/managed_model_agent_stack_producer.rs", "crates/paraegox-deployment/src/managed_model_agent_stack_apply.rs", "crates/paraegox-runtime/src/managed_model_runtime.rs", "crates/paraegox-runtime/src/managed_model_agent_stack_state.rs", "crates/paraegox-runtime/src/managed_model_agent_stack_runtime.rs", "crates/paraegox-runtime/src/runtime_control_endpoint.rs", "crates/paraegox-local/src/composition.rs"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::distributed_agent_stack_plan"
symbols = ["DISTRIBUTED_AGENT_STACK_PROJECTION_VERSION", "DISTRIBUTED_AGENT_STACK_APPLY_REQUEST_VERSION", "DISTRIBUTED_AGENT_STACK_TARGET_EXECUTION_VERSION", "DISTRIBUTED_AGENT_STACK_PROFILE_VERSION", "DISTRIBUTED_FABRIC_TOPOLOGY_VERSION", "DISTRIBUTED_FABRIC_AUTHENTICATION_REQUIREMENT_VERSION", "DISTRIBUTED_FABRIC_TRANSPORT_PROOF_VERSION", "DISTRIBUTED_AGENT_STACK_TERMINAL_RECEIPT_VERSION", "DISTRIBUTED_AGENT_STACK_TERMINAL_SIGNING_VERSION", "RESTRICTED_RUNTIME_APPLY_CARRIER_BINDING_VERSION", "RESTRICTED_RUNTIME_APPLY_TRANSPORT_PROFILE_VERSION", "DISTRIBUTED_AGENT_STACK_RESTRICTED_APPLY_REQUEST_VERSION", "DISTRIBUTED_AGENT_STACK_TERMINAL_RECEIPT_V2_VERSION", "DISTRIBUTED_AGENT_STACK_TERMINAL_SIGNING_V2_VERSION", "MAX_DISTRIBUTED_FABRIC_PEERS", "MAX_DISTRIBUTED_FABRIC_ENDPOINT_BYTES", "DISTRIBUTED_AGENT_STACK_PROJECTION_BYTES", "MAX_DISTRIBUTED_FABRIC_TOPOLOGY_BYTES", "MAX_DISTRIBUTED_AGENT_STACK_TARGET_EXECUTION_BYTES", "MAX_DISTRIBUTED_AGENT_STACK_PLAN_SLICE_BYTES", "MAX_DISTRIBUTED_AGENT_STACK_APPLY_REQUEST_BYTES", "DISTRIBUTED_FABRIC_TRANSPORT_PROOF_BYTES", "MAX_DISTRIBUTED_AGENT_STACK_TERMINAL_RECEIPT_BYTES", "MAX_DISTRIBUTED_AGENT_STACK_TERMINAL_SIGNATURE_BYTES", "MAX_RESTRICTED_RUNTIME_APPLY_ROUTE_BYTES", "MAX_RESTRICTED_RUNTIME_APPLY_OPERATION_TIMEOUT_NANOS", "MAX_RESTRICTED_RUNTIME_APPLY_TRANSPORT_PROFILE_BYTES", "MAX_RESTRICTED_RUNTIME_APPLY_CARRIER_BINDING_BYTES", "MAX_DISTRIBUTED_AGENT_STACK_RESTRICTED_APPLY_REQUEST_BYTES", "MAX_DISTRIBUTED_AGENT_STACK_TERMINAL_RECEIPT_V2_BYTES", "DistributedFabricTrustDomainRefV1", "DistributedFabricCredentialRefV1", "DistributedFabricTrustAnchorRefV1", "DistributedFabricPeerIdentityRefV1", "DistributedFabricSessionEpochV1", "DistributedFabricTransportEvidenceRefV1", "DistributedFabricTlsEndpointV1", "RestrictedRuntimeApplyTransportProfileFieldsV1", "RestrictedRuntimeApplyTransportProfileV1", "DistributedFabricAuthenticationProfileV1", "DistributedFabricPeerAuthenticationRequirementV1", "DistributedFabricPeerPlanV1", "DistributedFabricTopologyV1", "DistributedAgentStackProjectionV1", "DistributedAgentStackTargetModeV1", "DistributedAgentStackTargetExecutionV1", "DistributedAgentStackApplySigningTranscriptV2", "DistributedAgentStackApplyRequestDraftV1", "DistributedAgentStackApplyRequestV1", "RestrictedRuntimeApplyCarrierBindingFieldsV1", "RestrictedRuntimeApplyCarrierBindingV1", "DistributedAgentStackRestrictedApplySigningTranscriptV1", "DistributedAgentStackRestrictedApplyRequestDraftV1", "DistributedAgentStackRestrictedApplyRequestV1", "ControllerAuthenticatedDistributedAgentStackApplyRequestV1", "DistributedFabricObservedTransportProofFieldsV1", "DistributedFabricObservedTransportProofV1", "DistributedAgentStackTerminalOutcomeV1", "DistributedAgentStackLocalBindingEvidenceFieldsV1", "distributed_agent_stack_installed_binding_set_digest_v1", "distributed_agent_stack_empty_binding_set_digest_v1", "DistributedAgentStackTerminalEvidenceFieldsV1", "DistributedAgentStackTerminalObservationsV1", "DistributedAgentStackTerminalFactsV1", "DistributedAgentStackTerminalAuthClaimV1", "DistributedAgentStackTerminalSigningTranscriptV1", "DistributedAgentStackTerminalReceiptDraftV1", "DistributedAgentStackTerminalReceiptV1", "DistributedAgentStackTerminalSigningTranscriptV2", "DistributedAgentStackTerminalReceiptDraftV2", "DistributedAgentStackTerminalReceiptV2", "verify_distributed_agent_stack_durable_slice_v1", "distributed_agent_stack_compatibility_digest_v1", "DistributedAgentStackPlanError"]
owner = "Distributed fixed Fabric→Agent desired-contract owner"
consumers = ["paraegox-deployment", "paraegox-runtime"]
compatibility = "PXDP v1, PXDT v1, PXTE v7 and PXAR v8 are strict additive successors over the exact PXTE v6 fixed Fabric→Agent predecessor. The only active topology configures one FabricService-owned Zenoh Session with its predecessor loopback TCP listener, one canonical non-loopback IPv4 TLS listener and one to eight RuntimeHost-sorted explicit TLS connect peers. Every peer row carries only opaque owner-resolved trust-domain, credential, trust-anchor and expected-peer-identity references; no certificate, key, Secret, route fallback, generic bus/backend, local-plus-wire double delivery or discovery authority enters desired bytes. PXTP v1 remains an unsigned desired-row-correlated observation. PXDS v1 is the distinct Runtime-signed terminal carrier: it binds the exact PXAR8 request/store/operation/slice, request-time channel and Runtime key, completion epoch/snapshot/clock, service generations, complete or partial single-session PXTP observations, and signed local binding census/readiness evidence. ActiveReady requires both service generations, an exact complete two-binding census, dependency readiness and the complete expected peer set from one Fabric session; active non-ready or indeterminate outcomes may preserve zero/partial single-session observations and never become Ready. EmptyDeactivate has the separate EmptyExactZero outcome and `try_empty_exact_zero`: endpoint lengths and proof count are zero, topology/remote-observation commitments are all-zero absent values, both service generations are absent, and the complete zero census uses only the contract-owned empty-set digest. It cannot be encoded as ActiveReady or with any PXTP. The installed binding-set digest has one contract-owned request/event role order and rejects zero/duplicate descriptor digests. The checked-in request golden fixes canonical framing and deliberately is not authentication evidence; Runtime admission must verify a freshly signed PXAR8, and Deployment must verify the PXDS signature/correlation before reporting terminal state. Additive PXRP v1 fixes one bounded non-loopback TLS locator, concrete query route, RuntimeHost/endpoint generation, distinct Controller connector and Runtime listener credential refs, common trust refs, both certificate principals and a bounded operation budget; it contains no path, key or Secret and only its digest plus an owner-resolved nonzero profile ref enter PXCB. NodeDaemon remains the Runtime endpoint-fact publisher; PXRP must correlate those facts and never replaces discovery ownership. PXCB v1 binds that exact profile commitment and endpoint/key identities; Controller-signed PXRC v1 carries an unchanged PXAR8, while Runtime-signed PXDS v2 binds the exact PXRC/PXCB and terminal receipt. The PXRP correlation helper checks the owner-resolved profile ref, target, endpoint generation, route, both principals and profile digest, but never resolves credentials or grants transport/apply authority. These contracts alone create no Controller store, Runtime lifecycle, credential resolver, TLS handshake observer, reconnect owner, two-machine proof or continuous reconciliation. Incompatible topology, identity, terminal, wire, proof-carrier or ownership semantics require an explicit successor."
tests = ["crates/paraegox-runtime-contracts/src/distributed_agent_stack_plan.rs", "crates/paraegox-runtime-contracts/tests/fixtures/distributed_agent_stack_v1.hex", "crates/paraegox-runtime/src/admission.rs", "crates/paraegox-fabric/src/runtime_apply.rs"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::remote_agent_data_plane_plan"
symbols = ["REMOTE_AGENT_DATA_PLANE_PROJECTION_VERSION", "REMOTE_AGENT_DATA_PLANE_PROFILE_VERSION", "REMOTE_AGENT_DATA_PLANE_APPLY_REQUEST_VERSION", "REMOTE_AGENT_DATA_PLANE_TARGET_EXECUTION_VERSION", "REMOTE_AGENT_DATA_PLANE_TERMINAL_RECEIPT_VERSION", "REMOTE_AGENT_DATA_PLANE_TERMINAL_SIGNING_VERSION", "REMOTE_AGENT_DATA_PLANE_PROJECTION_BYTES", "MAX_REMOTE_AGENT_DATA_PLANE_PROFILE_BYTES", "MAX_REMOTE_AGENT_DATA_PLANE_TARGET_EXECUTION_BYTES", "MAX_REMOTE_AGENT_DATA_PLANE_PLAN_SLICE_BYTES", "MAX_REMOTE_AGENT_DATA_PLANE_APPLY_REQUEST_BYTES", "MAX_REMOTE_AGENT_DATA_PLANE_TERMINAL_RECEIPT_BYTES", "MAX_REMOTE_AGENT_DATA_PLANE_TERMINAL_SIGNATURE_BYTES", "RemoteAgentDataPlaneProfileFieldsV1", "RemoteAgentDataPlaneProfileV1", "RemoteAgentDataPlaneProjectionV1", "RemoteAgentBootstrapCasV1", "RemoteAgentDataPlaneTargetModeV1", "RemoteAgentDataPlaneTargetExecutionV1", "RemoteAgentDataPlaneApplySigningTranscriptV2", "RemoteAgentDataPlaneApplyRequestDraftV1", "RemoteAgentDataPlaneApplyRequestV1", "verify_remote_agent_data_plane_durable_slice_v1", "remote_agent_data_plane_compatibility_digest_v1", "RemoteAgentDataPlaneTerminalOutcomeV1", "RemoteAgentDataPlaneTerminalLifecycleEffectV1", "RemoteAgentDataPlaneTerminalHeadV1", "RemoteAgentDataPlaneTerminalResultRefV1", "RemoteAgentDataPlaneTerminalStateV1", "RemoteAgentDataPlaneRemoteObservationV1", "RemoteAgentDataPlaneTerminalEvidenceFieldsV1", "RemoteAgentDataPlaneTerminalEvidenceV1", "RemoteAgentDataPlaneTerminalFactsV1", "RemoteAgentDataPlaneTerminalAuthClaimV1", "RemoteAgentDataPlaneTerminalSigningTranscriptV1", "RemoteAgentDataPlaneTerminalReceiptDraftV1", "RemoteAgentDataPlaneTerminalReceiptV1", "RuntimeAuthenticatedRemoteAgentDataPlaneTerminalV1", "RemoteAgentDataPlanePlanError"]
owner = "Asymmetric remote-Agent desired-state and terminal-fact contract owner"
consumers = ["paraegox-deployment", "paraegox-runtime"]
compatibility = "PXAE v1, PXAD v1, PXTE v9, PXAR v10 and PXAU v1 form one additive asymmetric remote-Agent contract over the byte-exact frozen PXTE v6 FabricAndAgent predecessor; both RemoteAccessActive and LocalAgentOnlyDeactivate retain that complete local predecessor and exact PXTA-zero, and the latter revokes only the remote listener/client ACL rather than stopping local Fabric or Agent. Active desired state carries one dual-descriptor bootstrap CAS over the exact active PXFT and PXST roots, the distinct whole-PXAH receipt digest, the shared-domain inner PXAP payload digest, and current Fabric/Agent generations. Whole-PXAH and shared PXAP digests are never interchangeable, and PXAP descriptor bytes are structural data rather than an access capability. PXAU fixes the outcome/evidence matrix: ActiveReady is MayHaveStarted plus CommittedIncoming, exact complete retained-local census two, both base services ready, listener-and-client-ACL ready, no quarantine, all three generations present, both old descriptor roots echoed, a nonzero fresh PXAP root distinct from the old payload root, and strictly advanced Fabric/Agent generations; LocalOnlyReady is MayHaveStarted plus CommittedIncoming, the same complete ready local census, RemoteAbsent, Fabric/Agent generations present, access generation absent, and all descriptor roots zero; NoEffectRejected is ProvenNotStarted with a preserved head and observation/generation evidence consistent with the preserved physical state; Uncertain and Quarantined are MayHaveStarted with Unknown or PartialOrConflicting remote observation, no fresh descriptor root, and respectively clear or set quarantine evidence. Every PXAU remains mode-, target-, store-, operation-, envelope-, request-, Slice-, assignment-, CAS- and Runtime-signer-correlated; completion epoch and snapshot plus observed selection time are nonzero, the selection clock domain equals PXAR temporal authority and its generation is not older, and ready claims require the exact retained census. PXAD endpoint_generation identifies desired endpoint configuration, while PXAU/PXRR access_generation identifies the physical remote-access owner lifecycle; they are independent generation domains with no equality or monotonic relationship. The strict bounded canonical codecs reject frozen PXAR v1-v9, old terminal wires and PXAT* tenure frames. T2-A owns only this contract/state graph: it owns no Runtime listener or ACL mutation, Mac connector or outbox, CLI or deployable activation marker, descriptor access, session, Echo, reconnect, TUI, or two-host proof. Any incompatible desired shape, CAS/digest domain, outcome/evidence row, generation, temporal, signer, census, wire or owner semantic requires an explicit successor."
tests = ["crates/paraegox-runtime-contracts/src/remote_agent_data_plane_plan.rs", "crates/paraegox-runtime-contracts/tests/remote_agent_access_contract.rs", "tests/contract/test_t2_remote_agent_access.py", "tests/fixtures/wire/t2_remote_agent_data_plane_v1.json", "tests/fixtures/wire/t2_remote_agent_access_v1.json"]

[[registry.public_apis]]
module = "paraegox_runtime_contracts::remote_agent_access"
symbols = ["REMOTE_AGENT_ACCESS_REQUEST_MAGIC", "REMOTE_AGENT_ACCESS_RESPONSE_MAGIC", "REMOTE_AGENT_ACCESS_VERSION", "REMOTE_AGENT_ACCESS_REQUEST_SIGNING_VERSION", "REMOTE_AGENT_ACCESS_RESPONSE_SIGNING_VERSION", "MAX_REMOTE_AGENT_ACCESS_DESCRIPTOR_BYTES", "MAX_REMOTE_AGENT_ACCESS_REQUEST_BYTES", "MAX_REMOTE_AGENT_ACCESS_RESPONSE_BYTES", "RemoteAgentAccessRequestIdV1", "RemoteAgentAccessKindV1", "RemoteAgentAccessRequestFieldsV1", "RemoteAgentAccessSigningTranscriptV1", "RemoteAgentAccessRequestDraftV1", "RemoteAgentAccessRequestV1", "ControllerAuthenticatedRemoteAgentAccessRequestV1", "RemoteAgentAccessResponseAuthClaimV1", "RemoteAgentAccessResponseDraftV1", "RemoteAgentAccessResponseV1", "RuntimeAuthenticatedRemoteAgentAccessResponseV1", "RemoteAgentAccessError"]
owner = "Independent remote-Agent authenticated access-carrier contract owner"
consumers = ["paraegox-deployment", "paraegox-runtime"]
compatibility = "PXRA/PXRR v1 is an independent fixed-header, length-bounded canonical carrier with exactly ApplyRemoteAccess and DescribeRemoteAccess. Apply carries one byte-identical PXAR v10, makes PXRA request_id equal the inner operation_id, binds the exact PXCB, target, Runtime store, expected host epoch, active PXST root, client and PXAD profile, and requires the outer Controller nonce to be nonzero and distinct from the inner nonce. Describe is payload-free and binds an exact expected PXAU digest plus active PXST, profile and intended Mac client roots; its PXRR returns one exactly correlated PXAD plus bounded PXAP descriptor and reports nonzero Fabric, Agent and access generations. Both data-plane principals are distinct from each other and, at Apply/Describe validation, each is distinct from the PXCB Controller and Runtime control principals. PXCB selects the exact target and Controller/Runtime principals and keys; Controller request signatures are fixed Ed25519-v1/64-byte values, Runtime response signatures use the corresponding fixed PXCB response identity, and request_id/digest, nonce, kind, carrier, target, store, epoch, PXAU/PXST/profile/client roots and payload digests are echoed exactly. Apply verification independently authenticates the inner PXAU and outer PXRR: the inner PXAU must correlate to the embedded PXAR v10, its Runtime principal must equal the PXCB Runtime principal, and its completion epoch must equal the outer expected RuntimeHost epoch; neither signature substitutes for the other. Describe verification authenticates the outer PXRR but returns only structural descriptor/profile data, never authorization, liveness, discovery freshness, access, session or capability. The PXAP digest is computed by the shared frozen PXAH payload-domain helper and remains distinct from a whole-PXAH receipt digest. Decoders fail closed on PXAG, PXAH, PXCC, PXDR, PXAU and other cross-protocol frames. T2-A supplies no Runtime listener/ACL owner, Mac connector/outbox, CLI or deployable marker, access/session/Echo path, reconnect loop, TUI, or two-host evidence. Incompatible kind, width, offset, length, nonce/request identity, carrier, profile, principal, epoch, digest, signature or correlation semantics require a new explicit version."
tests = ["crates/paraegox-runtime-contracts/src/remote_agent_access.rs", "crates/paraegox-runtime-contracts/tests/remote_agent_access_contract.rs", "tests/contract/test_t2_remote_agent_access.py", "tests/fixtures/wire/t2_remote_agent_data_plane_v1.json", "tests/fixtures/wire/t2_remote_agent_access_v1.json"]

[[registry.public_apis]]
module = "paraegox_agent_contracts"
symbols = ["AGENT_CONVERSATION_PROTOCOL_MAGIC", "AGENT_CONVERSATION_PROTOCOL_VERSION", "AGENT_CONVERSATION_HEADER_BYTES", "MAX_AGENT_CONVERSATION_INPUT_BYTES", "MAX_AGENT_CONVERSATION_OUTPUT_BYTES", "MAX_AGENT_CONVERSATION_FRAME_BYTES", "MAX_AGENT_CONVERSATION_DEADLINE_BUDGET_NANOS", "MAX_AGENT_CONVERSATION_REQUESTS", "AgentConversationDeckRunId", "AgentConversationSessionId", "AgentConversationTurnId", "AgentConversationRequestId", "AgentConversationTerminalFailureV1", "AgentConversationRequestV1", "AgentConversationTerminalResultV1", "AgentConversationTerminalV1", "AgentConversationRequestAcceptanceV1", "AgentConversationRequestRegistryV1", "AgentConversationProtocolError"]
owner = "Agent conversation protocol owner"
consumers = ["paraegox-agent-service", "paraegox-runtime managed Agent transport", "paraegox-sdk Agent worker and Textual console client"]
compatibility = "Experimental PXAC v1 fixes DeckRun/Session/Turn/Request identity correlation, request digest, bounded UTF-8 input/output, relative deadline budget, scoped request-idempotency, one canonical terminal, and the CancelledBeforeModel terminal failure. Original request/terminal kinds 1 and 2 remain byte-for-byte unchanged. Rust and Python independently reproduce fixed bytes; incompatible framing or semantics require an atomic version migration across all consumers."
tests = ["crates/paraegox-agent-contracts/tests/agent_conversation_protocol.rs", "tests/agent_worker/test_agent_conversation_protocol.py", "tests/agent_worker/test_agent_conversation_worker.py", "tests/console/test_console_client.py"]

[[registry.public_apis]]
module = "paraegox_agent_contracts::control"
symbols = ["MAX_AGENT_CONVERSATION_CONTROL_PAYLOAD_BYTES", "MAX_AGENT_CONVERSATION_WATCH_EVENTS", "MAX_AGENT_CONVERSATION_CONTROL_FRAME_BYTES", "AgentConversationOpenOutcomeV1", "AgentConversationGetStateV1", "AgentConversationCancelStateV1", "AgentConversationWatchEventKindV1", "AgentConversationWatchEventV1", "AgentConversationWatchBatchV1", "AgentConversationControlBodyV1", "AgentConversationControlV1", "AgentConversationControlError"]
owner = "Agent conversation control protocol owner"
consumers = ["paraegox-agent-service", "paraegox-runtime managed Agent transport", "paraegox-sdk Agent worker and Textual console client"]
compatibility = "Additive PXAC control kinds 3 through 10 define explicit open/get/watch/cancel request and result values without reinterpreting kinds 1/2. Frames are bounded and canonical; watch batches carry finite contiguous events, while every request/response adapter must additionally call validate_for_request with the originating cursor and limit. Get, watch, and cancel do not imply Session creation, cancellation is an intent unless the Agent owner proves model execution did not start, and incompatible kind/outcome/cursor semantics require a new explicit protocol version."
tests = ["crates/paraegox-agent-contracts/tests/agent_conversation_control.rs", "tests/agent_worker/test_agent_conversation_control.py", "tests/console/test_console_client.py"]

[[registry.public_apis]]
module = "paraegox_agent_service"
symbols = ["MAX_AGENT_SERVICE_SESSIONS", "MAX_AGENT_SERVICE_TURNS_PER_SESSION", "MAX_AGENT_SERVICE_EVENT_BATCH", "AgentServiceConfigV1", "AgentServiceError", "AgentSessionJournalError", "AgentConversationModelFuture", "AgentConversationModelCancellation", "AgentConversationModelOutcomeV1", "AgentServiceModelInvocationV1", "AgentConversationModelProvider", "AgentConversationModelServiceProviderV1", "DeterministicEchoModelProvider", "AgentSessionOpenOutcomeV1", "AgentServiceSubmitOutcomeV1", "AgentServiceAcceptOutcomeV1", "AgentServiceEventKindV1", "AgentServiceEventV1", "AgentServiceEventBatchV1", "AgentServiceTurnSnapshotV1", "AgentServiceRequestSnapshotV1", "AgentServiceSessionSnapshotV1", "AgentDeckRunSealReportV1", "AgentService"]
owner = "AgentSession and Turn semantic owner"
consumers = ["paraegox-runtime managed Agent transport and assembly", "paraegox-local Model provider composition"]
compatibility = "The experimental surface owns bounded DeckRun-scoped Session, Turn, request, terminal, event cursor, snapshot, seal, and cancellation-intent semantics. POSIX open_durable is the sole journal writer. accept_request and begin_execution separate durable admission from the separately durable model-handoff boundary; begin_execution returns one non-Clone invocation token and complete_execution consumes it to commit exactly one provider outcome. Provider futures are non-borrowing so the adapter can continue serving typed controls while a call is pending. Cancellation intent is committed before its cooperative signal becomes observable. Recovery never invokes a provider: cancel-without-handoff becomes CancelledBeforeModel, while a handed-off request without a terminal becomes ModelOutcomeUncertain. AgentConversationModelServiceProviderV1 maps one exact Agent request/cancellation view into the provider-neutral ModelService and maps the six bounded Model outcomes back without acquiring Session, journal, cancellation-intent, recovery or terminal authority. Explicit control handling never auto-opens get/watch/cancel. The deterministic provider is an offline fixture; Runtime/Fabric lifecycle, forced provider interruption, provider-specific configuration, credentials and external-service availability remain outside this API claim."
tests = ["crates/paraegox-agent-service/tests/agent_service_semantics.rs", "crates/paraegox-agent-service/tests/agent_service_control.rs", "crates/paraegox-agent-service/tests/agent_service_durable_journal.rs"]

[[registry.public_apis]]
module = "paraegox_model"
symbols = ["MAX_MODEL_INVOCATION_INPUT_BYTES", "MAX_MODEL_INVOCATION_OUTPUT_BYTES", "MAX_MODEL_INVOCATION_DEADLINE_NANOS", "MAX_MODEL_SERVICE_IN_FLIGHT", "ModelInvocationIdV1", "ModelInvocationIdErrorV1", "ModelBackendIdentityV1", "ModelBackendIdentityErrorV1", "ModelInvocationRequestV1", "ModelInvocationRequestErrorV1", "ModelCancellationSourceV1", "ModelCancellationViewV1", "ModelInvocationOutcomeV1", "ModelBackendFuture", "ModelBackendV1", "ModelAdapterIdV1", "ModelAdapterIdErrorV1", "ModelAdapterVersionV1", "ModelAdapterVersionErrorV1", "ModelCapabilityIdV1", "ModelCapabilityIdErrorV1", "BOUNDED_TEXT_MODEL_CAPABILITY_ID_V1", "ModelAdapterDescriptorV1", "ModelAdapterMetadataV1", "ModelAdapterSelectionV1", "ModelAdapterBuildErrorV1", "ModelAdapterFactoryV1", "ModelAdapterRegistryErrorV1", "ModelAdapterRegistryV1", "ModelServiceConfigV1", "ModelServiceConfigErrorV1", "ModelServiceCountersV1", "ModelServiceSnapshotV1", "ModelServiceV1"]
owner = "Provider-neutral bounded Model invocation and admission mechanism owner"
consumers = ["paraegox-agent-service Agent-to-Model adapter", "paraegox-model-adapters ModelBackend adapters", "paraegox-runtime managed Model lifecycle", "paraegox-local adapter-registry composition"]
compatibility = "This experimental in-process surface validates one owner-bound text invocation, admits it without queueing under a fixed concurrency ceiling, observes cancellation before backend handoff, calls one configured ModelBackend exactly once, bounds terminal success output, and reports coherent saturating completion/abandonment counters. Its additive exact static Adapter seam lets a composition explicitly register compiled-in configured factories under a complete nonzero descriptor containing implementation ID, version and capability ID, rejects duplicate or unknown descriptors and provider/configuration drift, and rechecks the identity of every built backend before ModelService admission; it exposes no enumeration, discovery, compatibility search, automatic selection, routing or fallback. Backends own post-admission deadline/cancellation and provider I/O: they may report CancelledBeforeHandoff before real provider I/O, but only ModelService may originate CapacityExhausted. AgentService remains the Session/Turn journal, cancellation-intent, recovery and terminal owner. A2 commits the exact provider selection and adapter descriptor in PXTE v8/PXAR v9; Runtime rechecks both through its provider-neutral resolver before starting the Runtime-owned ModelService and generation-fenced Agent dependency. This does not turn the static registry into dynamic plugin admission. This surface is not a cross-process contract, router, retry/fallback mechanism, credential owner, dynamic loader, or independent service-process claim; incompatible admission, adapter identity, handoff, outcome or accounting semantics require an explicit successor."
tests = ["crates/paraegox-model/src/lib.rs", "crates/paraegox-agent-service/tests/agent_service_semantics.rs", "crates/paraegox-model-adapters/src/tests.rs", "crates/paraegox-local/src/composition.rs"]

[[registry.public_apis]]
module = "paraegox_model_adapters"
symbols = ["OPENAI_RESPONSES_PROVIDER_CONFIG_VERSION", "OPENAI_RESPONSES_ADAPTER_ID_V1", "OPENAI_RESPONSES_ADAPTER_VERSION_V1", "OPENAI_RESPONSES_ADAPTER_CAPABILITY_ID_V1", "OPENAI_RESPONSES_ADAPTER_DESCRIPTOR_V1", "OPENAI_RESPONSES_API_ENDPOINT", "MAX_OPENAI_RESPONSES_MODEL_BYTES", "MAX_OPENAI_API_KEY_BYTES", "MAX_OPENAI_RESPONSES_REQUEST_BYTES", "MAX_OPENAI_RESPONSES_OUTPUT_TOKENS", "MAX_OPENAI_RESPONSES_HTTP_BODY_BYTES", "MAX_OPENAI_RESPONSES_TIMEOUT_NANOS", "OpenAiResponsesProviderConfigV1", "OpenAiResolvedApiKeyV1", "OpenAiResponsesProviderFactoryV1", "OpenAiResponsesModelBackendV1", "OpenAiResponsesProviderError", "DEEPSEEK_CHAT_COMPLETIONS_PROVIDER_CONFIG_VERSION", "DEEPSEEK_CHAT_COMPLETIONS_ADAPTER_ID_V1", "DEEPSEEK_CHAT_COMPLETIONS_ADAPTER_VERSION_V1", "DEEPSEEK_CHAT_COMPLETIONS_ADAPTER_CAPABILITY_ID_V1", "DEEPSEEK_CHAT_COMPLETIONS_ADAPTER_DESCRIPTOR_V1", "DEEPSEEK_API_BASE_URL", "DEEPSEEK_CHAT_COMPLETIONS_API_ENDPOINT", "MAX_DEEPSEEK_API_KEY_BYTES", "MAX_DEEPSEEK_CHAT_COMPLETIONS_REQUEST_BYTES", "MAX_DEEPSEEK_CHAT_COMPLETIONS_OUTPUT_TOKENS", "MAX_DEEPSEEK_CHAT_COMPLETIONS_HTTP_BODY_BYTES", "MAX_DEEPSEEK_CHAT_COMPLETIONS_TIMEOUT_NANOS", "DeepSeekChatModelV1", "DeepSeekChatCompletionsProviderConfigV1", "DeepSeekResolvedApiKeyV1", "DeepSeekChatCompletionsProviderFactoryV1", "DeepSeekChatCompletionsModelBackendV1", "DeepSeekChatCompletionsProviderError"]
owner = "Provisioned HTTP ModelBackend leaf-adapter owner"
consumers = ["paraegox-local adapter factory and Runtime Model-backend resolver composition"]
compatibility = "These pre-1.0 ModelBackendV1 adapters each bind one canonical configuration digest to an exact provider reference, fixed official endpoint, model and bounded limits. Resolved API-key values immediately take zeroizing ownership and expose neither getters nor Secret-bearing Debug. Repeatable factories advertise exact compiled adapter descriptors, build only immutable configurations, and never import Runtime selection types. DeveloperLocal owns byte-exact config-selected profile/provider/model/SecretRef plus committed adapter-binding validation before every resolver build; Runtime consumes only the resulting provider-neutral backend and owns ModelService lifecycle. Arbitrary production endpoints, environment proxy selection, redirects, retries, provider fallback and discovery are absent. OpenAI sends one bounded Responses request with store=false and aggregates typed output_text. DeepSeek sends one bounded, non-streaming Chat Completions request with thinking disabled and accepts one exact terminal text choice. Both leaves own no selection, ModelService, Agent semantics, plugin manifest, activation or lifecycle. Unavoidable HTTP/TLS transient copies are marked sensitive and are not claimed zeroized. Test-only loopback endpoints remain private. Incompatible adapter identity, digest fields, endpoint, model set, request shape, Secret lifetime, retry, cancellation or uncertainty semantics require an explicit successor."
tests = ["crates/paraegox-model-adapters/src/tests.rs", "crates/paraegox-local/src/composition.rs"]

[[registry.public_apis]]
module = "paraegox_fabric"
symbols = ["REQUEST_RESPONSE_ENVELOPE_VERSION", "PORT_BINDING_DESCRIPTOR_VERSION", "PORT_BINDING_DESCRIPTOR_HEADER_BYTES", "MAX_PORT_BINDING_DESCRIPTOR_BYTES", "PortBindingDescriptorV1", "PortBindingDescriptorError", "BindingEpoch", "RequestId", "ResponseStatus", "BindingRequestEnvelopeV1", "BindingResponseEnvelopeV1", "FabricContractError", "IngressLimits", "IngressLimitError", "FabricIngressSnapshot", "SessionEndpoint", "RemoteTlsEndpoint", "ResolvedRemoteMtlsIdentityFiles", "ResolvedRemoteMtlsCredentialFiles", "ResolvedRemoteMtlsListenerCredentialFilesV1", "ResolvedRemoteMtlsConnectorCredentialFilesV1", "ExperimentalPeerCommonNameV1", "ExperimentalRemoteMtlsConfigErrorV1", "ExperimentalRemoteMtlsPeerBindingV1", "ExperimentalRemoteMtlsPeerLinkObservationV1", "ExperimentalRemoteMtlsLinkSnapshotV1", "ExperimentalRemoteMtlsObservationErrorV1", "FabricServiceConfig", "RequestResponseBindingSpec", "PortBinding", "ClientPortBindingV1", "InstalledBinding", "HandlerResponse", "InboundRequest", "RequestReceiver", "FabricService", "FabricConfigError", "FabricError", "RestrictedRuntimeApplyClientConfigV1", "RestrictedRuntimeApplyClientV1", "RestrictedRuntimeApplyConfigErrorV1", "RestrictedRuntimeApplyEndpointConfigV1", "RestrictedRuntimeApplyEndpointV1", "RestrictedRuntimeApplyErrorV1", "RestrictedRuntimeApplyInboundV1", "RestrictedRuntimeApplyPreflightV1", "RestrictedRuntimeApplyReceiverV1", "RestrictedRuntimeApplyRespondErrorV1", "RestrictedRuntimeControlClientConfigV1", "RestrictedRuntimeControlClientV1", "RestrictedRuntimeControlConfigErrorV1", "RestrictedRuntimeControlEndpointConfigV1", "RestrictedRuntimeControlEndpointV1", "RestrictedRuntimeControlErrorV1", "RestrictedRuntimeControlInboundV1", "RestrictedRuntimeControlPreflightV1", "RestrictedRuntimeControlReceiverV1", "RestrictedRuntimeControlRespondErrorV1", "RestrictedNodeControlTransportPinsV1", "RestrictedNodeControlClientConfigV1", "RestrictedNodeControlClientV1", "RestrictedNodeControlConfigErrorV1", "RestrictedNodeControlEndpointConfigV1", "RestrictedNodeControlEndpointV1", "RestrictedNodeControlErrorV1", "RestrictedNodeControlInboundV1", "RestrictedNodeControlPreflightV1", "RestrictedNodeControlReceiverV1", "RestrictedNodeControlRespondErrorV1", "restricted_runtime_apply_peer_certificate_common_name_v1"]
owner = "FabricService Zenoh session and binding owner"
consumers = ["paraegox-fabric host-local TCP integration harness", "paraegox-runtime managed Agent and restricted Runtime-control endpoints", "paraegox-deployment restricted Runtime-apply and Runtime Agent-control clients", "paraegox-local TLS credential validation and Node-control composition"]
compatibility = "The experimental owner-private v1 surface keeps Zenoh types and every raw session private while exposing explicit loopback TCP or exact secured-hybrid TCP/TLS configuration, canonical request/reply envelopes, bounded ingress, exact epoch replacement and no application-request retry. The secured profile requires one loopback listener, one canonical non-loopback TLS listener, one to eight unique TLS connectors, separate listen/connect certificate and private-key paths, one CA path, mTLS, hostname/IP verification, expiration closure, disabled scouting and no protocol fallback. The restricted Runtime-apply profile instead owns one Controller connector-only session or one Runtime listener-only session for one exact route, binds its application-message ACL to the deterministic certificate Common Name of the expected peer principal, limits unicast sessions/links, wire reassembly, ingress queues and reply collection, and applies one absolute operation deadline. Zenoh 1.9 may maintain or reconnect a live link and its connector trust store is not custom-CA-exclusive; the owner must therefore verify the outer pinned Controller PXRC or Runtime PXDS v2 signature, and the Controller must retain both preflight tokens across its single durable pair claim before invoking either consuming send. Before preflight or durable claim, each Controller client also matches the exact RuntimeHost, route, Runtime principal and complete PXCB binding digest, so clients for different endpoint generations, keys or transport profiles cannot be interchanged. Paths are bounded normalized absolute UTF-8 process-local values whose Debug output is redacted; they are neither desired state nor Secret bytes. Only successful install creates PortBinding lifecycle/CAS tokens; PXBD decode creates the nominally distinct ClientPortBindingV1 request route, and only request_client_v1 accepts it. Lifecycle and ingress-observation methods continue to require PortBinding. PXBD v1 fixes BindingId/epoch, concrete route, request/response schemas, item capacity no greater than 4096, byte capacities within existing envelope limits, exact header/length/flags/reserved fields, and digest. All legal wire capacities fit 32-bit and 64-bit usize representations identically. The additive `FabricServiceConfig::try_experimental_secured_hybrid_peer_with_cn_bindings` and `FabricService::observe_experimental_remote_mtls_links` seam is pinned to exact Zenoh 1.9.0 with its unstable API and reads only the owning FabricService's private live Session. Observation inspects the hard maximum of 64 links plus one overflow sentinel and fails `TooManyLiveLinks` on a 65th; TCP links are ignored, while TLS links must expose the first certificate Common Name and exactly complete the configured expected CN set with no missing, additional, unknown or absent CN. One CN mapping to multiple ZIDs and multiple CNs mapping to one ZID fail closed; duplicate links for the same CN/ZID select the lexicographically smallest source/destination locator pair. Before opening the Session, start obtains exactly one CSPRNG sample and accepts only a nonzero `DistributedFabricSessionEpochV1`; entropy failure or zero fails closed without retry. The epoch remains fixed for the owning FabricService across Session reconnects and is sampled again on another service start, but is not durable. Successful rows remain in configuration order and disclose binding digest, one owner-issued unique nonzero peer sequence, remote ZID and source/destination locators but no raw CN; the snapshot exposes the final peer sequence as the atomically advanced service-local high-water. Failed classification or sequence exhaustion does not advance that high-water. Zenoh exposes only the first certificate CN, connector roots may include WebPKI roots, and listener/connector role certificates currently require the same exact CN with no alias or rotation support. This snapshot is not production certificate identity or authorization, PXTP, Evidence append/readback, ActiveReady, Agent start/bindings/handle, continuous health, authenticated descriptor distribution, production restricted-endpoint composition, SAN/fingerprint proof, or two-host proof. Configuration, a live TLS link and an unkeyed digest grant no independent authentication, authorization, discovery or lifecycle right. The envelopes and restricted carrier are not yet a stable polyglot ABI; route, framing, bounds, status, descriptor, credential-role, certificate-principal mapping or correlation changes require an explicit successor before independent production consumers adopt them. Additive RestrictedRuntimeControl* types reuse that private raw-query implementation with distinct PXCC request and maximum PXDR/PXFR/PXQS response bounds, so neither the frozen PXRC/PXDS-v2 path nor its preflight type can be substituted. On Unix, RestrictedNodeControl* types use separate PXNR and maximum PXNE/PXNS/PXNA bounds plus an exact route/config digest and peer-certificate-principal pin; their listener and connector configurations consume paraegox-node only for canonical protocol bounds. Every control inbound remains unauthenticated raw bytes until its Runtime, Node or Deployment consumer performs strict decode, Controller/Runtime verification and correlation. Fabric never signs, decodes, admits, persists or retries these protocol values, and each restricted connector/listener lifecycle owns only its role-scoped query session rather than becoming a second FabricService, general Bus, discovery owner or reconnect policy. T2-B adds role-nominal, process-local and Debug-redacted `ResolvedRemoteMtlsListenerCredentialFilesV1` and `ResolvedRemoteMtlsConnectorCredentialFilesV1`; the listener value cannot carry a connector private key and the connector value cannot carry a listener private key. `FabricServiceConfig::try_remote_agent_listener_v1` requires one canonical loopback TCP endpoint, one canonical non-loopback IPv4 TLS endpoint, a nonzero expected Mac Agent data-plane principal and exactly two distinct bounded routes; the resulting peer-mode sole Session has those two listeners and zero connectors. `FabricServiceConfig::try_remote_agent_connector_v1` requires one canonical non-loopback IPv4 TLS endpoint, a nonzero expected Ubuntu Agent-listener data-plane principal and the same route shape; the resulting client-mode sole Session has zero listeners and exactly one TLS connector. Each constructor derives the expected certificate Common Name from its peer principal and emits a default-deny subject pinned to that exact Common Name plus `link_protocols = [\"tls\"]`. Listener ingress admits only query and egress only reply plus declare_queryable on the two exact routes; connector flows are the inverse. No put, delete, subscriber, liveliness or wildcard permission appears in the emitted ACL. Scouting, admin space and plugin loading remain off; pending sessions, sessions and links are each capped at one; the transport receive-message ceiling is fixed; TLS name and expiry checks remain enabled. Same-session local self-query succeeds only through the exact pinned Zenoh 1.9.0 local-dispatch behavior and does not authorize an independent plaintext loopback peer. The Linux real-network test proves correct-CN submit/control queries, same-session local submit/control, denied sentinel/parent/child routes, denied submit/control from a same-CA wrong-CN session after that session opens, denied submit/control from an independent plaintext loopback session, joined shutdown and both-port reuse. It does not claim that wrong-CN is disconnected during authentication: because a same-CA wrong-CN peer can open while the session limit is one, this profile makes no denial-of-service or availability guarantee. Wildcard `**`, dollar-prefixed, control-byte, non-ASCII and noncanonical routes are rejected statically before Session open; `**` was not sent over the public real-network harness. Put/delete/subscriber/liveliness absence is configuration evidence, not a dynamic observer claim. Sharing this validator intentionally tightens the existing experimental `RequestResponseBindingSpec::try_new` route contract to bounded ASCII, control-free, canonical `OwnedNonWildKeyExpr` values with no `*` or `$`; formerly tolerated non-ASCII or noncanonical values now fail `NonConcreteKeyExpression` with no fallback. These constructors consume neither PXCB nor PXAR v8, expose no control-plane carrier or CLI, and establish no deployed remote-Agent activation, descriptor access, conversation, Echo, reconnect ownership, partition recovery, TUI or two-host claim. Endpoint roles, credential roles, route canonicalization, ACL flows/messages/subject pins, session hardening or same-session behavior may change only through an explicit successor and fresh exact-Zenoh evidence."
tests = ["crates/paraegox-fabric/src/contract.rs", "crates/paraegox-fabric/src/ingress.rs", "crates/paraegox-fabric/src/port_descriptor.rs", "crates/paraegox-fabric/src/runtime_apply.rs", "crates/paraegox-fabric/src/service.rs", "crates/paraegox-fabric/tests/fixtures/port_binding_descriptor_v1.hex", "crates/paraegox-fabric/tests/host_local.rs", "crates/paraegox-fabric/tests/remote_agent_mtls.rs", "crates/paraegox-deployment/src/distributed_agent_stack_node_reconcile.rs", "crates/paraegox-local/src/composition.rs"]

[[registry.public_apis]]
module = "paraegox_fabric"
symbols = ["RestrictedRuntimeControlClientConfigV1", "RestrictedRuntimeControlClientV1", "RestrictedRuntimeControlConfigErrorV1", "RestrictedRuntimeControlEndpointConfigV1", "RestrictedRuntimeControlEndpointV1", "RestrictedRuntimeControlErrorV1", "RestrictedRuntimeControlInboundV1", "RestrictedRuntimeControlPreflightV1", "RestrictedRuntimeControlReceiverV1", "RestrictedRuntimeControlRespondErrorV1"]
owner = "Fabric restricted raw Runtime-control transport owner"
consumers = ["paraegox-runtime restricted Runtime-control endpoint", "paraegox-deployment restricted Runtime Describe and Agent-control clients"]
compatibility = "RestrictedRuntimeControl* reuses the one private raw-query transport implementation but has its own type boundary and exact maximum PXCC/PXAG request plus PXDR/PXFR/PXQS/PXAH response sizes. One connector is pinned to one target, route, Runtime principal and PXCB digest. Preflight yields a move-only authority whose consuming send performs at most one bounded query under one absolute deadline; Deployment alone decides when its durable state permits that send. The listener hands one bounded still-unauthenticated request to Runtime and consumes one bounded raw response. Fabric never selects a carrier kind, decodes or verifies PXAG/PXAH, signs, admits, persists, retries, replays or decides Uncertain/terminal state. Connector and listener own only their role-scoped query sessions, not Runtime/Deployment protocol state, FabricService, discovery or reconnect policy. Incompatible target pins, frame bounds, one-shot authority or raw-byte ownership requires an explicit successor."
tests = ["crates/paraegox-fabric/src/runtime_apply.rs", "crates/paraegox-runtime/src/runtime_control_endpoint.rs", "crates/paraegox-deployment/src/managed_serving_client.rs", "crates/paraegox-deployment/src/deployment_process.rs", "tests/system/test_t1_public_agent_bootstrap_cli.py"]

[[registry.public_apis]]
module = "paraegox_inspection"
symbols = ["LOCAL_INSPECTION_SNAPSHOT_VERSION", "LOCAL_INSPECTION_OWNER_COUNT", "LOCAL_INSPECTION_SNAPSHOT_BYTES", "LOCAL_INSPECTION_SNAPSHOT_V2_VERSION", "LOCAL_INSPECTION_SNAPSHOT_V2_BYTES", "InspectionSourceOwnerV1", "InspectionObservationClockRefV1", "InspectionSourceCoordinateV1", "InspectionSourceAvailabilityV1", "InspectionLivenessV1", "InspectionReadinessV1", "InspectionHealthV1", "InspectionFeatureSupportV1", "InspectionReasonV1", "InspectionFreshnessV1", "LocalInspectionOverallV1", "OwnerInspectionFactFieldsV1", "OwnerInspectionFactV1", "InspectionSourceSlotV1", "LocalInspectionProjectionInputV1", "LocalInspectionRecordV1", "LocalInspectionSnapshotV1", "LocalInspectionServiceV1", "project_local_inspection_snapshot_v1", "NodeInspectionFactFieldsV2", "NodeInspectionFactV2", "NodeInspectionSourceSlotV2", "LocalInspectionProjectionInputV2", "NodeInspectionRecordV2", "LocalInspectionSnapshotV2", "LocalInspectionServiceV2", "project_local_inspection_snapshot_v2", "InspectionContractError"]
owner = "Node-local Inspection projection owner"
consumers = ["paraegox-inspection contract integration tests", "paraegox-local", "paraegox-sdk Textual one-shot startup client"]
compatibility = "Experimental PXIS v1 remains one fixed-length canonical snapshot of exactly five owner slots in fixed Authority/DeploymentController/RuntimeHost/FabricService/AgentService order. Owner-specific tenure, revision, RuntimeHost epoch, and service-generation coordinates cannot alias. Observation and valid-until times are compared only under the exact local Inspection clock reference. Timeout produces Stale; only an explicit source/transport fact produces Partitioned; Missing, Stale, and Partitioned mask liveness/readiness/health/feature values to Unknown and can never aggregate Ready. PXIS v2 is an explicit fixed-length successor that embeds the complete canonical PXIS-v1 frame unchanged and appends one public-safe NodeDaemon record correlated by Node/incarnation, registration epoch, status sequence, local observation/validity and authenticated NodeStatus digest. Its conservative aggregate includes the Node record without changing v1 bytes or owner semantics. Neither version exposes private endpoint, route, key or capability material, owns source/action state, or accepts noncanonical bytes. Incompatible v1 bytes, v2 composition, record layout, time semantics, aggregate rules, or ownership changes require another explicit successor."
tests = ["crates/paraegox-inspection/tests/local_inspection_contract.rs", "crates/paraegox-inspection/tests/fixtures/local_inspection_snapshot_v1.hex", "crates/paraegox-local/src/inspection.rs", "tests/console/test_console_client.py", "tests/console/test_console_tui.py"]

[[registry.public_apis]]
module = "paraegox_inspection::protocol"
symbols = ["INSPECTION_PROTOCOL_VERSION", "INSPECTION_REQUEST_BYTES", "MAX_INSPECTION_RESPONSE_BYTES", "InspectionRequestKindV1", "InspectionRequestV1", "InspectionResponseOutcomeV1", "InspectionResponseV1", "InspectionEndpointErrorV1", "InspectionEndpointV1", "InspectionClientErrorV1", "InspectionClientV1", "INSPECTION_PROTOCOL_V2_VERSION", "INSPECTION_REQUEST_V2_BYTES", "MAX_INSPECTION_RESPONSE_V2_BYTES", "InspectionRequestKindV2", "InspectionRequestV2", "InspectionResponseOutcomeV2", "InspectionResponseV2", "InspectionEndpointErrorV2", "InspectionEndpointV2", "InspectionClientErrorV2", "InspectionClientV2", "InspectionProtocolError"]
owner = "Node-local Inspection read-protocol owner"
consumers = ["paraegox-inspection protocol integration tests", "paraegox-local", "paraegox-sdk Textual one-shot startup client"]
compatibility = "PXIQ/PXIP v1 remains a bounded canonical one-shot read of the last immutable PXIS-v1 cache. The separate PXIQ/PXIP v2 successor carries only PXIS-v2 and cannot be selected by interpreting a v1 frame. In each generation, Latest returns the current snapshot when present; Watch(after_revision) returns Snapshot only for a newer revision and otherwise NotModified; a missing cache or different projection is NotFound. Snapshot embeds one complete strict matching-version PXIS frame, and every response binds request id, projection id, kind, cursor and complete request digest. Each typed client performs one exchange with no retry, stream, heartbeat, history, discovery, authentication, transport policy, write, or projection revision mutation. Incompatible request shape, version, outcome, cursor, correlation, framing or cache semantics require an explicit successor."
tests = ["crates/paraegox-inspection/tests/inspection_protocol.rs", "crates/paraegox-inspection/tests/fixtures/inspection_latest_request_v1.hex", "crates/paraegox-inspection/tests/fixtures/inspection_watch_request_v1.hex", "crates/paraegox-inspection/tests/fixtures/inspection_snapshot_response_v1.hex", "crates/paraegox-inspection/tests/fixtures/inspection_not_modified_response_v1.hex", "crates/paraegox-inspection/tests/fixtures/inspection_not_found_response_v1.hex", "crates/paraegox-local/src/inspection.rs", "tests/console/test_console_client.py", "tests/console/test_console_tui.py"]

[[registry.public_apis]]
module = "paraegox_inspection::adapter"
symbols = ["InspectionSourceAdapterV1", "InspectionSourceAdapterReadErrorV1", "read_inspection_source_slot_once_v1", "LocalInspectionProjectionInputBuilderErrorV1", "LocalInspectionProjectionInputBuilderV1", "NodeInspectionSourceAdapterV2", "NodeInspectionSourceAdapterReadErrorV2", "read_node_inspection_source_slot_once_v2"]
owner = "Node-local Inspection source-adapter boundary owner"
consumers = ["paraegox-inspection adapter integration tests", "paraegox-local"]
compatibility = "The v1 owner-neutral seam reads at most one already-verified optional fact, preserves adapter errors instead of converting them to Missing, treats only explicit None as Missing, and rejects facts that do not correlate to the adapter's exact declared owner, subject and observation clock. Its builder accepts arbitrary insertion order but requires exactly one slot for each of the five fixed owners and emits canonical owner order through the existing strict projection input constructor. The separate Node v2 seam applies the same error-preserving single-read rule and additionally requires exact Node/incarnation and observation-clock correlation for one already-authenticated and fenced NodeStatus projection. Neither seam owns I/O, retry, discovery, authentication, fact synthesis, projection, cache or revision mutation. Incompatible error mapping, fact correlation, owner set or build ordering requires an explicit successor."
tests = ["crates/paraegox-inspection/tests/inspection_adapter.rs", "crates/paraegox-local/src/inspection.rs"]

[[registry.public_apis]]
module = "paraegox_inspection::developer_local"
symbols = ["DEVELOPER_LOCAL_INSPECTION_BOOTSTRAP_VERSION", "DEVELOPER_LOCAL_INSPECTION_BOOTSTRAP_HEADER_BYTES", "MAX_DEVELOPER_LOCAL_INSPECTION_BOOTSTRAP_BYTES", "DEVELOPER_LOCAL_INSPECTION_REQUEST_BYTES", "DeveloperLocalInspectionBootstrapV1", "encode_authenticated_request_v1", "decode_authenticated_request_v1", "DeveloperLocalInspectionTransportErrorV1", "DEVELOPER_LOCAL_INSPECTION_BOOTSTRAP_V2_VERSION", "DEVELOPER_LOCAL_INSPECTION_BOOTSTRAP_V2_HEADER_BYTES", "MAX_DEVELOPER_LOCAL_INSPECTION_BOOTSTRAP_V2_BYTES", "DEVELOPER_LOCAL_INSPECTION_REQUEST_V2_BYTES", "DeveloperLocalInspectionBootstrapV2", "encode_authenticated_request_v2", "decode_authenticated_request_v2", "DeveloperLocalInspectionTransportErrorV2"]
owner = "DeveloperLocal Inspection bootstrap and authenticated request-envelope owner"
consumers = ["paraegox-local", "paraegox-sdk Textual one-shot startup client"]
compatibility = "PXIB v1 strictly binds one absolute bounded Unix socket path, nonzero projection identity, private generation token, server uid/gid, finite operation timeout and private request-id seed under a canonical digest. PXIB v2 is a separate successor bound to PXIQ/PXIP v2; protocol selection is explicit and cannot be inferred from an otherwise identical path or capability. In both generations token and seed storage plus encoded private bytes are zeroizing and redacted from Debug, and the fixed outer request is exactly one constant-time-checked generation token followed by one strict canonical matching-version PXIQ frame. This module does not bind a socket, check peer credentials, project owner facts, retry, discover, federate, mutate state or grant operational authority; those lifecycle responsibilities remain with the executable producer and typed consumer. Framing, version, token, path, timeout, identity, digest or correlation changes require an explicit successor."
tests = ["crates/paraegox-inspection/src/developer_local.rs", "crates/paraegox-local/src/inspection.rs", "tests/console/test_console_client.py", "tests/console/test_console_tui.py"]

[[registry.public_apis]]
module = "paraegox_node"
symbols = ["MAX_RUNTIME_HOSTS_PER_NODE", "MAX_NODE_STATUS_FRESHNESS_NANOS", "NODE_MANAGEMENT_PROTOCOL_VERSION", "NodeId", "NodeIncarnation", "EnrollmentIssuerRefV1", "NodeManagementEndpointRefV1", "RuntimeApplyEndpointRefV1", "NodeIdentityV1", "NodeSpecV1", "NodeOperatingSystemV1", "NodeArchitectureV1", "NodeFeatureReportV1", "NodeFeatureReportInputV1", "RuntimeApplyTransportV1", "RuntimeApplyEndpointDescriptorV1", "RuntimeHostLivenessV1", "RuntimeHostStatusV1", "NodeRegistrationTenureV1", "NodeStatusV1", "NodeStatusInputV1", "NodeDaemonV1", "RuntimeHostObservationV1", "NodeStatusObservationV1", "NodeStatusTrackerV1", "NodeContractError"]
owner = "NodeDaemon observed-fact and Runtime endpoint-discovery contract owner"
consumers = ["paraegox-node contract and process adapters", "paraegox-deployment one-shot remote Node reconciliation", "paraegox-local Node and Inspection composition"]
compatibility = "The experimental v1 surface keeps stable enrollment NodeId, NodeDaemon publication incarnation/registration epoch, Deployment-owned NodeSpec, RuntimeHost epoch/status sequence, endpoint generation, and feature-report sequence as distinct coordinates. One tenure retains immutable fences for at most eight RuntimeHost identities; hiding a host does not erase its fence, older Runtime status or endpoint generations cannot overwrite newer facts, same-generation endpoint rewrites fail closed, and the management endpoint rotates across Node incarnations. Generic NodeStatus keeps its bounded relative freshness; an observation-backed NodeStatus additionally exposes a digest-bound absolute Unix-nanosecond valid-until fence that consumers must enforce without substituting a monotonic observation clock. Runtime apply discovery is a bounded ASCII paraegox/.../apply route only and remains read-only metadata: NodeDaemon cannot construct, admit, proxy, or mutate RuntimeApplyRequest or Deployment desired state. Incompatible owner, identity, fencing, route, bound, validity-clock, or digest semantics require an explicit successor."
tests = ["crates/paraegox-node/src/lib.rs", "crates/paraegox-node/src/process.rs", "crates/paraegox-node/tests/node_daemon_process.rs", "crates/paraegox-deployment/src/distributed_agent_stack_node_reconcile.rs", "crates/paraegox-local/src/composition.rs", "crates/paraegox-local/src/inspection.rs"]

[[registry.public_apis]]
module = "paraegox_evidence"
symbols = ["EVIDENCE_RECORD_VERSION", "EVIDENCE_RECORD_HEADER_BYTES", "MAX_EVIDENCE_INLINE_PAYLOAD_BYTES", "MAX_EVIDENCE_RECORD_BYTES", "EvidenceRecordIdV1", "EvidenceOwnerRefV1", "EvidenceCausalityRefV1", "EvidenceStoreEpochV1", "EvidenceKindV1", "EvidenceRedactionV1", "EvidencePayloadV1", "EvidenceRecordInputV1", "EvidenceRecordV1", "EvidenceRefV1", "EvidenceCommitReceiptV1", "EvidenceContractError", "MAX_EVIDENCE_QUERY_RECORDS", "MAX_EVIDENCE_STORE_RECORDS", "MAX_EVIDENCE_STORE_BYTES", "EvidenceRetentionPolicyV1", "EvidenceStoredRecordV1", "EvidenceAppendOutcomeV1", "EvidenceListCursorV1", "EvidenceListPageV1", "EvidenceStoreError", "LocalEvidenceStoreV1"]
owner = "Local Evidence durable-handoff owner"
consumers = ["paraegox-runtime PXAR8 transport-fact handoff", "future local Inspection/Evidence integration"]
compatibility = "Experimental PXEV v1 is one bounded canonical owner-issued record. PublicSafeInline means the caller has already classified the copied bytes as safe; DigestOnly persists no source bytes and requires a nonzero caller-issued digest. The record digest binds record/owner ids, nonzero producer sequence, optional causality and exact previous Evidence ids, kind, redaction and payload digest. PXES/PXEF v1 is a same-user Unix local append store with one advisory writer, nonzero caller-supplied epoch, immutable record/byte bounds, exact replay, conflicting-id rejection, strict owner chains, fsync-before-ack, bounded epoch-scoped cursors, incomplete-tail recovery and complete-corruption rejection. StorageFull preserves all records. Once append I/O begins, failure is CommitUncertain and poisons reads and writes until reopen; reopen either truncates an incomplete tail or discovers the complete record for exact replay. The first real consumer is the Runtime-owned PXAR8 path: it binds one composition-pinned store epoch/owner, hands off one canonical PXTP RuntimeFact per peer through durable intent, append receipt and exact readback, and requires that verified batch before its separate lifecycle owner may start Agent. That consumer rule does not make the store an effect oracle. This surface neither proves the represented effect nor provides replication, cross-Node/global order, Ops control, service transport, producer authentication, automatic redaction, eviction/compaction, or production filesystem support. Incompatible wire, digest, chain, retention, cursor, durability or uncertainty semantics require an explicit successor and migration."
tests = ["crates/paraegox-evidence/src/lib.rs", "crates/paraegox-evidence/src/store.rs"]

[[registry.public_apis]]
module = "paraegox_node::protocol"
symbols = ["NODE_MANAGEMENT_REQUEST_BYTES", "MAX_NODE_MANAGEMENT_RESPONSE_BYTES", "NODE_CONTROL_CARRIER_VERSION", "NODE_CONTROL_CARRIER_SIGNING_VERSION", "MAX_NODE_CONTROL_CARRIER_REQUEST_BYTES", "MAX_NODE_CONTROL_DESCRIBE_RESPONSE_BYTES", "NodeManagementTargetV1", "NodeStatusCursorV1", "NodeManagementRequestKindV1", "NodeManagementRequestV1", "NodeManagementResponseOutcomeV1", "NodeManagementResponseV1", "NodeManagementEndpointErrorV1", "NodeManagementEndpointV1", "NodeManagementClientErrorV1", "NodeManagementClientV1", "NodeControlCarrierKindV1", "NodeControlCarrierSigningTranscriptV1", "NodeControlCarrierRequestDraftV1", "NodeControlCarrierRequestV1", "ControllerAuthenticatedNodeControlCarrierV1", "NodeControlDescribeResponseKindV1", "NodeControlObservationChallengeV1", "NodeControlObservationChallengeFieldsV1", "NodeControlDescribeResponseDraftV1", "NodeControlDescribeResponseV1", "NodeManagementProtocolError"]
owner = "NodeDaemon read-only NodeManagement protocol owner"
consumers = ["paraegox-node protocol and process adapters", "paraegox-deployment one-shot remote Node adapter", "paraegox-fabric restricted Node-control transport", "paraegox-local Node control composition"]
compatibility = "PXNQ/PXNS v1 remains one bounded transport-neutral Latest or one-shot Watch exchange for one exact NodeId, management endpoint, incarnation and registration epoch. Its outcomes bind complete request correlation; the typed client never retries, and observation-backed status retains its absolute Unix-nanosecond validity fence in addition to relative freshness. Additive PXNR/PXNE v1 places only five typed operations under a Controller signature: target-free Describe; exact-target Latest or Watch carrying one byte-identical PXNQ; ObservationChallenge for one Runtime and bounded freshness; or PublishRuntimeObservation carrying one byte-identical PXNO. The target discovered by Describe is mandatory and byte-exact for every later operation. PXNE is a digest/nonce-correlated Describe or challenge response valid only inside the same independently pinned mTLS one-shot exchange; it is deliberately unsigned and grants no authority by itself. The Node-local challenge carries the exact PXOB-derived query nonce, authority digest, proposed next status sequence and finite issuance/expiry window but no PXOB token. Fabric consumes only platform-bounded raw-frame sizes; decoding, Controller verification, target fencing and mutation remain outside Fabric. These protocols provide no registration acquisition, retry/reconnect loop, heartbeat, partition inference, apply proxy or desired-state authority. Incompatible framing, kind, target, challenge, authentication, correlation, bounds, validity or replay semantics require an explicit successor."
tests = ["crates/paraegox-node/src/protocol.rs", "crates/paraegox-node/tests/node_management_protocol.rs", "crates/paraegox-node/tests/node_daemon_process.rs", "crates/paraegox-node/tests/fixtures/node_management_latest_request_v1.hex", "crates/paraegox-node/tests/fixtures/node_management_watch_request_v1.hex", "crates/paraegox-node/tests/fixtures/node_management_status_response_v1.hex", "crates/paraegox-node/tests/fixtures/node_management_not_modified_response_v1.hex", "crates/paraegox-node/tests/fixtures/node_management_not_found_response_v1.hex", "crates/paraegox-node/tests/fixtures/node_management_fenced_response_v1.hex", "crates/paraegox-node/tests/fixtures/node_management_cursor_conflict_response_v1.hex", "crates/paraegox-deployment/src/distributed_agent_stack_node_reconcile.rs", "crates/paraegox-fabric/src/runtime_apply.rs", "crates/paraegox-local/src/composition.rs"]

[[registry.public_apis]]
module = "paraegox_node::store"
symbols = ["NodeDaemonStoreError", "DurableNodeDaemonV1"]
owner = "NodeDaemon same-tenure durable-state owner"
consumers = ["paraegox-node process adapters", "paraegox-local reference Node bootstrap composition"]
compatibility = "PXND v2 is an additive owner-private Unix snapshot of one already-authorized NodeDaemon registration tenure. It retains the v1 current feature report, every RuntimeHost monotonic fence including hidden hosts, current visibility, next publication sequence and last immutable NodeStatus, and adds an optional exact last accepted PXNO request digest/Runtime subject plus sorted per-Runtime absolute valid-until records and their aggregate minimum needed to distinguish exact replay and suppress stale publication, all under one canonical integrity digest. The strict reader continues accepting canonical v1 only with its formerly reserved extension bytes zero; it never fabricates replay provenance or absolute validity, and the next real mutation atomically advances that state to v2. One observation can renew only its own Runtime deadline; each publication removes expired/untracked hosts from visibility while preserving their monotonic fences and uses the earliest retained per-Runtime deadline as the complete NodeStatus absolute fence. When the aggregate observation-backed PXNS expires, the owner atomically commits its next node-only publication under the same advisory writer lock, hides every currently visible Runtime, clears the exact PXNO/PXNA replay record and all Runtime validity records, and retains every RuntimeHost epoch/sequence and endpoint-generation fence. Consequently, exact historical PXNA recovery is available only until any successor publication commits. The non-Clone owner enforces one advisory writer, owner-private no-follow files, atomic replace plus file/directory synchronization, commit-before-visible mutation, uncertain-commit poisoning and exact identity/registration/incarnation/management-endpoint recovery pins. The initial feature input is creation-only; durable advanced reports win on recovery. Authentication remains owned by the process adapter; the store persists the accepted digest and per-Runtime validity. Injected-clock unit evidence covers the expiry boundary deterministically; the process harness covers real same-state Node restart, while a preserved genuinely expired state supplies separate manual evidence rather than a claim that the system harness waits the full 60-second maximum. This surface does not acquire or renew registration, generate identity/incarnation, infer readiness, serve a network, forward Runtime apply, mutate Deployment desired state, execute restart or claim ProductionReference filesystem support. Incompatible snapshot, recovery pin, validity, durability or mutation semantics require an explicit successor and migration."
tests = ["crates/paraegox-node/src/store.rs", "crates/paraegox-node/src/process.rs", "crates/paraegox-local/src/composition.rs", "tests/system/test_g2_public_deployment_cli.py"]

[[registry.public_apis]]
module = "paraegox_node::process"
symbols = ["DEVELOPER_LOCAL_REFERENCE_TOKEN_BYTES", "MAX_DEVELOPER_LOCAL_NODE_MANAGEMENT_EXCHANGE_TIMEOUT", "NodeDaemonProcessError", "DeveloperLocalReferenceBootstrapInputV1", "DeveloperLocalReferenceBootstrapV1", "DeveloperLocalNodeManagementEndpointErrorV1", "DeveloperLocalNodeManagementEndpointV1", "DeveloperLocalNodeControlBridgeErrorV1", "DeveloperLocalNodeControlBridgeV1", "serve_developer_local_reference_node_daemon_v1", "serve_developer_local_runtime_observation_node_daemon_v1"]
owner = "DeveloperLocal/reference NodeDaemon process substrate owner"
consumers = ["paraegox-noded DeveloperLocal commands", "paraegox-local NodeDaemon and authenticated remote-control composition"]
compatibility = "PXNB v1 remains the owner-private bounded Unix bootstrap for one externally supplied exact Node identity/tenure and the unchanged read-only PXNQ/PXNS socket. Its strict loader and typed DeveloperLocal endpoint pin private file/socket identity, peer uid/gid, token and one finite no-retry exchange. The additive runtime-observation command retains the separate PXOB/PXOL capability, sole child-held PXND writer, complete PXQS verification, atomic observe-plus-publish transaction, absolute-validity fence and exact lost-PXNA replay semantics. DeveloperLocalNodeControlBridgeV1 consumes only an already Controller-authenticated PXNR marker: it answers Describe from the exact PXNB/PXOB pair, sends Latest/Watch once through the existing PXNL endpoint, and sends ObservationChallenge/PublishRuntimeObservation once through the existing PXOL endpoint. It opens no second store, owns no retry/cache/background worker, and reaches the same child-held durable mutation owner for PXNO. The bridge exposes neither PXNB nor PXOB tokens. Neither command, endpoint nor bridge acquires registration, opens Zenoh, accepts PXAR/desired state, proxies apply, owns Runtime lifecycle/restart or proves multi-host operation. Incompatible PXNB framing/read semantics retain v1; incompatible PXOB/PXNO/PXNA/PXNR bridge or mutation ownership requires an explicit successor."
tests = ["crates/paraegox-node/src/process.rs", "crates/paraegox-node/src/bin/paraegox-noded.rs", "crates/paraegox-node/tests/node_daemon_process.rs", "crates/paraegox-local/src/composition.rs", "crates/paraegox-local/src/main.rs"]

[[registry.public_apis]]
module = "paraegox_node::observation"
symbols = ["RUNTIME_OBSERVATION_PROTOCOL_VERSION", "RUNTIME_OBSERVATION_TOKEN_BYTES", "MAX_RUNTIME_OBSERVATION_CHALLENGE_NANOS", "MAX_RUNTIME_OBSERVATION_REQUEST_BYTES", "RUNTIME_OBSERVATION_REQUEST_HEADER_BYTES", "RUNTIME_OBSERVATION_ACK_BYTES", "MAX_RUNTIME_OBSERVATION_BOOTSTRAP_BYTES", "RuntimeObservationEndpointRefV1", "RuntimeObservationAuthorityV1", "RuntimeObservationBootstrapInputV1", "RuntimeObservationBootstrapV1", "RuntimeObservationRequestInputV1", "RuntimeObservationRequestV1", "RuntimeObservationAckOutcomeV1", "RuntimeObservationAckV1", "derive_runtime_observation_query_nonce_v1", "RuntimeObservationError"]
owner = "NodeDaemon authenticated Runtime observation adapter owner"
consumers = ["paraegox-noded developer-local-runtime-observation-v1 CLI", "paraegox-node process bridge", "paraegox-deployment one-shot remote observation adapter", "paraegox-local Node schema v2/v3 composition"]
compatibility = "PXOB v1 pins one exact Node tenure/mutation endpoint and one to eight sorted Runtime authorities. Each authority fixes RuntimeHostId, Runtime principal, non-weak Ed25519 response key/ref, exact channel binding, authenticated bootstrap serving baseline and the complete immutable RuntimeApplyEndpointDescriptor including generation. PXNO carries one strict PXQR/PXQS pair, an exact authority digest, bounded status freshness, bounded nonzero issuance/expiry timestamps and proposed next PXNS sequence. Its PXQR nonce is domain-separated over the private observation token, exact Node tenure, observation endpoint ref, authority digest, exact endpoint descriptor, proposed sequence and time window; Node rejects not-yet-valid and expired challenges for every new publication and calculates the remaining relative budget under the durable owner lock. The authenticated absolute deadline is also carried in the PXNS status digest and PXND; all consumers must enforce it, and multi-Runtime publication uses the earliest retained deadline so one Runtime cannot renew another. The same-user token holder is the trusted DeveloperLocal query producer; this wall-clock window is not a substitute for Node-issued unpredictable challenges in a future production/federated transport. Node verifies the Runtime PXQS signature before inspecting/projecting facts, then validates request/response/channel/store/epoch/snapshot/clock correlation; a successful current challenge proves only Runtime responsiveness and never imports PXQS readiness. PXND persists the last accepted request digest so only that exact request may receive ExactReplay after recovery; this historical ACK recovery intentionally bypasses expiry but neither republishes nor renews the status. PXNA binds the source request digest and resulting durable PXNS/status digests, but never claims PXQR/PXQS source bytes were persisted. Endpoint rotation, different signer/channel/store, protocol framing or observation semantics require a successor or new bootstrap; caller-supplied endpoint generation is never accepted."
tests = ["crates/paraegox-node/src/observation.rs", "crates/paraegox-node/src/process.rs", "crates/paraegox-node/tests/node_daemon_process.rs", "crates/paraegox-deployment/src/distributed_agent_stack_node_reconcile.rs", "crates/paraegox-local/src/composition.rs"]

[[registry.public_apis]]
module = "paraegox-local CLI"
symbols = ["chat --config <absolute TOML>", "DeveloperLocal config schema v1", "node --config <absolute TOML>", "DeveloperLocal node config schema v1", "DeveloperLocal node config schema v2"]
owner = "DeveloperLocal composition root"
consumers = ["ParaEGOX developer-local operator"]
compatibility = "The sole public conversation grammar is exact `chat --config <absolute TOML>`; provider/model names are configuration values, never CLI subcommands. Config schema v1 strictly owns `schema_version`, `state_root`, `fabric_listen` and one `model` table. Unknown or duplicate fields fail decode; only schema version 1 and explicitly compiled profiles are accepted. deterministic-echo-v1 prohibits model/SecretRef. openai-responses-v1 requires a bounded model plus exact `env:OPENAI_API_KEY`; deepseek-chat-completions-v1 requires an admitted exact model plus `env:DEEPSEEK_API_KEY`. Secret values never enter config or argv, and no CLI flag or arbitrary environment variable overrides config. The absolute config path must name a regular non-symlink file no larger than 64 KiB. Local resolves only the selected SecretRef before state creation, pins the exact profile/provider/config/adapter identity, and fails closed if another profile or model owns the state root. All profiles use one backend resolver and ModelAdapterRegistryV1 path with no deterministic bypass, discovery or fallback. The command starts the real Authority, Deployment, Runtime-owned Fabric/Model/Agent, NodeDaemon, owner-private Inspection and Agent IPC chain, then launches the internal `paraegox-console` Textual child with only absolute bootstrap-file arguments and inherited terminal streams. The child environment excludes both admitted API-key variables; no alternate frontend or transport fallback exists. The Textual child consumes typed Runtime Agent IPC plus a separate strict PXIB/PXIQ/PXIP/PXIS v2 Inspection client that performs exactly one no-retry Latest read before App startup. A failed Inspection read prevents UI startup; a successful read renders three read-only startup-status lines with no Watch, retry, cache, background refresh, action, continuous monitoring, Ops, or federation. `paraegox-console` is internal packaging and never a second public command. Normal exit preserves Active desired state for same-root recovery. This config/CLI contract does not establish credentialed external availability, production support, distributed readiness, dynamic plugin admission, provider routing, Ops, federated Inspection or a public distributed command. Incompatible grammar, config field/meaning, provider profile or SecretRef authority requires an explicit schema/command successor rather than an implicit override. The separate exact public Node grammar is `node --config <absolute TOML>` with no identity, model or provider override flags. Node schema v1 retains the G1 split-trust Runtime plus feature-only NodeDaemon slice and generic restricted-apply rejection behavior. Schema v2 retains every v1 field and strictly requires `[node_control]`; it derives rather than accepts the route/config carrier digest, uses a separate PXNI v2 with only Runtime/PXNB/PXOB local secrets, and starts Runtime G2 control, PXOB-backed Node child and the authenticated remote PXNR listener before printing Ready. On same-state schema-v2 restart, Local reopens the sole PXND writer and, before child startup, atomically replaces an expired observation-backed aggregate PXNS with the next node-only publication; every visible Runtime is hidden, replay/validity metadata is cleared and all monotonic Runtime fences survive. An exact old PXNA can therefore be recovered only if no successor publication has committed. The Linux exact-prebuilt-binary process harness uses root only for fixture preparation before dropping ParaEGOX to a non-root uid/gid, and covers Node Ready, clean Node SIGTERM, same-state Node Ready after restart, Deployment Resume Ready and clean shutdown. Expiry is covered by injected-clock unit evidence plus separate manual evidence from a genuinely expired preserved state, not by claiming a full 60-second wait in the harness. Both schemas reject unknown fields, inline secrets, unsafe path/role aliasing, root execution and same-root schema/config drift. Schema v2 adds no Authority, Deployment, managed Fabric, Model, Agent, Inspection or Textual owner; the harness does not prove Authority-failure handling, distinct service-account or two-host execution, remote Agent conversation/TUI, reconnect or distributed readiness. Incompatible Node fields, identity wire, endpoint role or lifecycle semantics require an explicit schema successor."
tests = ["configs/paraegox-node.example.toml", "crates/paraegox-local/src/config.rs", "crates/paraegox-local/src/identity.rs", "crates/paraegox-local/src/layout.rs", "crates/paraegox-local/src/inspection.rs", "crates/paraegox-local/src/composition.rs", "crates/paraegox-local/src/main.rs", "tests/system/test_g2_public_deployment_cli.py"]

[[registry.public_apis]]
module = "paraegox-local CLI"
symbols = ["deployment --config <absolute TOML>", "DeveloperLocal deployment config schema v1"]
owner = "DeveloperLocal single-target DeploymentController composition root"
consumers = ["ParaEGOX developer-local operator"]
compatibility = "The exact public grammar is `deployment --config <absolute TOML>` with one argument and no Controller submode or identity/route override. Schema v1 strictly owns `schema_version`, `state_root`, `enrollment_artifact_file`, one nonzero lower-case `enrollment_artifact_sha256`, two signing-seed file paths, disjoint Authority state/socket paths, and exact `[runtime_connector]` and `[node_connector]` CA/certificate/private-key path tables; unknown, duplicate, relative, aliased, overlapping or additional values fail closed. Inline seed, private-key, certificate, PXNB/PXOB token, request nonce and bearer-secret bytes are not representable. The current non-root peer and both signal handlers are established before configured-path resolution or state mutation. Resolution revalidates owner, mode, no-follow identity, link count and cross-role inode separation, reads seeds into zeroizing values, and verifies the independently supplied whole-file PXEA SHA-256 before canonical parsing, Runtime-response signature and Controller/Authority cross-pins. Local then converts PXEA only into the registered Deployment enrollment facts and combines its endpoint/trust/principal/route facts with the separately gated connector files; neither config nor Deployment re-derives or overrides those artifact facts. Any existing Controller or PXFS entry selects explicit Resume and requires existing Authority state; empty owner directories select Fresh, while malformed or mixed durable state fails in the facade without deletion. Local prints and flushes exactly `paraegox: deployment ready` only for a live-owner facade Ready. ReconcileRequired, premature Authority exit, output failure and joined-shutdown failure are stable nonzero outcomes; SIGINT/SIGTERM trigger one joined owner shutdown. The macOS artifact job proves only locked build, exact help grammar and hidden-command rejection for this command. This registration does not claim a deployment execution smoke, real two-host proof, remote Agent conversation, remote TUI, reconnect, continuous reconciliation or production platform support. Incompatible grammar, field authority, PXEA ingress, Fresh/Resume, readiness or shutdown behavior requires an explicit successor."
tests = ["configs/paraegox-deployment.example.toml", "crates/paraegox-local/src/config.rs", "crates/paraegox-local/src/identity.rs", "crates/paraegox-local/src/layout.rs", "crates/paraegox-local/src/composition.rs", "crates/paraegox-local/src/error.rs", "crates/paraegox-local/src/main.rs", ".github/workflows/macos-cli-artifact.yml"]

[[registry.public_apis]]
module = "paraegox-local CLI"
symbols = ["node --config <absolute TOML>", "DeveloperLocal node config schema v3"]
owner = "DeveloperLocal Node Agent-bootstrap enrollment composition root"
consumers = ["ParaEGOX developer-local operator", "paraegox-local public deployment schema v2 composition through PXEA v2"]
compatibility = "Node schema v3 is an explicit additive successor for the unchanged exact `node --config <absolute TOML>` grammar. It retains every schema-v2 field and requires exactly `[managed_agent_bootstrap] provider = \"deterministic-echo-v1\"`; unknown providers, fields, missing schema sections and same-root schema/config drift fail closed. Local derives the public provider reference from the exact target, schema-v3 config commitment and fixed deterministic-provider configuration digest. Separate PXNI v3 preserves the PXNI-v2 private shape and version-separates its checksum domain: it contains only the Runtime response seed, PXNB/PXOB tokens, six stable local identities and complete config commitment, never Controller/Authority signing seeds, desired Fabric/Agent service identities, endpoint/limit choices or TLS private-key bytes. Node schema v3 starts the same Runtime, NodeDaemon, observation and Node-control owners as v2 and installs only the exact deterministic Agent-provider resolver; it does not activate an Agent. It publishes strict PXEA v2 as `enrollment-v2.pxea`, adding only the public-safe exact provider selection to the signed and Controller/Authority-cross-pinned predecessor facts. The whole-file digest remains an independent Deployment ingress pin. Node Ready and joined shutdown semantics remain unchanged. This schema does not start Deployment, Fabric, Agent, conversation, Echo or TUI ownership and adds no remote Agent data plane, T2, reconnect or distributed-readiness claim. Incompatible schema, provider derivation, PXNI/PXEA bytes, ownership or readiness semantics require another explicit successor."
tests = ["configs/paraegox-node.agent-bootstrap.example.toml", "crates/paraegox-local/src/config.rs", "crates/paraegox-local/src/identity.rs", "crates/paraegox-local/src/composition.rs", "crates/paraegox-local/src/main.rs", "tests/fixtures/wire/t1_node_enrollment_artifact_v2.json", "tests/system/test_t1_public_agent_bootstrap_cli.py"]

[[registry.public_apis]]
module = "paraegox-local CLI"
symbols = ["deployment --config <absolute TOML>", "DeveloperLocal deployment config schema v2", "paraegox: deployment agent bootstrap ready"]
owner = "DeveloperLocal single-target DeploymentController Agent-bootstrap composition root"
consumers = ["ParaEGOX developer-local operator"]
compatibility = "Deployment schema v2 is an explicit additive successor for the unchanged exact `deployment --config <absolute TOML>` grammar. It retains every schema-v1 field and requires exactly one `[managed_agent_bootstrap]` table with distinct nonzero `fabric_service_id` and `agent_service_id`, one strict loopback `fabric_listen`, and `limits_profile = \"developer-agent-bootstrap-v1\"`; unknown fields, profile drift, unsafe paths and aliases fail closed before owner mutation. Local still verifies the complete PXEA file SHA-256 before strict PXEA-v2 decode, Runtime signature and Controller/Authority cross-pins, takes the provider only from that artifact, combines it with the Deployment-owned desired service/listener fields, and calls only the registered Agent-bootstrap facade. Fresh and explicit same-state Resume preserve the predecessor Authority, connector, one-writer store and joined-shutdown rules. Local prints and flushes exactly `paraegox: deployment agent bootstrap ready` only for the typed Agent-bootstrap Ready variant while its Authority owner is live; schema-v1 keeps its distinct predecessor marker unchanged. ReconcileRequired, Node unavailability, owner death, output failure and cleanup failure produce nonzero status and no Agent-bootstrap marker. The Linux exact-ref system harness validates Node-v3/PXEA-v2 fresh readiness, whole-PXEA-SHA rejection without fresh mutation, Deployment-v2 fresh readiness and joined SIGTERM, same-state Node and terminal Deployment resume, Node-down failure/Authority-socket cleanup and secret-free logs. It has no independent provider-unavailable/mismatch scenario and proves only bootstrap descriptor receipt, not descriptor access, authorization, session, conversation, Echo, remote Agent data plane, T2, reconnect, TUI, two-host or distributed readiness. Incompatible schema, field authority, marker, PXEA ingress, Fresh/Resume, readiness or shutdown semantics require an explicit successor."
tests = ["configs/paraegox-deployment.agent-bootstrap.example.toml", "crates/paraegox-local/src/config.rs", "crates/paraegox-local/src/identity.rs", "crates/paraegox-local/src/composition.rs", "crates/paraegox-local/src/error.rs", "crates/paraegox-local/src/main.rs", "tests/fixtures/wire/t1_node_enrollment_artifact_v2.json", "tests/system/test_t1_public_agent_bootstrap_cli.py"]

[[registry.public_apis]]
module = "paraegox_runtime::host_watchdog"
symbols = ["HOST_WATCHDOG_PROTOCOL_MAGIC", "HOST_WATCHDOG_PROTOCOL_VERSION", "HOST_WATCHDOG_FRAME_BYTES", "HOST_WATCHDOG_ENABLE_ENV", "HOST_WATCHDOG_GENERATION_ENV", "HOST_WATCHDOG_HEARTBEAT_MILLIS_ENV", "HOST_WATCHDOG_HANDSHAKE_MILLIS_ENV", "HostWatchdogGeneration", "HostWatchdogSequence", "HostControlProbeNonce", "HostBootstrapPhase", "HostWatchdogDirection", "HostWatchdogFrameBody", "HostWatchdogFrame", "HostWatchdogProtocolErrorCode", "HostWatchdogProtocolError"]
owner = "RuntimeHost watchdog contract owner"
consumers = ["paraegox-runtime", "paraegox-runtime-host"]
compatibility = "PXHW v1 fixes one strict 40-byte big-endian frame with magic/version, nonzero exact child generation, direction-local nonzero sequence, ordered bootstrap progress, running heartbeat, exact control probe/ack nonce, zero reserved bytes, and stable decode reason codes. The inherited stream binds the adapter to its spawned child; these frames are external reactor-progress observations only and cannot claim Card readiness, ProcessDomain terminal state, effect outcome, or a recovery receipt. Incompatible framing or semantics require a new explicit protocol version."
tests = ["crates/paraegox-runtime/src/host_watchdog.rs", "crates/paraegox-runtime-host/tests/external_watchdog.rs"]

[[registry.public_apis]]
module = "paraegox_runtime"
symbols = ["RuntimeAgentConversationHandle", "RuntimeAgentConversationError", "run_runtime_host_process", "RuntimeHostProcessError", "run_runtime_host_entrypoint", "RuntimeHostEntrypointError"]
owner = "RuntimeHost process and release/install/control/migration entrypoint owner"
consumers = ["paraegox-runtime-host", "paraegox-local"]
compatibility = "This pre-1.0 surface owns one current-thread RuntimeHost reactor plus the strict final-executable command dispatcher. The additive opaque RuntimeAgentConversationHandle is a cloneable per-client lease over the current Runtime-owned stack: its direct transport lease fences Fabric and Agent generations, while the A2 owner releases it only after exact PXMT ActiveReady and revokes it when the Fabric/Model/Agent stack retires. It exposes only typed open/submit/get/watch/cancel and local lease close. Runtime privately owns the sole two-lane PXAP-over-Fabric adapter used by that handle; its port, descriptor, Fabric errors and lifecycle tokens are not public Runtime API, and transport failures cross the public boundary only as OperationRejected. No raw Fabric, ModelService, route, port, AgentService, journal or provider credential crosses the boundary. No arguments preserves existing RuntimeHost process behavior. Exact versioned release/install/control/migration commands remain fail closed; unknown, missing, additional or non-canonical arguments cannot select a fallback. Owner-private endpoint/store and assembly types remain private. Incompatible handle lifetime, generation fencing, request retry or executable command semantics require an explicit successor."
tests = ["crates/paraegox-runtime/src/managed_agent_transport/tests.rs", "crates/paraegox-runtime/src/managed_agent_transport/port_descriptor.rs", "crates/paraegox-runtime/tests/fixtures/agent_conversation_port_descriptor_v1.hex", "crates/paraegox-runtime/src/managed_agent_runtime.rs", "crates/paraegox-runtime/src/managed_model_runtime.rs", "crates/paraegox-runtime/src/managed_model_agent_stack_runtime.rs", "crates/paraegox-runtime/src/managed_fabric_runtime.rs", "crates/paraegox-runtime/src/runtime_host.rs", "crates/paraegox-runtime/src/runtime_host_entrypoint.rs", "crates/paraegox-runtime-host/tests/external_watchdog.rs", "tests/system/test_s7_runtime_install_process.py"]

[[registry.public_apis]]
module = "paraegox_runtime"
symbols = ["RuntimeFabricCredentialRequirementV1", "RuntimeFabricCredentialResolveError", "RuntimeFabricCredentialResolveErrorV2", "RuntimeFabricCredentialResolverV1", "RuntimeFabricCredentialResolverV2", "RuntimeResolvedFabricCredentialFilesV1", "RuntimeResolvedFabricPeerCredentialV2"]
owner = "Runtime distributed-Fabric credential-resolution boundary owner"
consumers = ["paraegox-runtime PXAR8 distributed Agent-stack owner"]
compatibility = "This experimental Unix-only seam presents one complete non-secret PXDT peer requirement to a repeatable composition-owned resolver: exact peer RuntimeHost, connect endpoint, mTLS profile, trust-domain, local-credential, trust-anchor and expected-peer-identity references plus the contract-owned requirement digest. V1 resolution returns only already validated process-local mTLS file paths and must echo that complete digest; Debug redacts the resolved path value. Runtime rejects digest mismatch and cross-peer credential-file drift before consuming one exact secured-hybrid configuration through the existing single FabricService/session owner. The additive V2 path accepts the same RuntimeFabricCredentialRequirementV1 input but invokes only RuntimeFabricCredentialResolverV2 and never calls or falls back to V1. Each result must echo the exact requirement digest and expected peer identity reference, bind that reference to one bounded canonical lower-case DNS-style CN, and return the common exact resolved file set; Runtime validates the complete canonical PXDT peer set, unique CNs, both echoes, file equality, and the recomputed `paraegox.runtime.distributed-fabric-peer-cn-binding.sha256.experimental.v2` digest over requirement digest, exact expected-peer-identity-ref bytes and exact canonical-CN bytes. Binding digests are retained in canonical PXDT peer order to build the experimental Fabric CN-binding configuration and to validate the later lifecycle-owned live snapshot in that same order; valid result Debug output redacts both CN and paths. V2 changes no PXDT or PXTP wire and grants no V1 fallback. The resolver seam itself neither reads a Secret store nor establishes production certificate identity or authorization. The actual PXAR8 owner accepts one live snapshot only after exact Fabric-generation/control-handle correlation, a typed nonzero Fabric-owned session epoch, and complete peer binding count/order validation. It durably appends and reads back the canonical PXTP/PXEV batch before starting the existing Agent assembly, verifies exactly two installed bindings and their digest, commits signed ActiveReady before handle publication, and uses fresh generations/session/PXTP/Evidence rather than old observations after a crash. Agent shutdown remains ordered before Fabric and fails closed if binding retirement is uncertain. This creates no credential rotation or application retry and is not production SAN/fingerprint proof, a real two-host mTLS system proof, continuous health/reconciliation, a public distributed CLI/TUI composition, or an Evidence service/replication claim. Incompatible requirement fields, resolver trust, credential lifetime, CN canonicalization, binding digest, or authorization semantics require an explicit successor."
tests = ["crates/paraegox-runtime/src/distributed_fabric_runtime.rs", "crates/paraegox-runtime/src/distributed_agent_stack_runtime.rs", "crates/paraegox-runtime/src/managed_fabric_runtime.rs", "crates/paraegox-fabric/src/service.rs"]

[[registry.public_apis]]
module = "paraegox_runtime"
symbols = ["RuntimeAgentProviderResolveError", "RuntimeResolvedAgentProviderV1", "RuntimeAgentProviderResolverV1", "RuntimeModelBackendResolveError", "RuntimeResolvedModelBackendV1", "RuntimeModelBackendResolverV1", "RuntimeDeveloperLocalIdentityRefsV1", "RuntimeDeveloperLocalSigningSeedsV1", "RuntimeDeveloperLocalIdentityV1", "RuntimeDeveloperLocalDistributedAgentStackConfigV1", "RuntimeDeveloperLocalConfigV1", "RuntimeDeveloperLocalReadyV1", "RuntimeDeveloperLocalLifecycleV1", "RuntimeDeveloperLocalError", "start_runtime_developer_local_v1"]
owner = "Runtime-owned DeveloperLocal lifecycle facade"
consumers = ["paraegox-local"]
compatibility = "This experimental Unix-only facade strictly observes the current executable, derives its build identity, initializes or reopens one owner-private Runtime store and publishes one authenticated UDS lifecycle. RuntimeDeveloperLocalIdentityV1 accepts Controller-request and tenure verification keys plus only the Runtime-owned response seed; paraegox-local does not pass Controller or Authority private signing material into Runtime. The original three-seed constructor remains pre-1.0 compatibility only. RuntimeDeveloperLocalDistributedAgentStackConfigV1 carries the complete bounded distributed successor dependencies without exposing their state owners. RuntimeDeveloperLocalConfigV1 admits exactly one all-or-nothing restricted endpoint selection: the G1 PXRC/PXDS-v2 apply transport or the additive G2 PXCC/PXDR control transport, each mapped from one canonical PXRP/profile/PXCB and normalized role-specific mTLS files; a second endpoint cannot replace the first. The G2 listener starts before Ready and authenticates every exact PXCC against the pinned Controller and PXCB. In LegacyReady it answers Describe, serves the nested byte-identical PXQR, and lets only a valid nested PXFB perform the durable one-way cutover on the same listener; in ManagedReady it answers Describe, handles the nested PXFB through the managed owner, and rejects PXQR so the frozen predecessor query never gains successor meaning. RuntimeDeveloperLocalReadyV1 exposes the exact RuntimeControlDescribeReadyFactsV1 only after independently rechecking its local ReferenceChannelBinding, target, store, manifest and build pins; this remains a Runtime-local UDS fact, not a TLS binding. Existing PXAR6/PXAR7/PXAR8/PXAR9, provider-neutral resolver, Model-before-Agent, generation fencing, handle issuance and joined shutdown semantics remain unchanged. Ready proves only the configured transports and local recovered owners started; it is not distributed Agent ActiveReady, a two-machine proof, a remote TUI, reconnect policy or ProductionReference support. Incompatible identity, endpoint selection, control phase, cutover, Ready fact, resolver, handle or shutdown semantics require an explicit successor."
tests = ["crates/paraegox-runtime/src/runtime_agent_provider.rs", "crates/paraegox-runtime/src/managed_agent_stack_runtime.rs", "crates/paraegox-runtime/src/managed_model_runtime.rs", "crates/paraegox-runtime/src/managed_model_agent_stack_state.rs", "crates/paraegox-runtime/src/managed_model_agent_stack_runtime.rs", "crates/paraegox-runtime/src/runtime_developer_local.rs", "crates/paraegox-runtime/src/runtime_control_endpoint.rs", "crates/paraegox-local/src/composition.rs"]

[[registry.public_apis]]
module = "paraegox_runtime"
symbols = ["RuntimeAgentDeveloperLocalBootstrapV1", "RuntimeAgentDeveloperLocalConversationV1", "RuntimeAgentDeveloperLocalIpcClientV1", "RuntimeAgentDeveloperLocalIpcConfigV1", "RuntimeAgentDeveloperLocalIpcError", "RuntimeAgentDeveloperLocalIpcLifecycleV1", "RuntimeAgentDeveloperLocalIpcLimitsV1", "RuntimeAgentDeveloperLocalIpcPathsV1", "start_runtime_agent_developer_local_ipc_v1"]
owner = "Runtime-owned DeveloperLocal Agent IPC capability owner"
consumers = ["paraegox-sdk Textual console client", "paraegox-local"]
compatibility = "This Unix-only PXAI/PXAB v1 surface converts one live generation-fenced Runtime Agent handle into a bounded same-user private IPC capability. Runtime creates the socket and mode-0600 bootstrap file, supplies an opaque zeroizing token and exact DeckRun/Session scope, checks peer uid/gid and token before dispatch, rejects forged cross-scope identifiers, bounds concurrent connections/commands/frame sizes/deadlines, and joins endpoint cleanup before Runtime shutdown. The client performs explicit open/submit/get/watch/cancel operations without retry and creates its own request nonce from OS entropy. Before bind, restart recovery rejects a connectable active owner and removes only a connection-refused mode-0600 socket with the exact expected uid/gid and stable device/inode/mode identity, either as socket-only residue or paired with a canonical mode-0600, single-link PXAB whose socket path and server uid/gid match. Bootstrap-only residue, symlink, ownership/type/permission/link-count drift, non-canonical bootstrap bytes or named-identity drift fail closed; removal rechecks each exact named identity and syncs the parent directory. Bootstrap bytes, token and endpoint are DeveloperLocal private capability material, not a public remote protocol, Fabric descriptor, Secret store, desired-state path, lifecycle right, production endpoint or distributed-readiness proof."
tests = ["crates/paraegox-runtime/src/runtime_agent_developer_local_ipc.rs", "crates/paraegox-local/src/composition.rs", "tests/console/test_console_client.py"]

[[registry.public_apis]]
module = "paraegox-runtime-host CLI"
symbols = ["release-descriptor-v1", "install-v1", "serve-bootstrap-v1", "migrate-journal-v3-to-v4-v1", "migrate-journal-v4-to-v5-v1"]
owner = "Final RuntimeHost release/install/control/migration command owner"
consumers = ["Runtime release pipeline", "system installer/operator", "DeploymentController"]
compatibility = "The five versioned commands use exact positional lower-hex and absolute-path grammars with no optional or additional arguments. No arguments continues to start the existing RuntimeHost process; unknown or malformed commands fail before mutation. `release-descriptor-v1` publishes the descriptor for the exact final executable; `install-v1` independently observes that executable, verifies the supplied descriptor digest, creates the singleton manifest, and initializes sequence one; `serve-bootstrap-v1` reopens the exact store and provisioning pins, completes bounded fixed-profile Loop/Empty restart reassembly before listener publication, and then serves the authenticated local PXBR/PXQR/PXAR endpoint. The frozen `migrate-journal-v3-to-v4-v1` and new `migrate-journal-v4-to-v5-v1` each take an explicit stopped store, separate evidence directory, expected store/target identities, and migration ID, then emit a canonical PXMR receipt only after their exact same-lock offline migration; the latter accepts only states with no prepared or recovery action. The ProductionReference profile requires Linux GNU ext4 state plus a root:root mode-0555 executable and distinct non-root Runtime, Controller, and Authority identity evidence; unsupported platforms fail closed. Query does not imply Controller reconciliation, fixed-profile reassembly does not imply general Thread/Process recovery, and migration is neither rollback nor recovery. Incompatible grammar or authority changes require a new command version."
tests = ["crates/paraegox-runtime-host/src/main.rs", "crates/paraegox-runtime/src/runtime_host_entrypoint.rs", "crates/paraegox-runtime/src/runtime_control_endpoint.rs", "tests/system/test_s7_runtime_install_process.py"]

[[registry.public_apis]]
module = "paraegox_runtime_host::service_manager"
symbols = ["HostWatchdogTiming", "HostTerminationTiming", "HostRestartPolicy", "RuntimeHostServiceManagerPolicy", "RuntimeHostLaunch", "ServiceManagerFailure", "ServiceManagerEvidenceKind", "ServiceManagerEvidence", "RuntimeHostServiceManagerState", "RuntimeHostServiceManagerSnapshot", "RuntimeHostServiceManager", "ServiceManagerError"]
owner = "External RuntimeHost lifecycle owner"
consumers = ["POSIX OS service-manager profiles", "external RuntimeHost system harness"]
compatibility = "This internal P2d reference adapter owns one exact RuntimeHost process group and the only restart-window/backoff/quarantine mutation ledger. The restart window counts actual replacement spawn attempts rather than pending schedules. Reaped/restart requires both direct-child reap and exact PGID absence, including TERM-ignoring same-group descendants; every OS-created child consumes a unique generation, and post-spawn setup cleanup failure retains the sole process owner and quarantines without replacement. The reference executable keeps that quarantine in a signal-responsive owner loop, with synchronous Drop retained only as the final fail-safe. Its fixed-capacity evidence records external observations and process actions, never Runtime/ProcessDomain receipts. The proof does not cover ProcessDomain workers deliberately launched into distinct process groups if RuntimeHost is uncatchably killed; production orphan cleanup needs a shared cgroup/job object or durable external ownership journal. It intentionally exposes no NodeDaemon/Node facts, Deployment input, arbitrary shell/environment launch, or second recovery-action owner; wider platform service-manager integration may replace this POSIX reference behind equivalent contract evidence."
tests = ["crates/paraegox-runtime-host/src/service_manager.rs", "crates/paraegox-runtime-host/tests/external_watchdog.rs"]

[cargo]
manifest_path = "Cargo.toml"
lockfile_path = "Cargo.lock"
crate_root = "crates"

[[cargo.rules]]
id = "CARGO-ARCH-001"
source = "paraegox-kernel"
allow_workspace_dependencies = []
allow_external_dependencies = ["sha2"]

[[cargo.rules]]
id = "CARGO-ARCH-002"
source = "paraegox-runtime-contracts"
allow_workspace_dependencies = ["paraegox-kernel"]
allow_external_dependencies = []

[[cargo.rules]]
id = "CARGO-ARCH-003"
source = "paraegox-runtime"
allow_workspace_dependencies = ["paraegox-agent-contracts", "paraegox-agent-service", "paraegox-evidence", "paraegox-fabric", "paraegox-kernel", "paraegox-model", "paraegox-runtime-contracts"]
allow_external_dependencies = ["ed25519-dalek", "getrandom", "nix", "sha2", "tokio", "zeroize"]

[[cargo.rules]]
id = "CARGO-ARCH-004"
source = "paraegox-deployment"
allow_workspace_dependencies = ["paraegox-fabric", "paraegox-kernel", "paraegox-node", "paraegox-runtime-contracts"]
allow_external_dependencies = ["ed25519-dalek", "nix", "tokio", "zeroize"]

[[cargo.rules]]
id = "CARGO-ARCH-005"
source = "paraegox-runtime-host"
allow_workspace_dependencies = ["paraegox-runtime"]
allow_external_dependencies = ["getrandom", "nix", "tokio"]

[[cargo.rules]]
id = "CARGO-ARCH-006"
source = "paraegox-agent-contracts"
allow_workspace_dependencies = ["paraegox-kernel"]
allow_external_dependencies = []

[[cargo.rules]]
id = "CARGO-ARCH-007"
source = "paraegox-agent-service"
allow_workspace_dependencies = ["paraegox-agent-contracts", "paraegox-kernel", "paraegox-model"]
allow_external_dependencies = ["nix", "sha2"]

[[cargo.rules]]
id = "CARGO-ARCH-016"
source = "paraegox-model"
allow_workspace_dependencies = ["paraegox-kernel"]
allow_external_dependencies = []

[[cargo.rules]]
id = "CARGO-ARCH-008"
source = "paraegox-fabric"
allow_workspace_dependencies = ["paraegox-kernel", "paraegox-node", "paraegox-runtime-contracts"]
allow_external_dependencies = ["getrandom", "serde_json", "tokio", "zenoh"]

[[cargo.rules]]
id = "CARGO-ARCH-011"
source = "paraegox-local"
allow_workspace_dependencies = ["paraegox-agent-contracts", "paraegox-agent-service", "paraegox-deployment", "paraegox-evidence", "paraegox-fabric", "paraegox-inspection", "paraegox-kernel", "paraegox-model", "paraegox-model-adapters", "paraegox-node", "paraegox-runtime", "paraegox-runtime-contracts"]
allow_external_dependencies = ["ed25519-dalek", "getrandom", "nix", "rustix", "serde", "serde_json", "sha2", "tokio", "toml", "zeroize"]

[[cargo.rules]]
id = "CARGO-ARCH-012"
source = "paraegox-model-adapters"
allow_workspace_dependencies = ["paraegox-kernel", "paraegox-model"]
allow_external_dependencies = ["reqwest", "serde_json", "tokio", "zeroize"]

[[cargo.rules]]
id = "CARGO-ARCH-013"
source = "paraegox-inspection"
allow_workspace_dependencies = ["paraegox-kernel"]
allow_external_dependencies = ["zeroize"]

[[cargo.rules]]
id = "CARGO-ARCH-014"
source = "paraegox-node"
allow_workspace_dependencies = ["paraegox-kernel", "paraegox-runtime-contracts"]
allow_external_dependencies = ["ed25519-dalek", "nix", "tokio", "zeroize"]

[[cargo.rules]]
id = "CARGO-ARCH-015"
source = "paraegox-evidence"
allow_workspace_dependencies = ["paraegox-kernel"]
allow_external_dependencies = ["nix"]

[[architecture.rules]]
id = "ARCH-001"
source = "paraegox.kernel"
forbid = [
  "paraegox.decks",
  "paraegox.deployment",
  "paraegox.drivers",
  "paraegox.gateways",
  "paraegox.runtime",
  "paraegox.services",
]
reason = "Kernel mechanisms must not depend on product, deployment, runtime, gateway, driver, or service owners."

[[architecture.rules]]
id = "ARCH-002"
source = "paraegox.runtime"
forbid = ["paraegox.decks", "paraegox.deployment"]
reason = "Runtime consumes runtime-owned apply contracts and must not import deployment or Deck implementation."

[[terminology.rules]]
id = "TERM-001"
identifier = "ModuleSpec"
replacement = "CardDefinition"
reason = "ADR-0002 accepts CardDefinition as the reusable capability contract."

[[terminology.rules]]
id = "TERM-002"
identifier = "ModuleRef"
replacement = "CardDefinitionRef"
reason = "ADR-0002 accepts CardDefinitionRef as the stable reference."

[[terminology.rules]]
id = "TERM-003"
identifier = "ApplicationTopology"
replacement = "DeckTopology"
reason = "DeckTopology is the proposed workload topology term; Application is not a current identity."

[[terminology.rules]]
id = "TERM-004"
identifier = "RemoteDomain"
replacement = "target RuntimeHost placement"
reason = "ExecutionDomain is local to RuntimeHost; remote placement is not a local domain."

[[terminology.rules]]
id = "TERM-005"
identifier = "LocalBus"
replacement = "PortBinding test fixture or Zenoh-backed PortBinding"
reason = "There is one production Fabric and no parallel local production bus."

[[terminology.rules]]
id = "TERM-006"
identifier = "MemoryBus"
replacement = "PortBinding test fixture"
reason = "A test fixture must not become a second public Bus product."

[[terminology.rules]]
id = "TERM-007"
identifier = "MemoryPortBinding"
replacement = "PortBinding test fixture"
reason = "Test implementations do not create a second public live-binding term."

[[terminology.rules]]
id = "TERM-008"
identifier = "ZenohBinding"
replacement = "PortBinding"
reason = "PortBinding is the only public live-binding term."
